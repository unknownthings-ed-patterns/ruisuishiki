/**
 * 用語辞書（公式の景色や問題文で使う前提知識への跳び方）。
 *
 * derivation や問題文の中で [用語名] と書くと、自動で辞書を引いて
 * ツールチップ → 詳細パネル（易しい例・図・関連系列）を段階的に開ける。
 *
 * 辞書にない用語は素通し（表示は変わらない）。
 *
 * ## 設計方針
 *
 * - **short**：1 文の超簡易説明。ホバーで即見られる
 * - **easy**：易しい例で噛み砕いた説明。「もっと詳しく」を押すと開く
 * - **figureMarker**：必要なら図のマーカー（components/Math.tsx の SVG）
 * - **relatedSeriesId**：もっと体系的に学べる関連系列
 *
 * ## ビジョン
 *
 * 小学生でもやる気のある子が、リンクをたどって高校・大学数学まで
 * 登っていけるよう、用語の説明は易しい例から始める。
 * 「下から見ても登れる、上から見ても降りられる」知識のグラフ。
 *
 * relatedSeriesId は学習者ビューの seriesId を直接指す。
 * 新しい系列を追加したら、関連する用語の relatedSeriesId も更新する。
 */

export type GlossaryEntry = {
  /**
   * 読み仮名（ひらがな）。辞書ページの一覧表示で「読み」順ソートに使う。
   * 例：「領域」→「りょういき」、「ベクトル」→「べくとる」。
   * 未設定の場合は key 文字列の UTF-16 順で fallback ソート。
   */
  reading?: string;
  /** 1 文の超簡易説明（ホバーで見える） */
  short: string;
  /**
   * 易しい例で噛み砕いた説明（「もっと詳しく」で開ける）。
   * 旧フォーマット：1ブロックで定義・例を一気に書く。
   * meaning が設定されているエントリでは無視される。
   * markdown 風： **強調**、$...$（インライン数式）、$$...$$（ディスプレイ数式）、<<図マーカー>> 対応。
   */
  easy?: string;
  /**
   * 構造化された辞書ページ。ワークシート方式（教師実践に由来）：
   *   §1 meaning   定義（説明）と例
   *   §2 figures   図や絵などをかいてみよう
   *   §3 scenes    使う場面 / 生活の中では？
   *   §4 relatedTerms にていることば / なかまのことば
   * これらのフィールドのうち meaning が設定されていれば構造化レイアウトで描画する。
   * 空のセクションは省略される（書けていない＝書く対象が見える）。
   */
  meaning?: string;
  figures?: string;
  scenes?: string;
  relatedTerms?: string;
  /** もっと体系的に学べる関連系列のID（公式の景色を持つ系列） */
  relatedSeriesId?: string;
  /**
   * 「自分で例を作って確かめる」インタラクティブセクション。
   * 結城浩さんのパタン「例示は理解の試金石」を実装する核。
   * 受動的な「読む」から能動的な「作る」への橋渡し。
   */
  example?: ExampleSpec;
};

/** 入力フィールドの仕様 */
export type ExampleInput = {
  /** 内部キー（verify 関数の引数）。例: "a", "b", "c" */
  name: string;
  /** 表示ラベル。例: "a"、"a（短辺）" */
  label: string;
};

/** 検証結果 */
export type VerifyResult = {
  ok: boolean;
  /** 計算過程の詳細。「a² + b² = 25、c² = 25」など。常に表示 */
  detail: string;
  /** 評価メッセージ。「一致！」「惜しい！」など。常に表示 */
  message: string;
  /** 失敗時の前向きなヒント。失敗時のみ表示 */
  hint?: string;
  /** 重複判定キー。「3,4,5」のように同じ例を識別する */
  canonicalKey: string;
  /** 自明な例（0だけなど）か。控えめなフィードバックにする */
  trivial?: boolean;
};

/** 「自分で例を作ってみる」セクションの仕様 */
export type ExampleSpec = {
  /** プロンプト文。入力欄の上に表示 */
  prompt: string;
  /** 入力フィールド群 */
  inputs: ExampleInput[];
  /** 検証関数。入力値から結果を返す */
  verify: (values: Record<string, number>) => VerifyResult;
};

export const GLOSSARY: Record<string, GlossaryEntry> = {
  /* ===== ベクトル ===== */
  ベクトル: {
    reading: "べくとる",
    short: "向きと大きさを持った矢印で表される量。位置・力・速度などを扱う基本道具。",
    meaning: `**「向き」と「大きさ」を持つ、$1$ 本の矢印。**

「東に $3$ m、北に $4$ m 進んだ」——この移動は、出発点から行き先までを **$1$ 本の矢印** で表せます。これがベクトル。

矢印には **$2$ つの情報** があります：

- **向き**：どの方向を指しているか
- **大きさ（長さ）**：どれだけ進んだか

座標平面では、$\\vec{v} = (3, 4)$ のように **$2$ つの数の組** で表します。「$x$ 軸方向に $3$、$y$ 軸方向に $4$ 進んだ矢印」という意味。

**ベクトルどうしの足し算**：

$$(3, 4) + (1, 2) = (4, 6)$$

「成分ごとに足す」——これは「最初の矢印の終点から $2$ 本目の矢印を継ぎ足した、新しい矢印」を意味します。

**スカラー倍**（数とベクトルの掛け算）：

$$3 \\times (1, 2) = (3, 6)$$

矢印の長さを $3$ 倍に、向きはそのまま。

────────

**もっと深く** — なぜベクトルが便利か

**$1$ つの記号で、向きと大きさを同時に扱える**。これがベクトルの威力です。

**例**：「東向きに時速 $30$ km、南風 $10$ m/s、力 $5$ N が斜め上向きに」など、現実世界の **向きを持つ量** はすべてベクトルで書ける。

**$3$ 次元・$n$ 次元への拡張**：平面では $(a, b)$、空間では $(a, b, c)$、$n$ 次元では $(a_1, a_2, \\dots, a_n)$——次元が増えても **同じ規則** で扱える。

**物理・工学の言語**：力学・電磁気学・量子力学はすべてベクトル（やテンソル）の言葉で書かれます。「物理学者の数学」がベクトル。

**機械学習**：データ点は高次元ベクトル（画像が $1024$ 次元、文章が $768$ 次元のベクトル）として扱われます。`,
    scenes: `- **物理：力・速度・加速度**：すべてベクトル
- **航海・航空**：風の影響・推力の合成
- **コンピュータグラフィクス**：3 次元の位置・回転
- **機械学習・データサイエンス**：高次元データはベクトル
- **GPS・地図**：位置の相対表現
- **ゲーム**：キャラクターの移動・物理エンジン`,
    relatedTerms: `- [内積]：2 つのベクトルの「向きの一致度」
- [ベクトルの大きさ]：矢印の長さ
- [法線ベクトル]：直線・曲線に垂直なベクトル`,
    relatedSeriesId: "algebra2_dot_01",
  },
  内積: {
    reading: "ないせき",
    short: "2 つのベクトルの「向きの一致度」を表す数。成分どうしを掛けて足す。",
    meaning: `**$2$ つの矢印の「向きの相性」を $1$ つの数で表す。**

ベクトル $\\vec{u} = (a, b)$ と $\\vec{v} = (c, d)$ の **内積**：

$$\\vec{u} \\cdot \\vec{v} = ac + bd$$

「**成分どうしを掛けて、足す**」——これだけ。

**何が分かるか**：内積の **符号** で、$2$ つのベクトルの関係が一目で分かります。

| 内積 | 関係 | 意味 |
|---|---|---|
| **正** | $0°$ 〜 $90°$ 未満 | だいたい同じ向き（鋭角） |
| **$0$** | $90°$ | **垂直**（直角） |
| **負** | $90°$ 〜 $180°$ | 反対向き（鈍角） |

特に「**$0$ のとき垂直**」が応用上とても便利。

**例**：

- $(1, 0)$ と $(0, 1)$ の内積 $= 0$ → 垂直 ✓
- $(3, 4)$ と $(4, -3)$ の内積 $= 12 - 12 = 0$ → 垂直 ✓
- $(1, 1)$ と $(1, 1)$ の内積 $= 2$ → 同じ向き（実は $|\\vec{v}|^2$）

────────

**もっと深く** — 内積のもう一つの顔

**幾何的な定義**：

$$\\vec{u} \\cdot \\vec{v} = |\\vec{u}| \\cdot |\\vec{v}| \\cdot \\cos \\theta$$

ここで $\\theta$ は $2$ ベクトルのなす角。

「**$2$ ベクトルの長さの積に、なす角のコサインを掛けた量**」——これが本質的な定義。

成分計算 $ac + bd$ と幾何定義 $|\\vec{u}||\\vec{v}| \\cos \\theta$ が **同じ値** を返すのは、ピタゴラスの定理と余弦定理から導けます。

**応用：物理の「仕事」**：力 $\\vec{F}$ で物を $\\vec{s}$ だけ動かしたときの **仕事量**：

$$W = \\vec{F} \\cdot \\vec{s} = |\\vec{F}| |\\vec{s}| \\cos \\theta$$

「**力の向きと動きの向きが一致する成分だけが、仕事になる**」——内積の幾何的意味そのもの。

**機械学習**：高次元ベクトルの内積は「**類似度**」として使われます。コサイン類似度 $\\dfrac{\\vec{u} \\cdot \\vec{v}}{|\\vec{u}||\\vec{v}|} = \\cos \\theta$ は文書や画像の似ている度合いの指標。`,
    scenes: `- **垂直判定**：内積 $= 0$ で垂直
- **物理：仕事**：$W = \\vec{F} \\cdot \\vec{s}$
- **CG・ゲーム**：光と面の角度（コサインで明るさが決まる）
- **機械学習**：コサイン類似度
- **検索エンジン**：文書ベクトルの類似度を内積で計算`,
    relatedTerms: `- [ベクトル]：内積を作る対象
- [法線ベクトル]：内積 $= 0$ の関係を使って定義
- [コサイン]：内積の幾何的定義に登場`,
    relatedSeriesId: "algebra2_dot_01",
  },
  法線ベクトル: {
    reading: "ほうせんべくとる",
    short: "直線や曲線・面に垂直な方向を表すベクトル。直線 ax+by+c=0 なら (a, b) が法線ベクトル。",
    meaning: `**直線にまっすぐ「立てた」矢印——その向きを表すベクトル。**

直線 $ax + by + c = 0$ の **法線ベクトル** は $(a, b)$。

**直線の式の係数 $a, b$ を取り出すだけ** で、その直線に垂直な方向の矢印が分かる——これが法線ベクトルの便利さです。

**例**：

- 直線 $3x + 4y - 12 = 0$ → 法線ベクトル $(3, 4)$
- 直線 $y = 2x + 1$ →（一般形に直すと $2x - y + 1 = 0$ なので）法線ベクトル $(2, -1)$

**なぜ $(a, b)$ が法線になるのか**：

直線 $ax + by + c = 0$ 上の $2$ 点 $P_1, P_2$ を取ると、向きベクトル $\\vec{P_1 P_2}$ は $(b, -a)$ の倍数になります。これと $(a, b)$ の [内積] は $ab - ab = 0$ → 垂直。

────────

**もっと深く** — 点と直線の距離公式の正体

[点と直線の距離] の公式：

$$d = \\dfrac{|ax_0 + by_0 + c|}{\\sqrt{a^2 + b^2}}$$

これは実は「**点 $(x_0, y_0)$ から直線への、法線ベクトル方向の射影の大きさ**」を計算しています。

- 分母 $\\sqrt{a^2 + b^2}$ は **法線ベクトル $(a, b)$ の大きさ**
- 分子 $|ax_0 + by_0 + c|$ は **点から直線への「ずれ」を法線方向で測ったもの**

「直線に垂直に降ろした距離」を法線ベクトルで計算しているわけです。

**$3$ 次元では平面の法線**：平面 $ax + by + cz + d = 0$ の法線ベクトルは $(a, b, c)$。光と面の角度・面の向きの判定に使われます。`,
    scenes: `- **[点と直線の距離] の公式の正体**
- **直線と直線の角度**：法線ベクトルどうしの内積から
- **CG：面の向き**（明るさ・カメラ視野・隠面消去）
- **物理：壁との衝突**（法線方向で跳ね返る）
- **ロボット：障害物回避**（壁の法線方向に進路修正）`,
    relatedTerms: `- [ベクトル] / [内積]：法線ベクトルの基盤
- [点と直線の距離]：法線ベクトル方向の射影
- [一般形]：法線ベクトルが直接読める形`,
    relatedSeriesId: "algebra2_dot_01",
  },
  ベクトルの大きさ: {
    reading: "べくとるのおおきさ",
    short: "ベクトル (a, b) の長さ。$\\sqrt{a^2 + b^2}$——ピタゴラスの定理そのもの。",
    meaning: `**矢印の「長さ」——ピタゴラスの定理がそのまま顔を出す。**

ベクトル $\\vec{v} = (a, b)$ の **大きさ** $|\\vec{v}|$ は：

$$|\\vec{v}| = \\sqrt{a^2 + b^2}$$

これは [ピタゴラス数] の定理を、**横 $a$・縦 $b$ の直角三角形** に当てただけ。斜辺の長さがそのままベクトルの大きさ。

**例**：

| ベクトル | 大きさ |
|---|---|
| $(3, 4)$ | $\\sqrt{9 + 16} = \\sqrt{25} = 5$ |
| $(5, 12)$ | $\\sqrt{25 + 144} = \\sqrt{169} = 13$ |
| $(1, 1)$ | $\\sqrt{2} \\approx 1.41$ |
| $(0, 1)$ | $1$ |

**身近な例**：地図上で「東に $3$ km、北に $4$ km」進んだ場所までの **直線距離** は $5$ km——これは $(3, 4)$ の大きさそのもの。

────────

**もっと深く** — $3$ 次元・$n$ 次元での大きさ

**$3$ 次元**：$\\vec{v} = (a, b, c)$ の大きさは $\\sqrt{a^2 + b^2 + c^2}$。ピタゴラスを $3$ 次元に拡張したもの。

**$n$ 次元**：$\\vec{v} = (a_1, a_2, \\dots, a_n)$ の大きさは $\\sqrt{a_1^2 + a_2^2 + \\dots + a_n^2}$。

「**成分の $2$ 乗の和のルート**」——次元が増えても同じ規則。

**内積との関係**：$|\\vec{v}|^2 = \\vec{v} \\cdot \\vec{v}$（自分との [内積]）。

**単位ベクトル**：大きさ $1$ のベクトル。$\\vec{v} / |\\vec{v}|$ で、どんなベクトルも単位ベクトルに正規化できる。「向きだけを取り出す」操作です。

**距離公式**：$2$ 点 $A(x_1, y_1)$ と $B(x_2, y_2)$ の距離は、$\\vec{AB} = (x_2 - x_1, y_2 - y_1)$ の大きさ：

$$AB = \\sqrt{(x_2 - x_1)^2 + (y_2 - y_1)^2}$$`,
    scenes: `- **2 点間の距離**：座標から計算
- **物理：速さの大きさ**（速度ベクトルの長さ）
- **力の大きさ**：合力の大きさ
- **機械学習**：特徴ベクトルの「ノルム」（大きさ）でデータを評価
- **画像処理**：エッジの強さ（勾配ベクトルの大きさ）`,
    relatedTerms: `- [ベクトル]：大きさを測る対象
- [ピタゴラス数]：大きさの計算の基盤
- [内積]：$|\\vec{v}|^2 = \\vec{v} \\cdot \\vec{v}$`,
    relatedSeriesId: "algebra2_vec_mag_01",
  },

  /* ===== 三角関数 ===== */
  三角関数: {
    reading: "さんかくかんすう",
    short: "角度を入れると辺の比を返す関数。sin, cos, tan の総称。三角比を $0°$ 以外にも拡張したもの。",
    meaning: `**角度を入れると、長さの比を返す関数。**

直角三角形を思い浮かべます。$3$ 辺のうち、**$2$ 辺の比は、角度だけで決まります**——三角形の大きさを変えても、角度が同じなら比は同じ。これが [三角比] の基本。

| 関数 | 意味 | 比 |
|---|---|---|
| $\\sin \\theta$ | サイン | 高さ ÷ 斜辺 |
| $\\cos \\theta$ | コサイン | 横 ÷ 斜辺 |
| $\\tan \\theta$ | タンジェント | 高さ ÷ 横 |

**よく使う値**：

| $\\theta$ | $\\sin$ | $\\cos$ | $\\tan$ |
|---|---|---|---|
| $0°$ | $0$ | $1$ | $0$ |
| $30°$ | $1/2$ | $\\sqrt{3}/2$ | $1/\\sqrt{3}$ |
| $45°$ | $\\sqrt{2}/2$ | $\\sqrt{2}/2$ | $1$ |
| $60°$ | $\\sqrt{3}/2$ | $1/2$ | $\\sqrt{3}$ |
| $90°$ | $1$ | $0$ | — |

────────

**もっと深く** — 単位円で全角度に拡張

直角三角形だと角度が $0°$〜$90°$ に限られますが、**単位円**（半径 $1$ の円）を使うと、$\\sin$ と $\\cos$ を **すべての角度** に拡張できます。

単位円の上で、原点から角度 $\\theta$ だけ回転した点の **座標が $(\\cos \\theta, \\sin \\theta)$**。だから：

- $\\theta = 90°$：$(\\cos 90°, \\sin 90°) = (0, 1)$
- $\\theta = 180°$：$(\\cos 180°, \\sin 180°) = (-1, 0)$
- $\\theta = 270°$：$(\\cos 270°, \\sin 270°) = (0, -1)$

回転して一周（$360°$）すると元に戻る——だから **周期 $360°$** の関数です。

**最重要の恒等式**：

$$\\sin^2 \\theta + \\cos^2 \\theta = 1$$

単位円の点 $(\\cos \\theta, \\sin \\theta)$ が「半径 $1$ の円上」にあること——ピタゴラスの定理がここでも顔を出します。

**波の言語**：$\\sin(\\omega t)$ という形は、音・光・電気信号・水面の波——あらゆる **周期的な現象** を表します。**フーリエ変換** は「あらゆる関数は $\\sin$ と $\\cos$ の重ね合わせで書ける」という驚きの定理。`,
    scenes: `- **波・振動**：音・光・電気・地震・水面
- **フーリエ変換**：信号処理・画像圧縮・音声認識
- **天文学**：惑星の周期運動
- **電気回路**：交流の電圧・電流
- **物理：単振動・振り子・ばね**
- **CG・3D グラフィクス**：回転変換`,
    relatedTerms: `- [サイン] / [コサイン] / [タンジェント]：三角関数のメンバー
- [三角比]：直角三角形での出発点
- **単位円**：すべての角度に拡張する道具`,
    relatedSeriesId: "algebra2_trig_period_01",
  },

  /* ===== 数Ⅰ・A 2次関数 ===== */
  平方完成: {
    reading: "へいほうかんせい",
    short: "x² + bx + c を (x + b/2)² + (c − b²/4) の形に書き直す変形。",
    meaning: `**ばらけた 2 次の式を「ひとかたまりの 2 乗」に押し込む変形。**

$x^2 + bx + c$ を、$(x + p)^2 + q$ の形に書き直す——これが **平方完成**。

なぜ「平方完成」と呼ぶか？それは文字通り、$x \\times x$ の正方形に $x \\times b$ の長方形を加えた図形を、辺 $(x + b/2)$ の大きな正方形に「完成」させる手順だから。

<<COMPLETE_SQUARE>>

**算法**：

$$x^2 + bx + c = \\left(x + \\dfrac{b}{2}\\right)^2 - \\dfrac{b^2}{4} + c = \\left(x + \\dfrac{b}{2}\\right)^2 + \\left(c - \\dfrac{b^2}{4}\\right)$$

「**$b/2$ の 2 乗を加えて、すぐ引き戻して調整する**」という $1$ つの操作。

────────

**もっと深く** — なぜこの形にしたいか

$(x + p)^2 + q$ の形なら、いろいろなことが一気に見えます：

- $(x+p)^2 \\geq 0$ なので、**最小値が $q$**（$x = -p$ のとき）
- 2 次関数の **[頂点] $(-p, q)$** が読める
- 円の一般形 $x^2 + y^2 + lx + my + n = 0$ を平方完成すると **中心と半径** が現れる
- **解の公式** $x = \\dfrac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}$ も、平方完成から導かれる

「**ばらけた 2 次を、ひとかたまりの 2 乗に押し込む**」——この手口は数学のあちこちで顔を出す、基本中の基本の道具です。`,
    scenes: `- **2 次関数の最小・最大**：頂点を読むため
- **円の方程式**：一般形 → 標準形に直して中心と半径を読む
- **2 次方程式の解の公式の導出**：$ax^2 + bx + c = 0$ の両辺を平方完成して解く
- **[判別式]**：$b^2 - 4ac$ は平方完成した余りそのもの
- **積分**：$\\int dx / (x^2 + bx + c)$ のような積分で、分母を平方完成してから解く`,
    relatedTerms: `- [頂点]：平方完成された $(x+p)^2 + q$ から、頂点 $(-p, q)$ が直接読める
- [一般形] / [標準形]：直線・円・放物線の方程式の書き換えに必須
- [判別式]：$b^2 - 4ac$ は平方完成の「あまり」の符号を反映する`,
    relatedSeriesId: "adv_quad_min_01",
  },
  頂点: {
    reading: "ちょうてん",
    short: "放物線の一番低い点（または一番高い点）。お椀の底、または逆さお椀のてっぺん。",
    meaning: `**放物線の「お椀の底」または「逆さお椀のてっぺん」。**

$2$ 次関数 $y = ax^2 + bx + c$ のグラフ（放物線）には、**最も低い点（または高い点）** が必ず $1$ つあります。これが **頂点**。

<<PARABOLA_WITH_LABELS>>

- $a > 0$（下に凸、お椀の形）：底 → **最小値**
- $a < 0$（上に凸、逆さお椀）：てっぺん → **最大値**

**頂点の座標**：

$$\\text{頂点の x 座標} = -\\dfrac{b}{2a}$$

$a = 1$（$y = x^2 + bx + c$）なら：

$$\\text{頂点} = \\left(-\\dfrac{b}{2},\\ c - \\dfrac{b^2}{4}\\right)$$

**どうやって出すか**：[平方完成] で $y = (x + b/2)^2 + (c - b^2/4)$ の形に直すと、頂点が **そのまま顔を出す**。

────────

**もっと深く** — 頂点は「対称軸」も決める

放物線は **左右対称な曲線**。その **対称軸** は、頂点を通る縦の直線：

$$x = -\\dfrac{b}{2a}$$

頂点の $x$ 座標と同じ。だから「頂点が分かれば、対称軸も分かる」。`,
    scenes: `- **最大／最小問題**：利益・面積・速度を最大化／最小化する点を求める
- **橋・建築**：放物線アーチの最高点
- **物理：投射運動**：投げ上げた物の最高点（投げ上げの軌跡が放物線）
- **パラボラアンテナ・反射鏡**：頂点に焦点が来る性質を利用
- **設計の最適化**：「ここがいちばん効率がよい」点を頂点として求める`,
    relatedTerms: `- [平方完成]：頂点を取り出すための一番の道具
- [判別式]：$b^2 - 4c$ の符号で、頂点が $x$ 軸の上か下かが決まる`,
    relatedSeriesId: "adv_quad_min_01",
  },
  判別式: {
    reading: "はんべつしき",
    short: "2 次方程式 ax² + bx + c = 0 の解の公式のルートの中身、D = b² − 4ac。符号で解の種類（異なる 2 実数解・重解・異なる 2 虚数解）が決まる。",
    meaning: `**「$2$ 次方程式の解が、どんな種類になるか」を、解かずに見抜く目印。**

$2$ 次方程式 $ax^2 + bx + c = 0$ の解の公式：

$$x = \\dfrac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}$$

このルートの中身 $b^2 - 4ac$ が **判別式** $D$。

**$D$ の符号で解の種類が決まる**（[複素数] の世界も含めて）：

| $D$ の符号 | ルートの中身 | 解の種類 |
|---|---|---|
| $D > 0$ | 正 → ふつうのルート | **異なる $2$ つの実数解** |
| $D = 0$ | $0$ → ルートが消える | **実数の [重解]**（同じ値 $1$ つ） |
| $D < 0$ | 負 → $i$ が顔を出す | **異なる $2$ つの虚数解**（[共役複素数] のペア） |

**例**：

- $x^2 + 4x + 3 = 0$ → $D = 16 - 12 = 4 > 0$ → 異なる $2$ 実数解
- $x^2 + 4x + 4 = 0$ → $D = 16 - 16 = 0$ → 重解
- $x^2 + 2x + 5 = 0$ → $D = 4 - 20 = -16 < 0$ → 異なる $2$ 虚数解（$-1 \\pm 2 i$）

中学までは「$D < 0$ なら解なし」と扱っていたが、[複素数] を導入した数Ⅱ・B 以降は「**$D < 0$ なら異なる $2$ 虚数解**」と読み替える——これが「数の世界を $1$ 段広げた」効果。

**「解かなくても、解の種類が分かる」**——これが判別式の威力。

────────

**もっと深く** — 判別式と放物線のグラフ

$D$ の符号は、放物線 $y = ax^2 + bx + c$ と $x$ 軸の関係そのもの：

| $D$ | グラフ |
|---|---|
| $D > 0$ | $x$ 軸と **$2$ 点** で交わる |
| $D = 0$ | $x$ 軸に **接する**（$1$ 点で触れる） |
| $D < 0$ | $x$ 軸とは **離れている**（共有点なし） |

これは「$2$ 次方程式の解 $=$ 放物線が $x$ 軸と交わる点の $x$ 座標」だから自然です。

**判別式の正体**：$D/(4a) = $ [頂点] の $y$ 座標と関係する。頂点が $x$ 軸より下なら $2$ 点で交わる → $D > 0$。`,
    scenes: `- **2 次方程式の解の個数判定**：解かなくても個数が分かる
- **円と直線の位置関係**：[点と直線の距離] と判別式の併用で「接する／交わる／離れる」を判定
- **不等式 $ax^2 + bx + c \\geq 0$ が常に成り立つ条件**：$D \\leq 0$（つねに $x$ 軸より上 or 接する）
- **2 次関数のグラフ概形**：$D$ の符号で「$x$ 軸を切るか」が分かる
- **物理・工学**：振動・減衰の系で「振動するか、しないか」の境目を $D$ の符号で判定`,
    relatedTerms: `- [平方完成]：判別式は平方完成の余りの符号を反映する
- [頂点]：頂点の $y$ 座標と判別式は直結
- [解と係数の関係]：解の和と積を、解そのものを出さずに知る別の道具`,
    relatedSeriesId: "algebra_disc_01",
  },
  解と係数の関係: {
    reading: "かいとけいすうのかんけい",
    short: "ax² + bx + c = 0 の解の和は −b/a、積は c/a。解そのものを出さずに和と積が分かる。[複素数] 解でも同じ。",
    meaning: `**解そのものを知らなくても、和と積はすぐ分かる。**

$2$ 次方程式 $a x^2 + b x + c = 0$ の $2$ つの解を $\\alpha, \\beta$ とすると：

$$\\alpha + \\beta = -\\dfrac{b}{a}, \\quad \\alpha \\beta = \\dfrac{c}{a}$$

**解の和は $-b/a$、解の積は $c/a$**——係数を読んで割るだけ。$a = 1$ なら $\\alpha + \\beta = -b, \\alpha \\beta = c$（定数項そのまま）。

**なぜ成り立つか**：解が $\\alpha, \\beta$ なら、$2$ 次式は [因数分解] して $a (x - \\alpha)(x - \\beta) = 0$ の形になります。展開して整理すると：

$$a x^2 - a (\\alpha + \\beta) x + a \\alpha \\beta = 0$$

元の式 $a x^2 + b x + c = 0$ と係数を比較すれば、$- a (\\alpha + \\beta) = b$、$a \\alpha \\beta = c$ ——つまり $\\alpha + \\beta = -b/a$、$\\alpha \\beta = c/a$。

**[複素数] 解でも同じ**：実数係数の $2$ 次方程式の解が虚数（$p \\pm q i$ の共役ペア）でも、和 $2 p$ と積 $p^2 + q^2$ は実数になります。だから「係数 $b, c$ が実数なら、和と積も実数」は破れない。

────────

**もっと深く** — 解を出さずに対称式が計算できる

「**対称式**」（$\\alpha$ と $\\beta$ を入れ替えても同じ値の式）は、すべて **和と積だけで表せる**：

| 対称式 | 和と積で表すと |
|---|---|
| $\\alpha^2 + \\beta^2$ | $(\\alpha + \\beta)^2 - 2\\alpha\\beta$ |
| $\\dfrac{1}{\\alpha} + \\dfrac{1}{\\beta}$ | $\\dfrac{\\alpha + \\beta}{\\alpha \\beta}$ |
| $\\alpha^3 + \\beta^3$ | $(\\alpha + \\beta)^3 - 3\\alpha\\beta(\\alpha + \\beta)$ |

だから「$x^2 + 5x + 6 = 0$ の $2$ 解の $2$ 乗の和は？」のような問いは、**解 $\\alpha, \\beta$ を実際に出さずに** $(\\alpha + \\beta)^2 - 2\\alpha\\beta = (-5)^2 - 2 \\cdot 6 = 13$ で出せる。

**[判別式] との対比**：判別式は「解の **個数・種類**」を見抜く。解と係数の関係は「解の **和と積**」を見抜く。どちらも「解そのものを出さずに重要情報を得る」道具です。

**$n$ 次への拡張（Vieta の公式）**：$a x^n + \\cdots + a_0 = 0$ の解 $\\alpha_1, \\dots, \\alpha_n$ について、**$k$ 次の対称式** $\\sum \\alpha_{i_1} \\cdots \\alpha_{i_k}$ は、係数比から計算できる（一般の Vieta の公式）。$2$ 次の場合の「和と積」は、その最も基本的な例。`,
    scenes: `- **数学の試験**：$\\alpha + \\beta, \\alpha\\beta, \\alpha^2 + \\beta^2$ など対称式の値を求める問題
- **多項式の因数分解**：解が $\\alpha, \\beta$ と分かれば $(x-\\alpha)(x-\\beta)$
- **物理・工学**：2 次の特性方程式の解の和・積から、システムの応答（時定数の和・積）を読む
- **整数問題**：与えられた和と積を持つ整数解を探す`,
    relatedTerms: `- [判別式]：解の個数を見抜く道具（こちらは和と積）
- [因数分解]：解 $\\alpha, \\beta$ から $(x - \\alpha)(x - \\beta)$ を作る`,
    relatedSeriesId: "adv_complex_root_coefficient_01",
  },
  因数分解: {
    reading: "いんすうぶんかい",
    short: "多項式を「掛け算の形」に直す。展開の逆向きの操作。",
    meaning: `**ばらけた式を、もとの「掛け算」に戻す。**

展開と因数分解は **裏返しの関係**：

| 展開 | $(x + 2)(x + 3) = x^2 + 5x + 6$ |
|---|---|
| 因数分解 | $x^2 + 5x + 6 = (x + 2)(x + 3)$ |

**$x^2 + Cx + D$ を因数分解する手順**：**和が $C$、積が $D$** になる $2$ つの数 $a, b$ を探す：

$$x^2 + Cx + D = (x + a)(x + b)$$

**例**：$x^2 + 5x + 6$。和が $5$、積が $6$ になる $2$ 数は $(2, 3)$。だから $(x + 2)(x + 3)$。

**例**：$x^2 - 7x + 12$。和が $-7$、積が $12$ → $(-3, -4)$。だから $(x - 3)(x - 4)$。

[解と係数の関係] と同じ「**和と積**」の発想。

────────

**もっと深く** — なぜ因数分解できると便利か

**$2$ 次方程式を解ける**：$(x - \\alpha)(x - \\beta) = 0$ なら、「**$AB = 0$ なら $A = 0$ または $B = 0$**」の原理で、$x = \\alpha$ または $x = \\beta$。

**因数分解できない場合の対処**：因数分解できない（または見つけにくい）$2$ 次式は、[平方完成] で形を整えるか、解の公式で解く。判別式 $D < 0$ なら、**実数の範囲では因数分解できません** が——[複素数] の世界では話が変わります。

**複素数の世界では、すべての $2$ 次式が因数分解できる**：解の公式 $x = \\dfrac{-b \\pm \\sqrt{b^2 - 4 a c}}{2 a}$ は複素数の範囲では必ず $2$ つの解 $\\alpha, \\beta$ を返すので、

$$a x^2 + b x + c = a (x - \\alpha)(x - \\beta)$$

として **例外なく** 因数分解できます。$a \\ne 1$ のときは先頭に $a$ が必要（$x^2$ の係数を合わせるため）。たとえば $x^2 + 4 x + 8 = (x + 2 - 2 i)(x + 2 + 2 i)$、$2 x^2 + 3 x - 2 = 2 (x - 1/2)(x + 2)$。「**$2$ 次式に分解不可能な式は存在しない**」——複素数の世界が広げた最大のご褒美のひとつ。

**公式集**：

| 形 | 因数分解 |
|---|---|
| $a^2 - b^2$ | $(a + b)(a - b)$ |
| $a^2 + 2ab + b^2$ | $(a + b)^2$ |
| $a^2 - 2ab + b^2$ | $(a - b)^2$ |
| $a^3 - b^3$ | $(a - b)(a^2 + ab + b^2)$ |
| $a^3 + b^3$ | $(a + b)(a^2 - ab + b^2)$ |

「$a^2 - b^2 = (a+b)(a-b)$」は **和と差の積** とも呼ばれ、計算の高速化や $\\sqrt{a^2 - b^2}$ の処理で頻出。`,
    scenes: `- **2 次方程式を解く**：因数分解できれば即解ける
- **分数式の計算**：分母分子を因数分解して約分
- **不等式**：$f(x) > 0$ の解を、因数分解した形 $(x - \\alpha)(x - \\beta)$ から読む
- **暗算**：$103 \\times 97 = (100+3)(100-3) = 10000 - 9 = 9991$ のような計算で活躍`,
    relatedTerms: `- [解と係数の関係]：和と積から因数分解、または因数分解から和と積
- [判別式]：因数分解できるかどうかの目印
- [平方完成]：因数分解と並ぶ 2 次式の処理法
- [素因数分解]：整数を「素数の積」に分解する、似た発想`,
    relatedSeriesId: "algebra_factoring_01",
  },

  /* ===== 整数の性質 ===== */
  ピタゴラス数: {
    reading: "ぴたごらすすう",
    short: "$a^2 + b^2 = c^2$ を満たす整数の組。3 辺がすべて整数の直角三角形。",
    meaning: `**直角三角形の 3 辺がすべて整数になる、めったにない組。**

直角三角形の 3 辺の関係はピタゴラスの定理 $a^2 + b^2 = c^2$。$a, b$ にどんな整数を入れても、ふつう $c$ は無理数（ルートを含む数）になります。

ところが、ごく稀に **3 つともきれいな整数で揃う** ことがある——その組が **ピタゴラス数**。

| 組 | 確かめ |
|---|---|
| $(3, 4, 5)$ | $9 + 16 = 25$ ✓ |
| $(5, 12, 13)$ | $25 + 144 = 169$ ✓ |
| $(8, 15, 17)$ | $64 + 225 = 289$ ✓ |
| $(7, 24, 25)$ | $49 + 576 = 625$ ✓ |
| $(6, 8, 10)$ | $36 + 64 = 100$ ✓ |

最後の $(6, 8, 10)$ は $(3, 4, 5)$ を $2$ 倍しただけ。$3$ 辺がすべて [互いに素] な組を **原始ピタゴラス数** と呼びます。

────────

**もっと深く** — 無限に作る公式

ピタゴラス数は偶然できているわけではなく、**無限に作り出せます**。

$m > n > 0$ で [互いに素] な整数 $m, n$ を選ぶと：

$$(m^2 - n^2,\\ 2mn,\\ m^2 + n^2)$$

がピタゴラス数。たとえば：

- $m = 2, n = 1$ → $(3, 4, 5)$
- $m = 3, n = 2$ → $(5, 12, 13)$
- $m = 4, n = 1$ → $(15, 8, 17)$
- $m = 4, n = 3$ → $(7, 24, 25)$

**フェルマーの最終定理との関係**：$a^2 + b^2 = c^2$ には整数解が無限にある（ピタゴラス数）。でも $a^n + b^n = c^n$ で $n \\geq 3$ では整数解が **ない**——これがフェルマーの最終定理。「$n = 2$ だけが特別」という驚きの事実です。`,
    scenes: `- **算数・数学の問題**：分母にルートが残らないきれいな答えにするための「カリキュラム上のしかけ」（直角三角形の計算問題でよく使われる）
- **建築・大工**：「$3:4:5$ の三角形を作れば直角」——古代から職人の道具
- **測量**：直角を出すために、紐に $12$ 等分の結び目を作って $3:4:5$ の三角形にする手法（古代エジプトでも使われた）
- **整数論**：「ディオファントス方程式」の最初の例として、数学の歴史に大きく登場`,
    relatedTerms: `- [互いに素]：原始ピタゴラス数は $a, b, c$ が互いに素
- [素因数分解]：ピタゴラス数の構造を解析する道具`,
    relatedSeriesId: "middle_pythagorean_01",
    example: {
      prompt: "3 つの整数 a, b, c を入れて、ピタゴラス数（$a^2 + b^2 = c^2$）になっているか確かめてみよう。",
      inputs: [
        { name: "a", label: "a" },
        { name: "b", label: "b" },
        { name: "c", label: "c（斜辺）" },
      ],
      verify: ({ a, b, c }) => {
        // 整数の有効性確認
        if (
          !Number.isInteger(a) ||
          !Number.isInteger(b) ||
          !Number.isInteger(c)
        ) {
          return {
            ok: false,
            detail: "ピタゴラス数は「整数」の組です。",
            message: "整数を入れてみよう。",
            hint: "たとえば 3, 4, 5 のような正の整数を試してみよう。",
            canonicalKey: `${a},${b},${c}`,
          };
        }
        const lhs = a * a + b * b;
        const rhs = c * c;
        // 正規化キー：a, b は順序問わない、c は別
        const [sa, sb] = [a, b].sort((x, y) => x - y);
        const key = `${sa},${sb},${c}`;

        // 0 を含む組は自明
        const trivial = a === 0 || b === 0 || c === 0;

        if (lhs === rhs) {
          if (trivial) {
            return {
              ok: true,
              detail: `$a^2 + b^2 = ${lhs}$、$c^2 = ${rhs}$`,
              message:
                "計算上は一致していますが、$a$ や $b$ や $c$ に 0 が入っていると、ふつうの直角三角形にはなりません。",
              hint: "0 以外の正の整数で試してみよう。たとえば (3, 4, 5) や (5, 12, 13)。",
              canonicalKey: key,
              trivial: true,
            };
          }
          if (a < 0 || b < 0 || c < 0) {
            return {
              ok: true,
              detail: `$a^2 + b^2 = ${lhs}$、$c^2 = ${rhs}$`,
              message:
                "数式としては一致していますが、辺の長さなので正の整数で考えるのがふつうです。",
              hint: "正の整数で試してみよう。",
              canonicalKey: key,
              trivial: true,
            };
          }
          return {
            ok: true,
            detail: `$a^2 + b^2 = ${a}^2 + ${b}^2 = ${lhs}$、$c^2 = ${c}^2 = ${rhs}$`,
            message: "ぴったり一致！これはピタゴラス数の組です。",
            canonicalKey: key,
          };
        }
        // ヒントを失敗の様子に応じて
        let hint: string;
        if (lhs > rhs) {
          hint = `$c$ が小さすぎるようです。$c$ をもう少し大きくしてみよう。`;
        } else {
          hint = `$c$ が大きすぎるようです。$c$ を小さくするか、$a, b$ を大きくしてみよう。`;
        }
        return {
          ok: false,
          detail: `$a^2 + b^2 = ${a}^2 + ${b}^2 = ${lhs}$、$c^2 = ${c}^2 = ${rhs}$`,
          message: `$${lhs}$ と $${rhs}$ で違うので、この組はピタゴラス数ではありません。`,
          hint,
          canonicalKey: key,
        };
      },
    },
  },
  互いに素: {
    reading: "たがいにそ",
    short: "2 つの整数が、1 以外の共通の約数を持たない関係。",
    meaning: `2 つの整数 $a, b$ について、**両方を割り切る数が $1$ しかない** とき、$a$ と $b$ は **互いに素** といいます。最大公約数（GCD）が $1$ の関係、と言い換えてもよいです。

**例で確かめる**：

| 組 | 共通の約数 | 互いに素？ |
|---|---|---|
| $(3, 4)$ | $1$ だけ | ✓ そう |
| $(5, 12)$ | $1$ だけ | ✓ そう |
| $(6, 9)$ | $1, 3$ | ✗ 違う（$3$ で両方割れる） |
| $(8, 15)$ | $1$ だけ | ✓ そう |
| $(4, 6)$ | $1, 2$ | ✗ 違う（$2$ で両方割れる） |

**確かめ方の三本柱**：

1. **小さい素数で順に試す** — $2, 3, 5, 7, \\dots$ で両方割れなければ互いに素
2. [素因数分解] して **共通の素因数があるか** を見る
3. **ユークリッド互除法** で最大公約数を出して、$1$ になれば互いに素

**ポイント**：「素」という字はここでは「素数」の素ではなく、**「単純で何も付いていない」** イメージ。互いに素なら、分数 $\\dfrac{a}{b}$ は **これ以上約分できない** 状態（既約分数）になっています。`,
    scenes: `- **約分** — $\\dfrac{6}{8}$ を $\\dfrac{3}{4}$ にできるのは、$6$ と $8$ が「互いに素ではない」から。約分しきった分数は分母分子が互いに素
- **歯車・カレンダー** — 歯数が互いに素な歯車を組み合わせると、同じ位置に戻るまで長く回る。月と週（$30$ と $7$ は互いに素）でカレンダーが繰り返さないのも同じ理屈
- **暗号（公開鍵暗号 RSA）** — 鍵を作れる／作れないを決めるのが、まさに「互いに素」の判定
- **音楽の和音** — 振動数の比が小さい整数比（互いに素な比）だと澄んだ和音、複雑な比だと不協和音
- **分数の足し算の通分** — 分母どうしが互いに素なら、通分は単に分母を掛け算するだけでよい`,
    relatedTerms: `- [素因数分解] — 整数を素数の積に書き直す操作。互いに素の確認に直結する
- **最大公約数（GCD）** — 互いに素は「GCD $= 1$」の言い換え
- **既約分数** — 分母と分子が互いに素な分数。これ以上約分できない
- **原始ピタゴラス数** — [ピタゴラス数] の中で $a, b, c$ が互いに素なもの。たとえば $(3, 4, 5)$ は原始、$(6, 8, 10)$ は原始でない（$2$ 倍だから）`,
  },

  素因数分解: {
    reading: "そいんすうぶんかい",
    short: "整数を素数の積に書き直す操作。整数の世界の原子論。",
    meaning: `**整数を「素数のレゴ」に分解する。**

すべての整数（$2$ 以上）は、**素数の掛け算** で書き直せます。しかも **書き方は本質的に $1$ 通り**（整数の **一意分解の定理**）。

**例**：

| 整数 | 素因数分解 |
|---|---|
| $12$ | $2 \\times 2 \\times 3 = 2^2 \\times 3$ |
| $60$ | $2^2 \\times 3 \\times 5$ |
| $100$ | $2^2 \\times 5^2$ |
| $1000$ | $2^3 \\times 5^3$ |

**手順**：小さい素数（$2, 3, 5, 7, \\dots$）で順に割っていく。

$$60 \\div 2 = 30 \\div 2 = 15 \\div 3 = 5 \\div 5 = 1$$

→ $60 = 2^2 \\times 3 \\times 5$。

「整数を **素数というレゴブロック** に分解する」——これが素因数分解の本質。

────────

**もっと深く** — 素因数分解で見える整数の性質

**約数の個数**：$n = p^a \\times q^b$ なら、約数の個数は $(a+1)(b+1)$。たとえば $12 = 2^2 \\times 3^1$ なら $(2+1)(1+1) = 6$ 個の約数（$1, 2, 3, 4, 6, 12$）。

**最大公約数（GCD）と最小公倍数（LCM）**：

- GCD：両方に共通する素因数を最小のべき指数で取る
- LCM：すべての素因数を最大のべき指数で取る

$12 = 2^2 \\times 3$、$18 = 2 \\times 3^2$ なら：

- GCD $= 2^1 \\times 3^1 = 6$
- LCM $= 2^2 \\times 3^2 = 36$

**[互いに素]** との関係：$2$ つの整数が互いに素 ⟺ 素因数分解で **共通の素因数を持たない**。

**素数の無限性**：素数は無限に存在することがユークリッドの時代から知られています（背理法で美しく証明されます）。

**現代社会での応用**：

- **公開鍵暗号（RSA）**：「大きな数の素因数分解は難しい」という事実が、インターネットのセキュリティの基盤
- **暗号通貨**：ハッシュ関数の安全性も同じく数論的困難性に基づく`,
    scenes: `- **約分**：分母分子を素因数分解して共通因数で割る
- **最大公約数・最小公倍数**：日常的な計算（カレンダー、歯車、音楽の周期）
- **既約分数の確認**：分母分子が互いに素 ⟺ 共通の素因数がない
- **公開鍵暗号（RSA）**：素因数分解の計算量的困難性に基づく
- **数学オリンピック**：整数論の問題で必須`,
    relatedTerms: `- [互いに素]：素因数分解の共通因数がない関係
- [因数分解]：似た発想を多項式に適用したもの
- **素数**：それ以上分解できない整数の「原子」
- **約数 / 倍数**：素因数分解から直接読める`,
    relatedSeriesId: "e5_lcm_01",
  },

  /* ===== 複素数と方程式 ===== */
  複素数: {
    reading: "ふくそすう",
    short: "実数 $a, b$ と虚数単位 $i$（$i^2 = -1$）を組み合わせた $a + bi$ の形の数。実数を含み、四則演算で閉じている。",
    meaning: `**中心の問い** ｜ $x^2 = -1$ を解けるようにするために「数」を $1$ 段拡張するなら、どう書けばいい？

────────

**実数 $a, b$ と新しい数 $i$（$i^2 = -1$）の組み合わせ $a + bi$。**

実数の世界では $x^2 = -1$ は解けない（実数を $2$ 乗すると必ず $0$ 以上になるから）。そこで「$2$ 乗すると $-1$ になる」新しい数 $i$ を導入する：

$$i^2 = -1$$

この $i$ を **虚数単位** と呼びます。$i$ と実数を組み合わせた

$$a + bi \\quad (a, b \\text{ は実数})$$

の形の数を **複素数** といいます。$a$ を [実部]、$b$ を [虚部]。

| 条件 | 呼び方 |
|---|---|
| $b = 0$ | 実数（複素数の特別な場合） |
| $b \\ne 0$ | 虚数 |
| $a = 0, b \\ne 0$ | 純虚数 |

実数も「$b = 0$ の複素数」として複素数の中に含まれる——だから複素数は実数の世界を **壊さずに拡張** している。

**四則演算がすべて成り立つ**：

- **足し算**：$(2 + 3i) + (1 - i) = 3 + 2i$（実部・虚部を別々に）
- **かけ算**：$(2 + 3i)(1 - i) = 2 - 2i + 3i - 3i^2 = 5 + i$（最後に $i^2 = -1$ で置き換える）
- **割り算**：$\\dfrac{1 + i}{1 - i}$ は **分母の共役 $1 + i$ を上下に掛けて分母を実数化**：$\\dfrac{(1+i)^2}{(1-i)(1+i)} = \\dfrac{2i}{2} = i$

どの結果も必ず複素数になる——これが「数の世界」としての性質。

────────

**もっと深く** — 複素平面、ピタゴラスの形、応用

**複素平面**：複素数 $z = a + bi$ を点 $(a, b)$ として平面上にプロットすると、$z$ の **「大きさ」** は原点からの距離：

$$|z| = \\sqrt{a^2 + b^2}$$

これは [ピタゴラス数] の定理そのもの。$|z|^2 = z \\cdot \\bar{z} = a^2 + b^2$（[共役複素数] $\\bar{z} = a - bi$ との積）。

**かけ算は回転**：$z \\mapsto i \\cdot z$ は複素平面上で **$90°$ 回転**、$z \\mapsto (1 + i) z$ は **$45°$ 回転 + $\\sqrt{2}$ 倍**。複素数のかけ算は「数の計算」と「平面の動き」の両方を兼ねている。

**応用**：
- **電気工学・信号処理**：交流回路の電圧・電流（フェーザ表示）、フーリエ変換
- **量子力学**：波動関数は複素数値（実部・虚部の両方が物理的意味を持つ）
- **流体力学・電磁気学**：複素関数で空気の流れや電場を扱う
- **コンピュータグラフィクス**：回転・拡大を $1$ つの掛け算で表す`,
    scenes: `**「実数では解けない方程式に解を作る」ための数の拡張**

- **$x^2 = -1$ や $x^2 + x + 1 = 0$** の解を扱う（数Ⅱ・B「複素数と方程式」全体の土台）
- **交流回路の解析**：電圧 $v(t) = V \\cos(\\omega t + \\phi)$ を複素数 $V e^{i\\phi}$ で扱うと、$\\omega$ に依存しない式になり計算が楽
- **フーリエ変換**：音波・画像を「周波数成分」に分解する道具。複素数の振幅と位相が同時に扱える
- **量子力学**：粒子の状態を表す波動関数は複素数値
- **CG・回転**：$2D$ の回転は複素数のかけ算で記述（$3D$ ではクォータニオン）

**出典**

- 池田洋介（2023）『数学Ⅱ・B 入門問題精講 改訂版』旺文社
  — 第 $2$ 章「複素数と方程式」§ $1$「新しい数を作る」の節構成（「ないのなら作ってみる」の発想 → $i$ の定義 → 四則演算 → 共役）を参考。`,
    relatedTerms: `- [虚数単位] — 複素数を作る土台。$i^2 = -1$
- [実部] — 複素数 $a + bi$ の $a$
- [虚部] — 複素数 $a + bi$ の $b$
- [共役複素数] — 虚部の符号を反転した $\\bar{z} = a - bi$
- [ピタゴラス数] — $|z| = \\sqrt{a^2 + b^2}$ の形に登場`,
    relatedSeriesId: "adv_complex_new_number_01",
  },
  虚数単位: {
    reading: "きょすうたんい",
    short: "$2$ 乗すると $-1$ になる新しい数 $i$。$i^2 = -1$。これを認めれば $x^2 = -1$ は $i, -i$ を解にもつ。",
    meaning: `**$2$ 乗すると $-1$ になる「ないのなら作る」数。**

中学までの実数の世界では、すべての実数は $2$ 乗すると $0$ 以上になります。だから $x^2 = -1$ には解がない。

そこで「$2$ 乗すると $-1$ になる」新しい数 $i$ を **約束として導入**：

$$i^2 = -1$$

この $i$ を **虚数単位** といいます。$i$ を認めると、

$$x^2 = -1 \\iff x^2 - i^2 = 0 \\iff (x + i)(x - i) = 0$$

から、$x = i, -i$ という **$2$ つの解** が出てきます。$i$ も $-i$ も、$2$ 乗するとちゃんと $-1$ になることが確かめられる（$(-i)^2 = (-1)^2 \\cdot i^2 = i^2 = -1$）。

**$\\sqrt{-a}$ の表し方**：$a$ が正の実数のとき、

$$\\sqrt{-a} = \\sqrt{a}\\,i$$

と書く約束。$\\sqrt{-3} = \\sqrt{3}\\,i$、$\\sqrt{-1} = i$、$\\sqrt{-12} = 2\\sqrt{3}\\,i$ など。

────────

**もっと深く** — ルートの落とし穴、$i$ の累乗の周期

**ルートの計算規則の落とし穴**：$\\sqrt{a}\\sqrt{b} = \\sqrt{ab}$ は **$a, b$ がともに非負のとき** だけ成り立ちます。負の数が混ざると壊れる：

$$\\sqrt{-3} \\times \\sqrt{-2} = \\sqrt{3}\\,i \\times \\sqrt{2}\\,i = \\sqrt{6}\\,i^2 = -\\sqrt{6}$$

「$\\sqrt{(-3)(-2)} = \\sqrt{6}$」と書くと **間違い**。負のルートに出会ったら、まず $\\sqrt{-a} = \\sqrt{a}\\,i$ で $i$ を取り出すのが安全。

**$i$ の累乗は $4$ 周期**：

$$i^1 = i, \\quad i^2 = -1, \\quad i^3 = -i, \\quad i^4 = 1, \\quad i^5 = i, \\dots$$

$4$ つで $1$ 周。$i$ の累乗は **$90°$ ずつ回る** イメージ（[複素数] の複素平面で $i$ をかけると $90°$ 回転）。`,
    scenes: `- **$x^2 = -1$ などの実数解を持たない $2$ 次方程式の解**
- **判別式 $D < 0$ の場合の解**：解の公式に $\\sqrt{D}$（$D$ 負）が現れたら $i$ を取り出す
- **電気工学では $j$ と書く**：電流 $i$ と区別するため $j^2 = -1$ と書く慣習
- **複素平面で「$90°$ 回転」を表す**：$z \\mapsto i \\cdot z$ は反時計回り $90°$ 回転`,
    relatedTerms: `- [複素数] — $i$ と実数の組み合わせ $a + bi$
- [共役複素数] — $i$ の符号を反転した相方`,
    relatedSeriesId: "adv_complex_new_number_01",
  },
  実部: {
    reading: "じつぶ",
    short: "複素数 $a + bi$ の $a$ の部分（実数の部分）。",
    meaning: `**複素数 $z = a + bi$ の「実数の部分」。**

複素数 $z = a + bi$（$a, b$ は実数）に対して、$a$ を **実部** といい $\\operatorname{Re}(z)$ と書きます。

**例**：
- $z = 3 + 4i$ → 実部 $= 3$
- $z = -5i$ → 実部 $= 0$（純虚数）
- $z = 7$ → 実部 $= 7$（実数）

[複素数] の足し算・引き算では、実部どうしを別々に足し引きします。

**等式の比較**：$2$ つの複素数 $a + bi = a' + b'i$ が等しい ⟺ 実部どうしも虚部どうしも等しい。これを使うと「複素数の等式」が「$2$ つの実数の連立方程式」に分解できる。`,
    scenes: `**「複素数のうち、実数で動く部分」を取り出したいとき**

- **複素数の等式を実数の連立に分解する**：複素数 $a + bi = a' + b'i$ ⟺ 実部 $a = a'$ かつ虚部 $b = b'$。$1$ 本の複素数等式から $2$ 本の実数方程式が出てくる（数Ⅱ・B の「複素数と方程式」で頻出）
- **複素平面の「横」を読む**：実部 $a$ は、複素数を $(a, b)$ として平面に置いたときの $x$ 座標。図形と方程式の世界と地続き
- **物理：交流の「平均仕事率」**：交流電力の実部は、抵抗で実際に消費されるエネルギーの量。虚部は「行ったり来たり」して仕事にならない部分
- **信号処理**：複素数値のフーリエ係数のうち、実部は「コサイン成分」（対称な波の強さ）を表す`,
    relatedTerms: `- [複素数] — 実部が住む先
- [虚部] — もう一方の半分`,
    relatedSeriesId: "adv_complex_new_number_01",
  },
  虚部: {
    reading: "きょぶ",
    short: "複素数 $a + bi$ の $b$ の部分（$i$ の係数）。",
    meaning: `**複素数 $z = a + bi$ の「$i$ の係数」。**

複素数 $z = a + bi$（$a, b$ は実数）に対して、$b$ を **虚部** といい $\\operatorname{Im}(z)$ と書きます。**注意：虚部は実数 $b$ のことであって、$bi$ ではない**。

**例**：
- $z = 3 + 4i$ → 虚部 $= 4$（$4i$ ではない）
- $z = -5i$ → 虚部 $= -5$
- $z = 7$ → 虚部 $= 0$（実数）

[複素数] の足し算・引き算では、虚部どうしを別々に足し引きします。

**判定**：
- $b = 0$ なら $z$ は **実数**
- $b \\ne 0$ なら $z$ は **虚数**
- $a = 0$ かつ $b \\ne 0$ なら $z$ は **純虚数**`,
    scenes: `**「複素数のうち、実数ではない動き」を取り出したいとき**

- **実数か虚数かの判定**：複素数の虚部が $0$ かどうかで「実数 / 虚数」が決まる
- **複素平面の「縦」を読む**：虚部 $b$ は、複素数を $(a, b)$ として平面に置いたときの $y$ 座標。図形と方程式と地続きで、複素数を平面上で扱う土台になる
- **物理：交流の「位相」**：交流電圧 $V e^{i \\phi}$ の虚部 $V \\sin \\phi$ は、波が時間方向にどれだけずれているかを表す——音や電気信号の「タイミング」を扱う実用面
- **数値計算で「虚部の有無」をデバッグする**：実数解だけ欲しい場面で計算結果に虚部が $0$ で残らないかを確認する`,
    relatedTerms: `- [複素数] — 虚部が住む先
- [実部] — もう一方の半分
- [虚数単位] — 虚部に掛かる $i$ そのもの`,
    relatedSeriesId: "adv_complex_new_number_01",
  },
  重解: {
    reading: "じゅうかい",
    short: "$2$ 次方程式が「同じ値の解を $2$ 重に持つ」状態。判別式 $D = 0$ のとき起きる。放物線がちょうど $x$ 軸に接する瞬間。",
    meaning: `**「$2$ つの解が同じ値に重なる」瞬間。**

ふつう、$2$ 次方程式 $ax^2 + bx + c = 0$ は $2$ つの解 $\\alpha, \\beta$ をもちます。ところが、係数を少しずつ動かすと、$\\alpha$ と $\\beta$ が **だんだん近づいて、ぴったり重なる** ことがある——その瞬間を **重解** といいます。

**例**：$x^2 - 6x + 9 = 0$

$$x^2 - 6x + 9 = (x - 3)^2 = 0 \\quad \\Longrightarrow \\quad x = 3$$

解は $1$ つに見えますが、本当は「$3$ と $3$ が重なった」状態。数えるときは **$2$ 重解として「$2$ つ」と数える** のが流儀です。

**[判別式] との関係**：解の公式 $x = \\dfrac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}$ で、$\\sqrt{}$ の中身 $D = b^2 - 4ac$ がちょうど $0$ になると、$\\pm \\sqrt{0} = 0$ で **$\\pm$ が消える**——これが重解の正体。

| 場合 | $D$ | 解 |
|---|---|---|
| 重解 | $D = 0$ | $x = -\\dfrac{b}{2a}$ の $1$ 値（$2$ 重） |
| 異なる $2$ 実数解 | $D > 0$ | $2$ つの異なる実数 |
| 異なる $2$ 虚数解 | $D < 0$ | 異なる $2$ つの [共役複素数] |

**放物線で見る重解**：$y = ax^2 + bx + c$ のグラフは、$D = 0$ のときちょうど $x$ 軸に **接します**（$1$ 点で触れるが切らない）。「触れた瞬間が重解」と覚えると景色がつかめる。

────────

**もっと深く** — 重解と完全平方、解と係数の関係

**完全平方式との関係**：$x^2 + bx + c$ が **完全平方**（$(x - p)^2$ の形）になるのは $b^2 - 4c = 0$、つまり $c = (b/2)^2$ のとき。これが「重解になる条件」と同じ式。

**解と係数の関係**：重解 $x = p$ を「$\\alpha = \\beta = p$」と見れば、解の和 $\\alpha + \\beta = 2p = -b/a$、解の積 $\\alpha \\beta = p^2 = c/a$ がふつうの解と係数の関係をそのまま満たす——重解は「$2$ つの解が偶然一致した特殊なケース」として、解と係数の枠組みに自然に収まる。

**接する円・接線との類似**：「重解 = 解が重なる」は、「$2$ 円が接する = $2$ 交点が重なる」「直線が円に接する = 交点が重なる」と同じ景色。$D = 0$ が出るたびに「重なる瞬間が来た」と思える。`,
    scenes: `**「ちょうど境界・ちょうど接する」状況を見つけたいとき**

- **$2$ 次関数のグラフが $x$ 軸に接する瞬間**：頂点がちょうど $x$ 軸の上にある状態
- **直線が円に接する条件**：直線と円の連立で $D = 0$ → 接する瞬間
- **不等式 $ax^2 + bx + c \\geq 0$ が常に成り立つ条件の境界**：$D = 0$ で「ぴったり $0$ になる」境界
- **パラメータ問題の境目を探す**：「異なる $2$ 実数解」から「虚数解」に切り替わる境界は重解の場所
- **物理：臨界減衰**：振動系で「振動するか、しないか」のちょうど境目（過減衰と不足減衰の境界）`,
    relatedTerms: `- [判別式] — $D = 0$ が重解の条件
- [複素数] — $D < 0$（重解の向こう側）で初めて顔を出す
- [共役複素数] — $D < 0$ のときの $2$ 虚数解のペア`,
    relatedSeriesId: "adv_complex_quadratic_solutions_01",
  },
  共役複素数: {
    reading: "きょうやくふくそすう",
    short: "複素数 $z = a + bi$ の虚部の符号を反転した数 $\\bar{z} = a - bi$。$z\\bar{z} = a^2 + b^2$ は実数（ピタゴラスの形）。",
    meaning: `**虚部の符号を反転した「相方」。**

複素数 $z = a + bi$ に対して、虚部の符号を反転させた

$$\\bar{z} = a - bi$$

を **$z$ の共役複素数** といいます（記号は $\\bar{z}$ または $z^*$）。

複素平面では、$z$ と $\\bar{z}$ は **実軸に関して対称**——上下に折り返した位置にあります。

**和と積はいつも実数**：

- **和**：$z + \\bar{z} = (a + bi) + (a - bi) = 2a$（[実部] の $2$ 倍）
- **積**：$z \\cdot \\bar{z} = (a + bi)(a - bi) = a^2 - (bi)^2 = a^2 + b^2$

積の式 $a^2 + b^2$ は **ピタゴラスそのもの**——$z$ を点 $(a, b)$ として複素平面に置けば、$|z|^2 = z\\bar{z} = a^2 + b^2$ は原点からの距離の $2$ 乗を表します。

**例**：
- $z = 3 + 4i$ → $\\bar{z} = 3 - 4i$、$z\\bar{z} = 9 + 16 = 25$、$|z| = 5$（$3$-$4$-$5$ 直角三角形！）
- $z = 1 + i$ → $\\bar{z} = 1 - i$、$z\\bar{z} = 1 + 1 = 2$

────────

**もっと深く** — 割り算の道具、解の対称性

**複素数の割り算は共役掛け**：分母 $c + di$ を実数化するには、**分母の共役 $c - di$ を分母・分子に掛ける**：

$$\\dfrac{a + bi}{c + di} = \\dfrac{(a + bi)(c - di)}{(c + di)(c - di)} = \\dfrac{(a + bi)(c - di)}{c^2 + d^2}$$

分母が $c^2 + d^2$（実数）になるので、計算が複素数の標準形に戻る。

**実数係数 $2$ 次方程式の解の対称性**：$ax^2 + bx + c = 0$ で $a, b, c$ が実数のとき、虚数解が出れば必ず **共役どうしのペア** で出る（解の対称性）。これは「実数係数 $\\Rightarrow$ 解は共役で閉じる」という代数の基本定理。`,
    scenes: `**「複素数の割り算」と「対称性のある解」を扱いたいとき**

- **複素数の割り算（分母の実数化）**：分母 $c + di$ の共役 $c - di$ を分母・分子に掛けて分母を $c^2 + d^2$ にする。複素数を標準形 $a + bi$ に戻す道具
- **複素数の絶対値・距離**：$|z|^2 = z \\bar{z} = a^2 + b^2$。複素平面で原点からの距離を測る基盤（ピタゴラスそのものの形）
- **実数係数方程式の解は共役ペアで現れる**：実数係数の $2$ 次・$3$ 次方程式が虚数解を持つとき、必ず共役どうしのペアになる（解の対称性）。これを使えば「片方の解を見つければ、もう片方は自動で決まる」
- **量子力学・信号処理での「絶対値の $2$ 乗」**：波動関数の確率密度 $|\\psi|^2 = \\psi \\bar{\\psi}$、信号のパワースペクトル $|F|^2 = F \\bar{F}$ で活躍。位相を消して「大きさ」だけを取り出す道具`,
    relatedTerms: `- [複素数] — 共役を作る土台
- [虚数単位] — 符号反転は $i \\to -i$ という意味
- [ピタゴラス数] — $z\\bar{z} = a^2 + b^2$ の形に登場
- [実部] / [虚部] — 共役で実部は不変、虚部は符号反転`,
    relatedSeriesId: "adv_complex_new_number_01",
  },
  剰余の定理: {
    reading: "じょうよのていり",
    short: "整式 $P(x)$ を $x - a$ で割った余りは $P(a)$",
    meaning: `**中心の問い** ｜ 割り算をせずに、整式の「余り」を取り出せるか？

────────

整式 $P(x)$ を $1$ 次式 $x - a$ で割ったときの **余り** $R$ は、$P(x)$ に $x = a$ を代入した値 $P(a)$ そのもの——**割り算を実行する必要はない**。

なぜなら、商を $Q(x)$、余りを $R$（定数）とすると

$$P(x) = (x - a) Q(x) + R$$

——両辺で $x = a$ を代入すると、$(a - a) Q(a) = 0$ なので $Q$ の部分が消え、$R = P(a)$ が残る。**「割り算」という重い操作が「$1$ 点での代入」という軽い操作に翻訳される**。

**考え方の核**：「$1$ 点での値（代入）が、整式の大域構造（割り算の余り）を決める」。

────────

**もっと深く** — 一般の $1$ 次式・余りが $0$ の特例・$n$ 点拡張

**一般の $1$ 次式 $a x + b$ で割る場合**：$a x + b = 0$ となるゼロ点 $x = -b/a$ を代入すれば、同じく余り $R = P(-b/a)$ が出る。$x - a$ も $a x + b$ も統一的に扱える。

**余りが $0$ になる特例 — 因数定理**：$P(a) = 0$ なら $R = 0$、つまり $P(x)$ は $x - a$ で割り切れる。「$P(a) = 0$ ⟺ $x - a$ が $P(x)$ の因数 ⟺ $x = a$ が $P(x) = 0$ の解」が同じ事柄を $3$ 通りに言い換えたもの——これが [因数定理]。

**$n$ 点への拡張 — Lagrange 補間**：$n$ 個の点での値 $P(x_1) = y_1, \\dots, P(x_n) = y_n$ から、次数 $\\leq n - 1$ の整式 $P(x)$ を **一意に再構成** できる（Lagrange 補間）。「$n$ 点の値 = $n - 1$ 次の整式」が等価で、剰余の定理を $n$ 点に拡張した姿。

**ホーナー法との組合せ**：実際に $P(a)$ を計算するとき、入れ子の形 $( \\cdots ((a_n x + a_{n-1}) x + a_{n-2}) x + \\cdots ) x + a_0$ で評価すれば $n$ 回の乗算で済む。剰余の定理と組み合わせて、高次方程式の解候補チェックが高速化できる。`,
    scenes: `**生活の中では？**

「式を実際に割り算せずに、結果の一部だけを知りたい」場面で剰余の定理は効きます。

- **$10$ 次方程式の整数解を探す**：$P(x) = 0$ の解候補を順番に代入し、$P(\\text{候補}) = 0$ となる値が見つかれば、そこが解で、$x - \\text{解}$ が因数。実際に $10$ 次の割り算を $5, 6$ 回するのは大変ですが、代入だけなら速い
- **暗号のシークレット**：Shamir の秘密分散では「秘密 $S$ を $n$ 人で分け持ち、$k$ 人集まれば復元できる」のに $k - 1$ 次の整式と剰余の定理を使う
- **誤り訂正符号（QR コード・CD・通信）**：データを多項式の係数に乗せ、複数点での値を送る——一部が壊れても残りから多項式を復元できるのは、剰余の定理（と Lagrange 補間）の応用

「点で値を測ると、見えない全体（多項式の構造）が分かる」——これが剰余の定理の射程です。`,
    relatedTerms: `**にていることば**

- [因数定理] — 剰余の定理で $R = 0$ になる場合。「$P(a) = 0$ ⟺ $x - a$ が因数 ⟺ $x = a$ が解」が等価
- [整式] — 剰余の定理が扱う対象。多項式・有理関数の世界で考える
- [解と係数の関係] — 因数定理を逆向きに使う場面（解から方程式を作る）で密接`,
    relatedSeriesId: "adv_remainder_theorem_01",
  },
  因数定理: {
    reading: "いんすうていり",
    short: "$P(a) = 0$ ⟺ $x - a$ は $P(x)$ の因数 ⟺ $x = a$ は $P(x) = 0$ の解",
    meaning: `**中心の問い** ｜ ある $1$ 点で式が $0$ になることから、何を読み取れるか？

────────

整式 $P(x)$ について、次の $3$ つは **同じ事柄を $3$ 通りに言い換えたもの**：

1. $P(a) = 0$（$x = a$ で値が $0$）
2. $x - a$ は $P(x)$ の **因数**（$P(x)$ は $x - a$ で割り切れる）
3. $x = a$ は方程式 $P(x) = 0$ の **解**

これを保証するのが **因数定理**——[剰余の定理] の特殊化で、「余り $= 0$」の場面に焦点を当てたもの。

**考え方の核**：「$1$ 点で値が $0$ ⟺ そこに対応する因子 $x - a$ が抜き出せる」——**$1$ 点の局所情報が、整式の大域構造（因数分解・解の集合）を直接決める**。

────────

**もっと深く** — 高次方程式の因数分解・有理解定理・代数閉体

**高次方程式の因数分解への応用**：$P(x) = 0$ の解を $1$ つ見つけたら（たとえば $x = a$）、因数定理で $P(x) = (x - a) Q(x)$ と分解できる。$Q(x)$ は次数が $1$ 減るから、再び解を探して因数定理を使えば、さらに分解できる——これを繰り返すと、整数係数・有理数係数の高次方程式が因数分解できる。

**有理解定理（有理根定理）**：整数係数の方程式 $a_n x^n + \\cdots + a_0 = 0$ の有理解 $p/q$（既約分数）の候補は $p \\mid a_0, q \\mid a_n$ に限られる。**候補が有限個に絞れる** ので、因数定理で順番にチェックすれば有理解が網羅できる。

**代数閉体への拡張**：複素数の範囲で考えると、$n$ 次の整式は必ず $n$ 個の因子 $(x - \\alpha_i)$ に分解できる（代数学の基本定理）。**因数定理が完備に効く世界が複素数**——これが [解と係数の関係] の基盤。`,
    scenes: `**生活の中では？**

「ある条件を満たす点を見つけて、そこから式の構造を読み解く」場面で因数定理は効きます。

- **$3$ 次方程式 $x^3 - 4 x^2 + x + 6 = 0$ を解く**：有理解候補 $\\pm 1, \\pm 2, \\pm 3, \\pm 6$ を順に代入。$x = -1$ で $P(-1) = -1 - 4 - 1 + 6 = 0$ と発見すれば、$P(x) = (x + 1) Q(x)$ と分解できる
- **既知の解から方程式を復元する**：「$x = 2, 3, 5$ を解とする $3$ 次方程式は何？」→ $(x - 2)(x - 3)(x - 5) = 0$ で組み立てられる
- **データに合う関数を作る**：「$x = 1, 2, 3$ で値が $0$ になる多項式」が必要なら、$(x - 1)(x - 2)(x - 3)$ を骨組みにすればよい

「点での値 $= 0$」を **手がかり** にして、整式の構造を抜き出す——これが因数定理の使い方です。`,
    relatedTerms: `**にていることば**

- [剰余の定理] — 因数定理の親。余りが $0$ でない場合も含めた一般形
- [整式] — 因数定理が扱う対象
- [解と係数の関係] — 因数定理で「解 → 因数 → 係数」をたどる場面で密接`,
    relatedSeriesId: "adv_remainder_theorem_01",
  },

  /* ===== 図形と方程式 ===== */
  一般形: {
    reading: "いっぱんけい",
    short: "直線・円・2 次曲線などの図形を統一された形で書いた式。直線なら ax + by + c = 0。",
    meaning: `**「すべての場合を $1$ つの形で書く」ための統一表現。**

直線の方程式の中で、**例外なくすべての直線を $1$ つの形で書ける** のが一般形：

$$ax + by + c = 0$$

[標準形] $y = mx + n$ では書けない **垂直な直線 $x = k$** も、一般形なら $1 \\cdot x + 0 \\cdot y - k = 0$ で書ける。

**円の一般形**：

$$x^2 + y^2 + lx + my + n = 0$$

[標準形] $(x-a)^2 + (y-b)^2 = r^2$ と表す円は同じだが、書き方が違うだけ。

────────

**もっと深く** — なぜ一般形が公式で使われるか

[点と直線の距離] の公式 $d = \\dfrac{|ax_0 + by_0 + c|}{\\sqrt{a^2 + b^2}}$ も、円の中心・半径の計算も、**一般形で式を統一して扱う** ことで成立しています。

「例外を作らない」「すべてを $1$ つの形で扱える」——これが一般形の哲学。一方、標準形は「人間が読みやすい・図形の特徴がすぐ見える」設計。**用途で使い分ける**。`,
    scenes: `- **[点と直線の距離] の公式**：一般形が前提
- **2 直線の交点**：両方の一般形を連立して解く
- **円の一般形 → 平方完成で標準形へ**：中心と半径を読む準備
- **2 次曲線（楕円・双曲線・放物線）の統一表現**：すべて $Ax^2 + By^2 + Cxy + Dx + Ey + F = 0$ で書ける`,
    relatedTerms: `- [標準形]：人間が読みやすい形、図形の特徴が直接見える
- [平方完成]：円・放物線の一般形 → 標準形の架け橋
- [傾き]：直線の標準形 $y = mx + n$ の $m$`,
    relatedSeriesId: "adv_point_line_distance_01",
  },
  標準形: {
    reading: "ひょうじゅんけい",
    short: "直線・円などを「図形の特徴がそのまま見える」形で書いた式。直線なら y = mx + n、円なら (x-a)² + (y-b)² = r²。",
    meaning: `**「図形の特徴」が式から直接読める書き方。**

**直線の標準形**：

$$y = mx + n$$

ここで $m$ が **[傾き]**、$n$ が **$y$ 切片**——式を見るだけで $2$ つの特徴が読める。

**円の標準形**：

$$(x - a)^2 + (y - b)^2 = r^2$$

中心 $(a, b)$ と半径 $r$ が、式の中にそのまま顔を出している。

**放物線の標準形**（[平方完成] された形）：

$$y = (x - p)^2 + q$$

[頂点] $(p, q)$ が読める。

**どれも共通：図形の「形」と「位置」を決める量が、式の中で見えるようになっている**——これが標準形の哲学。

────────

**もっと深く** — 一般形と標準形を使い分ける

| 用途 | 向く形 |
|---|---|
| 中心や頂点を読みたい | **標準形** |
| 点との距離を計算したい | **[一般形]**（公式が一般形で書かれている） |
| すべての場合を $1$ 本の式で表したい | **一般形**（垂直な直線も含めて） |
| 図形を頭に思い浮かべたい | **標準形** |

「**人間に読みやすい標準形**」と「**機械的に扱いやすい一般形**」——どちらも同じ図形を表しているが、目的で使い分ける。`,
    scenes: `- **直線のグラフを描く**：標準形なら傾きと切片で即座に描ける
- **円のグラフを描く**：標準形なら中心と半径で即座に描ける
- **2 次関数のグラフを描く**：標準形（平方完成された形）なら頂点で即座に描ける
- **位置や形を一目で読みたいとき**：標準形を選ぶ`,
    relatedTerms: `- [一般形]：すべての場合を統一して扱える別の書き方
- [傾き]：直線の標準形 $y = mx + n$ の $m$
- [平方完成]：標準形に直すための道具
- [頂点]：放物線の標準形から読める情報`,
    relatedSeriesId: "adv_line_equation_01",
  },
  傾き: {
    reading: "かたむき",
    short: "直線がどれだけ斜めかを表す数。「x が 1 増えると y がどれだけ増えるか」。",
    meaning: `**「$x$ が $1$ 進むあいだに、$y$ がどれだけ上がる（下がる）か」**

直線 $y = mx + n$ の $m$ が **傾き**。

「$x$ を $1$ 増やすと、$y$ が $m$ 増える」——この **比** が傾きの正体です。

<<LINE_SLOPE>>

**$2$ 点を通る直線の傾き**：

$$m = \\dfrac{y_2 - y_1}{x_2 - x_1} = \\dfrac{y \\text{ の差}}{x \\text{ の差}}$$

**例**：

- $(1, 2)$ と $(3, 8)$ を通る → $m = (8-2)/(3-1) = 3$
- $(0, 4)$ と $(2, 0)$ を通る → $m = (0-4)/(2-0) = -2$（右下がり）

**ざっくり覚え方**：

| $m$ | 意味 |
|---|---|
| $m = 0$ | 水平（変化なし） |
| $m = 1$ | $45°$ 右上がり |
| $m = 2$ | 急な右上がり |
| $m < 0$ | 右下がり |
| $m$ が定義できない | **垂直**（$x$ の変化が $0$） |

────────

**もっと深く** — 傾きはタンジェントそのもの

直線が $x$ 軸と作る角度 $\\theta$ に対して：

$$m = \\tan \\theta$$

これは [タンジェント] の定義「向かい ÷ となり」がそのまま「$y$ の差 ÷ $x$ の差」と一致するから。

**垂直関係**：2 つの直線が垂直なら、傾きの積 $m \\cdot m' = -1$。「片方の傾きをひっくり返して符号を変える」と覚える。`,
    scenes: `- **坂・道路の勾配**：標識「勾配 $10\\%$」は傾き $0.1$
- **屋根の角度**：屋根の傾き = 高さ ÷ 横の張り出し
- **グラフの読み取り**：時間 vs 距離のグラフの傾きは「速さ」
- **回帰直線**：データの $x$ と $y$ の関係を直線で表したときの傾き
- **[微分]**：曲線のある点での傾き = 接線の傾き = 微分係数`,
    relatedTerms: `- [タンジェント]：角度から傾きを出す、傾きから角度を出す
- [一般形] / [標準形]：直線の方程式の書き方
- [微分]：曲線の傾きを「点ごと」に求める拡張`,
    relatedSeriesId: "adv_line_equation_01",
  },
  垂直二等分線: {
    reading: "すいちょくにとうぶんせん",
    short: "線分の中点を通り、線分に垂直な直線。2 点から等距離の点の集まり。",
    meaning: `**$2$ 点から「同じ距離」にある点を、すべて集めた直線。**

$2$ 点 $A, B$ について、$A$ からも $B$ からも **同じ距離** にある点を集めると、$1$ 本の直線になります——それが **垂直二等分線**。

**作り方**：

1. $A$ と $B$ の **[中点] $M$** を求める
2. $M$ を通り、線分 $AB$ に **垂直** な直線を引く

これが垂直二等分線。線分 $AB$ を **真ん中で直角に切る** 直線です。

**なぜ「真ん中で直角」が等距離になるのか**：

線分 $AB$ の中点 $M$ から立てた垂線上のどの点 $P$ をとっても、$PA = PB$ になります（$P, M, A$ と $P, M, B$ がピタゴラスの三角形を作り、共有の縦の辺と等しい横の辺で、斜辺の長さも等しくなる）。

────────

**もっと深く** — 円との関係、3 点の外心

**$3$ 点から等距離の点（外心）**：三角形 $ABC$ の **$3$ 辺の垂直二等分線** はすべて $1$ 点で交わります。その交点が **外心**——$A, B, C$ から **等距離** にある点。だから外心を中心にすれば、$A, B, C$ を通る円（**外接円**）が描けます。

**直線の方程式での作り方**：

- 中点 $M = \\left(\\dfrac{x_A + x_B}{2}, \\dfrac{y_A + y_B}{2}\\right)$
- 線分 $AB$ の傾き $m_{AB} = (y_B - y_A)/(x_B - x_A)$
- 垂直二等分線の傾き $m = -1/m_{AB}$（[傾き] の積が $-1$）
- 中点 $M$ を通って傾き $m$ の直線が答え`,
    scenes: `- **作図問題**：「$2$ 点から等距離の場所はどこか」
- **三角形の外接円**：$3$ 辺の垂直二等分線の交点が外心
- **道路設計**：$2$ つの町から等距離の駅を作るとき
- **無線基地局**：$2$ 拠点から電波が等しく届く境界
- **数学オリンピック**：幾何の問題で頻出`,
    relatedTerms: `- **中点**：垂直二等分線が通る点
- [傾き]：垂直関係（傾きの積 $= -1$）から方向を決める
- **外心**：$3$ 辺の垂直二等分線の交点`,
    relatedSeriesId: "adv_line_equation_01",
  },
  線束: {
    reading: "せんそく",
    short: "$2$ 直線の交点を通る、すべての直線の集まり。$L_1 + k L_2 = 0$ という 1 つの式に住んでいる。交点を実際に求めずに、その点を通る別の直線を作れる「束の考え方」。",
    meaning: `**中心の問い** ｜ $2$ 直線の交点を、いちいち座標で求めずに、その交点を通る別の直線を書けないか？

────────

**「$2$ 直線が交わる点」を、座標を出さずに、別の直線で通す。**

$2$ つの直線 $L_1: ax+by+c=0$ と $L_2: a'x+b'y+c'=0$ が交わっていて、その交点を $P$ とする。$P$ を通る **別の** 直線を書きたい——ふつうなら、まず $L_1, L_2$ を連立して $P$ の座標を求めてから、新しい直線を組み立てる。でもこれは計算が $2$ 段重なる。

**束の発想**：次の式を作る。

$$L_1 + k L_2 = 0 \\quad \\Longleftrightarrow \\quad (ax+by+c) + k(a'x+b'y+c') = 0$$

ここで $k$ は任意の実数。**この式は、$k$ をどう選んでも、必ず $P$ を通る**。なぜなら、$P$ の座標を代入すると $L_1 = 0$ も $L_2 = 0$ も成り立つので、左辺は $0 + k \\cdot 0 = 0$ となり、つねに方程式を満たすから。

つまり **$k$ を $1$ つ動かすだけで、$P$ を通るあらゆる直線が次々と取り出せる**——これが束の考え方。「**束**（そく／たば）」という呼び名は、$P$ を通る直線をぜんぶ束ねた家族、というイメージから来ています。

**例**：直線 $x+y-3=0$ と $2x-y=0$ の交点と、点 $(0, 1)$ を通る直線を求めよ。

ふつうのやり方なら、まず連立して交点 $(1, 2)$ を出してから、それと $(0,1)$ を通る直線を組み立てる——$2$ 段階。

束のやり方なら、

$$(x+y-3) + k(2x-y) = 0$$

を作る。これは $k$ にかかわらず $2$ 直線の交点を通る。あとは「$(0, 1)$ を通る」条件で $k$ を決めるだけ。代入すると、

$$(0 + 1 - 3) + k(0 - 1) = -2 - k = 0 \\quad \\Longrightarrow \\quad k = -2$$

束の式に戻して整理：

$$(x+y-3) - 2(2x - y) = -3x + 3y - 3 = 0 \\quad \\Longrightarrow \\quad x - y + 1 = 0$$

**交点の座標を一度も求めずに**、その点を通る直線が書けました。

────────

**もっと深く** — 円束、根軸、束で表せないもの

**円束**：同じ考え方は **円** にも効きます。$2$ つの円 $C_1, C_2$ が $2$ 点で交わるとき、

$$C_1 + k C_2 = 0$$

は、$k$ にかかわらず $2$ つの交点を通る。$k$ を選べば、その $2$ 交点を通る **別の円** が描けます。

特に **$k = -1$** にすると、$x^2 + y^2$ の項どうしが打ち消し合って、円ではなく **直線** が現れる。これが $2$ 円の **根軸（こんじく）**——$2$ 円の共有点を結ぶ直線そのもの。$1$ 本の式 $C_1 + k C_2 = 0$ の中に、円の家族と $1$ 本の直線が同居しているわけです。

**束で表せないもの**：式 $L_1 + k L_2 = 0$ には、$L_2$ そのものは **入っていません**。$k = 0$ にすれば $L_1$ は取り出せますが、$L_2$ を取り出すには $k$ を無限大に飛ばす必要があり、そういう代入はできない。$L_2$ も含めたければ、対称な形 $\\alpha L_1 + \\beta L_2 = 0$（$\\alpha, \\beta$ は同時に $0$ でない実数）を使います。

**なぜ強力か**：束の本質は「**交点という具体的な数を経由せずに、図形どうしの関係だけで新しい図形を作れる**」こと。座標計算を $1$ 段すっ飛ばして結論まで進める、代数的なエレガンスを最初に味わえる道具です。

────────

**問いに戻ると**

「$2$ 直線の交点を、いちいち座標で求めずに、その交点を通る直線を書けないか？」——その答えが **線束**。

$L_1 + k L_2 = 0$ という $1$ 本の式の中に、交点 $P$ を通るすべての直線が住んでいる。$k$ を動かせば、別の直線が次々と取り出せる。この発想は **円束（根軸）** にも、もっと一般の図形にも広がっていく。「**交点を求めない**」「**式の組み合わせだけで答えに辿り着く**」——代数で図形を扱う力の、原点のひとつ。`,
    scenes: `**$2$ 直線（や $2$ 円）の交点を経由せずに、別の直線・円を作りたいとき。**

たとえば「$2$ 直線 $L_1, L_2$ の交点と、別の点 $P$ を通る直線を求めよ」——交点を計算してから直線を組み立てる代わりに、束 $L_1 + k L_2 = 0$ を作って $P$ を代入し $k$ を決めるだけ。交点の座標が要らないので、計算量が減ります。

**そのほかの使う場面**：

- **入試問題の常套句**：「$2$ 直線（$2$ 円）の交点と点 $P$ を通る…」のパターンに即座に効く
- **$2$ 円の共有点を結ぶ直線**：$C_1 + k C_2 = 0$ で $k = -1$ とすると現れる **根軸**——交点を求めずに、その交点を結ぶ直線が出る
- **$3$ 直線が $1$ 点で交わることの証明**：「$3$ つ目の直線が、$L_1, L_2$ の交点を通る」ことを、束 $L_1 + k L_2 = 0$ がある $k$ で $3$ つ目の式と一致することから示せる
- **解析幾何の道具箱**：「具体的に解かずに、式の組み合わせだけで結論まで進む」という、代数的な見通しのよさを最初に体験できる単元

**出典**

- 池田洋介『数学Ⅱ・B 入門問題精講 改訂版』旺文社
  — 第 $3$ 章「図形と方程式」の「束の考え方」節構成（直線束 → 円束への展開）を参考。例題の値はオリジナル。`,
    relatedTerms: `- [一般形] — 束の式 $L_1 + k L_2 = 0$ は、両辺とも一般形で書かれていることが前提
- **連立方程式** — 「交点を求める」普通のやり方の **裏返し**。束は交点を求めずに、交点を通る式の家族をまるごと扱う
- **根軸** — $2$ 円の束で $k = -1$ にしたときに現れる、$2$ 円の共有点を結ぶ直線（円束の特別な要素）`,
    relatedSeriesId: "adv_bundle_01",
  },
  軌跡: {
    reading: "きせき",
    short: "「ある条件を満たしながら動く点」が描く図形。距離条件を式に翻訳して両辺を $2$ 乗すれば、円・直線・放物線などが現れる。",
    meaning: `**中心の問い** ｜ 動く点が描く図形を、距離の条件から「式」で見抜くには？

────────

**「条件を満たす点の集まり」が描く図形。**

「**軌跡**」とは、ある条件を満たしながら点が動くとき、その点が描く図形のこと。

たとえば点 $P$ が「原点 $O$ からの距離が $1$」という条件を満たしながら動くと、$P$ の軌跡は **$O$ を中心とする半径 $1$ の円**。これは直観的にも見えますが、**式で求める** とこうなる：

$$OP = 1 \\iff \\sqrt{x^2 + y^2} = 1 \\iff x^2 + y^2 = 1$$

両辺を $2$ 乗するだけで、軌跡の方程式が得られる——「**$P$ の満たすべき条件を $x, y$ の式に書き、それを変形する**」のが軌跡を求める基本手順。

**条件によって軌跡の形が変わる**：

| 条件 | 軌跡 |
|---|---|
| 定点 $A$ から距離一定 | $A$ を中心とする **円** |
| $2$ 定点 $A, B$ から **距離の比一定**（$\\ne 1 : 1$） | **アポロニウスの円** |
| $2$ 定点 $A, B$ から **等距離**（$1 : 1$） | $AB$ の **[垂直二等分線]**（直線） |
| 定点 $F$ と定直線 $\\ell$ から **等距離** | **放物線**（焦点 $F$、准線 $\\ell$） |

「距離の比 $\\ne 1 : 1$ なら円、$1 : 1$ なら直線（円が無限に大きくなった極限）」「定点 vs 定点なら円、定点 vs 定直線なら放物線」——同じ「距離の方程式」のレシピから、場面に応じて違う図形が現れる。

────────

**もっと深く** — 「両辺を $2$ 乗する」変形の同値性、デカルトの座標系がもたらしたもの

**「両辺を $2$ 乗する」は同値変形か？**：一般には $A = B$ と $A^2 = B^2$ は **同値ではない**（例：$1 = (-1)^2 / 1$ から $1^2 = (-1)^2$ は導けるが、$1 = -1$ は導けない）。しかし軌跡の問題で扱う $A, B$ は **距離**、つねに非負：$A \\geq 0, B \\geq 0$。このとき $A^2 = B^2 \\iff A = B$ が成り立つ——**両辺を $2$ 乗する変形は必ず同値**。距離が $0$ 以上という保証つきだから、安心してルートを外せる。

**アポロニウスの円の歴史**：「$2$ 点からの距離の比が一定の点の軌跡 → 円」は、古代ギリシャの数学者 **アポロニウス**（紀元前 $200$ 年頃）が幾何だけで証明した性質。彼は「式」を一切使わず、図形の性質だけで議論した。私たちが今「両辺を $2$ 乗する」手法で扱えるのは、**$17$ 世紀のデカルト** が座標系を発明して初めて可能になった——約 $1800$ 年の時を経て、図形が「式」で扱えるようになった。

**放物線の幾何的定義**：「焦点と准線から等距離」という定義は、$y = a x^2$ のような関数のグラフとして与える代数的定義より、**幾何的に本質**。光学（パラボラアンテナ）や力学（物体の投射軌道）と直結する性質を、距離条件から自然に説明できる。

**$3$ 次元への拡張**：
- 「定点から距離一定」→ **球**
- 「定直線から距離一定」→ **円柱**
- 「$2$ 定点から距離の和が一定」→ **楕円**（$2$ 次元）、**回転楕円体**（$3$ 次元）
- 「$2$ 定点から距離の差が一定」→ **双曲線**

距離条件から、$2$ 次曲線・$2$ 次曲面が全部出てくる——古典幾何の見事な統一が、座標系を経由して現代の代数幾何にもつながっていく。`,
    scenes: `**「条件を満たす場所」を式で求めたいとき**

- **作図問題**：「$2$ 点から等距離の場所はどこ？」「直線と点から等距離の場所はどこ？」
- **物理：等電位面・等温線**：電場で「ポテンシャルが一定」の場所、熱伝導で「温度が一定」の場所——これらはすべて軌跡
- **天文・力学**：惑星の軌道（焦点に太陽があり、距離の和が一定 → 楕円）。**ケプラーの法則** が軌跡の発想で説明される
- **光学**：放物面鏡・楕円鏡は焦点と准線（または $2$ 焦点）の幾何的性質を使う
- **無線基地局・GPS**：距離の差が一定の場所 → 双曲線。位置を $2$ 局からの時間差で測る発想は軌跡の応用
- **作問・コンテスト**：「点 $P$ がこう動くとき、$P$ の軌跡を求めよ」は数学オリンピックや入試で頻出`,
    relatedTerms: `- [垂直二等分線] — $2$ 点から等距離の点の軌跡
- [円の方程式] — 距離一定の点の軌跡の式
- [点と直線の距離] — 「直線からの距離」条件を式にする道具
- [平方完成] — 軌跡の方程式を中心と半径が読める形に整理する道具
- **アポロニウスの円** — 距離の比が一定の点の軌跡
- **放物線・楕円・双曲線** — 距離条件のバリエーションから現れる $2$ 次曲線`,
    relatedSeriesId: "adv_locus_01",
  },
  媒介変数表示: {
    reading: "ばいかいへんすうひょうじ",
    short: "動く点を $x = f(t), y = g(t)$ ともう $1$ つの変数 $t$ を経由して表す書き方。$y = f(x)$ で書けない曲線（円・楕円・サイクロイドなど）も扱える。",
    meaning: `**中心の問い** ｜ $y = f(x)$ では書けない動きを、もう $1$ つの変数 $t$ を経由して表すには？

────────

**動く点の $x$ と $y$ をそれぞれ、もう $1$ つの変数 $t$ の関数として書く。**

動く点の経路（[軌跡]）を「外側から見た形」$y = f(x)$ で書こうとすると、円や楕円・垂直な直線などは関数として書けません（$1$ つの $x$ に $2$ つの $y$ が対応する、または $x$ が固定される）。

そこで「**$x$ と $y$ をそれぞれ、もう $1$ つの変数 $t$（時刻のイメージ）の関数として書く**」発想に切り替える：

$$x = f(t), \\quad y = g(t)$$

この $t$ を **媒介変数（パラメータ）**、この書き方を **媒介変数表示** という。

**例**：

- **直線**：$x = t + 1, \\ y = 2 t - 3$ → $t$ を消去すると $y = 2 x - 5$
- **円**：$x = r \\cos\\theta, \\ y = r \\sin\\theta$ → $\\cos^2\\theta + \\sin^2\\theta = 1$ から $x^2 + y^2 = r^2$
- **楕円**：$x = a \\cos\\theta, \\ y = b \\sin\\theta$ → $x^2/a^2 + y^2/b^2 = 1$
- **放物線の頂点の軌跡**：$y = x^2 - 2 (t + 3) x + 6 t$ の頂点 $(t + 3, -t^2 - 9)$ → 軌跡は $y = -(x - 3)^2 - 9$

**$t$ を消去すれば軌跡の方程式に戻る**：媒介変数表示と軌跡の方程式 $F(x, y) = 0$ は、**同じ図形の表と裏**。

**変域の引き継ぎに注意**：$t$ に変域制限があると、消去後の $x$（または $y$）にも変域が引き継がれる。$t \\geq 0$ なら、対応する変数の動く範囲も限定される——軌跡の **一部のみ** が描かれる。

────────

**もっと深く** — 媒介変数微分、$y = f(x)$ で書けない曲線、物理との接続

**媒介変数微分（数Ⅲ）**：$x = f(t), \\ y = g(t)$ で表される曲線の接線の傾きは、

$$\\dfrac{dy}{dx} = \\dfrac{dy / dt}{dx / dt} = \\dfrac{g'(t)}{f'(t)}$$

——**$t$ を消去せずに、媒介変数のままで微分する** 道具。円・楕円・サイクロイドの接線を求めるときに必須。

**$y = f(x)$ では絶対に書けない曲線**：
- **サイクロイド**（回転する車輪上の点が描く曲線）：$x = r (t - \\sin t), \\ y = r (1 - \\cos t)$。媒介変数表示なら $2$ 行で書けるが、$x, y$ の関係式では簡単に書けない
- **リサジュー曲線**（電気信号のオシロスコープ表示）：$x = \\sin(\\alpha t), \\ y = \\sin(\\beta t)$。比 $\\alpha : \\beta$ に応じて複雑なパターン
- **アステロイド**（星形曲線）：$x = a \\cos^3 t, \\ y = a \\sin^3 t$。$x^{2/3} + y^{2/3} = a^{2/3}$（陰関数）

**物理：質点運動の自然な記述**：放物運動 $x = v_0 t \\cos\\theta_0, \\ y = v_0 t \\sin\\theta_0 - \\frac{1}{2} g t^2$ のように、時刻 $t$ で位置 $(x, y)$ を記述するのは物理の基本——媒介変数表示は **物理の言語そのもの**。

**ベクトルへの橋渡し**：媒介変数表示 $(x, y) = (f(t), g(t))$ は、**位置ベクトル** $\\vec{p}(t) = f(t) \\vec{i} + g(t) \\vec{j}$ と同じ書き方。$3$ 次元 $(x, y, z) = (f(t), g(t), h(t))$ への自然な拡張、関数解析・物理学・コンピュータグラフィクスの基盤に直結する。`,
    scenes: `**「$y = f(x)$ では書きにくい動きや曲線」を扱いたいとき**

- **円・楕円・サイクロイドの記述**：$y = f(x)$ では関数にならない曲線を $1$ つの式で書ける
- **物理：質点運動**：放物運動・等速円運動・単振動など、時刻 $t$ で位置を記述するすべての運動
- **コンピュータグラフィクス**：アニメーションは「時間 $t$ で物体の位置・回転・スケール」を媒介変数で動かす
- **ベジエ曲線（CAD・フォントデザイン）**：滑らかな曲線を媒介変数 $t \\in [0, 1]$ で表現
- **天文学**：惑星の軌道は媒介変数（離心近点角）で表すと計算が楽
- **数学オリンピック・入試**：「動点の軌跡」「動点と中点の合成」など、軌跡 + 媒介変数の組み合わせ問題`,
    relatedTerms: `- [軌跡] — $t$ を消去すれば軌跡の方程式に戻る。媒介変数表示は軌跡の「内側からの言葉」
- [円の方程式] — 円は $x = r\\cos\\theta, y = r\\sin\\theta$ で自然に表せる
- [三角関数] — 円・楕円・リサジュー曲線の媒介変数表示で必須
- [平方完成] — 動く放物線の頂点座標を取り出すのに使う
- **ベクトル** — 媒介変数表示 $(f(t), g(t))$ は位置ベクトル $\\vec{p}(t)$ と同じ書き方`,
    relatedSeriesId: "adv_parametric_01",
  },
  領域: {
    reading: "りょういき",
    short: "不等式 $y > f(x)$、$x > k$、$(x-a)^2 + (y-b)^2 < r^2$ などが表す平面の広がり。等式が描く境界線を、不等号は『どちら側』へと育てる。",
    meaning: `**中心の問い** ｜ 等式が描く境界線を、不等号は『どちら側の広がり』へとどう育てるのか？

────────

**等式は境界線、不等号は『どちら側の広がり』。**

[直線] $y = x + 1$ や円 $x^2 + y^2 = 9$ のような **等式** は、平面上の **線や曲線** を描きます。これを **不等式** に書き換えると、線ではなく **平面の広がり（領域）** が現れる：

- $y > x + 1$ → 直線 $y = x + 1$ の **上側**
- $y < x + 1$ → 直線 $y = x + 1$ の **下側**
- $x > 3$ → 縦の直線 $x = 3$ の **右側**
- $x^2 + y^2 > 9$ → 円 $x^2 + y^2 = 9$ の **外側**
- $x^2 + y^2 < 9$ → 円 $x^2 + y^2 = 9$ の **内側**

**$3$ つの典型ケース**：

| 境界 | 不等式 | 領域 |
|---|---|---|
| 直線 $y = f(x)$（傾きあり） | $y > f(x)$ / $y < f(x)$ | **上側** / **下側** |
| 縦の直線 $x = k$ | $x > k$ / $x < k$ | **右側** / **左側** |
| 円 $(x-a)^2 + (y-b)^2 = r^2$ | $\\ldots > r^2$ / $\\ldots < r^2$ | **外側** / **内側** |

**なぜ円は『内外』なのか？——距離条件として読む**

円の方程式 $(x - a)^2 + (y - b)^2 = r^2$ は、もともと「中心 $(a, b)$ から点 $P(x, y)$ までの距離が $r$ に等しい」という条件 $OP = r$ を $2$ 乗した形。だから：

- $(x - a)^2 + (y - b)^2 > r^2 \\iff OP > r \\iff$ 円の **外側**
- $(x - a)^2 + (y - b)^2 < r^2 \\iff OP < r \\iff$ 円の **内側**

**不等式を『距離の言葉』に翻訳する** と、内外が自然に見える——これが円の領域の核です。

**境界線の含む／含まない**：不等号に **等号が含まれる**（$\\geq, \\leq$）場合、境界線は領域に **含まれる**（実線で描く）。**含まれない**（$>, <$）場合、境界線は領域に **含まれない**（破線で描く）。

**連立で共通部分を切り出す**：複数の不等式を **かつ** で結ぶと、それぞれの領域の **共通部分** が新しい領域になる——線形計画法・最適化問題の基盤。

────────

**もっと深く** — 等高線、線形計画法、$2$ 変数関数の世界へ

**等高線——平面上の高低差を読む**：

地図の **等高線** は「標高が同じ場所を結んだ線」。地図上に等高線を書き加えると、高い場所と低い場所が一目で分かる——これは数学の領域とまったく同じ仕組み。$z = f(x, y)$ という $2$ 変数関数で、$z = k$ という等式は等高線、$z > k$ という不等式は『その等高線より高い領域』を表します。

**線形計画法**：

領域そのものを描くのは入口で、本領は **領域上で何かの値の最大最小** を考えること。例えば：

- 工場の生産制約 $2 x + y \\leq 10, \\ x + 3 y \\leq 12, \\ x \\geq 0, \\ y \\geq 0$ のもとで、利益 $300 x + 500 y$ が最大の組み合わせは？
- 食品の栄養制約のもとで、コストが最小の配合は？

制約は不等式、目的関数は $z = a x + b y$。**領域内で「等しい $z$ を結ぶ直線」を平行移動して、領域に接する最後の点を探す** ——これが線形計画法。経済学・経営学・工学最適化の出発点。

**$2$ 変数関数の世界へ**：

領域は $1$ 変数関数 $y = f(x)$ の世界から、$2$ 変数関数 $z = f(x, y)$ の世界への扉。「$x$ と $y$ の組」が独立変数になると、関数の値は **高さ** や **温度** など、平面の上に乗った「もう $1$ 次元の量」になる。等高線・偏微分・最大最小（数Ⅲ以降、大学の解析学）はこの世界の道具。

**確率・統計との接続**：

$2$ 変数の確率密度関数 $f(x, y)$ の領域 $D$ 上の確率は、$D$ を底とする立体の体積で表される——重積分の世界へ自然に拡張する。

**整数の解の個数（数A 場合の数との橋）**：

$x + y \\leq 5, \\ x \\geq 0, \\ y \\geq 0$ の領域に含まれる **格子点**（$x, y$ が整数の点）の個数を数えると、組合せの問題に変わる——領域は連続と離散の橋渡し。`,
    scenes: `**「条件を満たす点の広がり」を考えたいとき**

- **工場の生産計画（線形計画法）**：「材料 A は $10$ kg 以下」「労働時間は $40$ 時間以下」など複数の制約条件を不等式で書くと、生産可能な領域が現れる。利益最大の点は領域の頂点
- **地図の等高線**：「標高 $50$ m 以上の地域」「降水量 $100$ mm 以上の地域」——条件を満たす地域の境界線
- **天気予報の警報領域**：「最高気温が $35$℃ 以上の地域」を地図上に塗り分ける——気象データから領域を読む
- **物理：許される運動の範囲**：エネルギー保存 $E = \\frac{1}{2} m v^2 + V(x) > V(x_0)$ から、粒子が到達できる位置 $x$ の領域
- **経済学の予算制約**：所得 $I$ で価格 $p, q$ の品物 $X, Y$ を買うとき、予算内の組合せは $p x + q y \\leq I$ の領域
- **入試・大学入学共通テスト**：「不等式の表す領域を図示せよ」「領域内の点の最大値・最小値」が定番出題範囲`,
    relatedTerms: `- [軌跡] — 等式（距離条件）で境界線を作る発想は領域と表裏一体
- [媒介変数表示] — 動く点の経路を式で書く発想の延長線
- [標準形] — 円の領域 $(x-a)^2 + (y-b)^2 \\gtrless r^2$ は標準形と直結
- [傾き] — 直線の境界 $y = m x + n$ の傾きが領域の向きを決める
- [線形計画法] — 領域内で線形関数の最大最小を求める応用
- [等高線] — $2$ 変数関数 $z = f(x, y)$ の領域論の入口`,
    relatedSeriesId: "adv_region_01",
  },
  線形計画法: {
    reading: "せんけいけいかくほう",
    short: "領域上で線形な関数の最大・最小を、領域の頂点を巡って見抜く最適化の手法。経済・経営・工学のあらゆる最適化問題の入口。",
    meaning: `**中心の問い** ｜ 制約条件の中で、何かの利益や費用を最も有利にするには？

────────

**線形な制約と線形な目的関数の最適化を、領域の頂点を巡って解く手法。**

「製品 A と B を作るとき、原料と労働時間の制約を満たしながら利益を最大化したい」「複数の食品を組み合わせて、栄養を満たしながらコストを最小化したい」——こうした問題は線形不等式の連立で書け、目的関数（利益・コスト）も線形なら、**領域の頂点での値を比べるだけで答えが出る**。

**なぜ頂点で解けるのか**：

- 制約 $a_1 x + b_1 y \\leq c_1, \\ a_2 x + b_2 y \\leq c_2, \\ldots$ は領域を直線で切り取った **多角形**
- 目的関数 $z = a x + b y$ の等高線は **傾き $-a / b$ の直線群**
- 等高線（直線）を平行移動して領域を貫く $\\to$ 領域から離れる瞬間（最大）or 領域に最初に触れる瞬間（最小）
- 直線と多角形が接する点は必ず **多角形の頂点**（または辺の一部だが、辺全体で同値の最大値）

したがって：**頂点で目的関数の値を計算し、最大 / 最小を選ぶだけ**——これが線形計画法の基本アルゴリズム。

**例**：制約 $x + 2 y \\leq 14, \\ 5 x + y \\leq 25, \\ x, y \\geq 0$、利益 $3 x + 2 y$ の最大値。

頂点 $(0, 0), (5, 0), (0, 7)$ と $2$ 制約の交点 $(4, 5)$。利益は $0, 15, 14, 22$。**最大は $(4, 5)$ で $22$**——「$2$ つの制約が同時に効く交点」で利益が最大化される。

────────

**もっと深く** — シンプレックス法、現代の応用、計算複雑性

**シンプレックス法（Simplex method）**：

線形計画法の頂点を効率的に巡るアルゴリズム。Dantzig（$1947$）が考案。**変数が数百〜数千個** の大規模問題でも、領域のすべての頂点を試さず、利益が増える方向の頂点だけを巡って答えを見つける——コンピュータ最適化の古典。

**現代の応用**：

- **生産・物流計画**：自動車・電子部品の生産配分、トラック配送ルート最適化
- **金融ポートフォリオ**：制約付きの資産配分（リスク制約のもとでリターンを最大化）
- **広告配信**：複数広告枠への予算配分でクリック数を最大化
- **電力網**：電源の組合せ最適化、需要と供給のバランス
- **エネルギー**：石油精製プラントの製品配分（線形計画法の元祖の応用領域）
- **食事プラン**：栄養制約のもとでコスト最小化（ストィグラー問題、$1945$ 年）

**整数線形計画法（ILP）**：

変数が整数値しか取れない場合は計算が一気に難しくなる（NP 困難）。例：「製品を $4.7$ 個作る」は不可能で、整数解だけを求めたい。組合せ最適化・整数計画法は現代研究の最前線。

**カントロビッチとダンツィヒ**：

ソ連の数学者カントロビッチ（$1939$）が経済計画問題で先に発見、ダンツィヒ（$1947$）が独立に再発見。カントロビッチはこれで $1975$ 年ノーベル経済学賞を受賞。「線形計画法は $20$ 世紀の数学が経済学に与えた最大の贈り物」（ポール・サミュエルソン）。`,
    scenes: `**「制約の中で何かを最も有利にしたい」とき**

- **工場の生産計画**：複数の製品 / 原料 / 労働時間の制約下で利益最大化
- **食事プラン**：栄養素の必要量を満たしつつ食費を最小化（ダイエットメニューも）
- **物流配送**：トラック / 倉庫 / 配送先の制約下でコスト最小化
- **金融ポートフォリオ**：リスク制約下で期待リターン最大化
- **広告予算配分**：複数チャネルへの配分でリーチ最大化
- **電力需給**：複数電源の組合せで燃料コスト最小化
- **大学入試・公務員試験**：「不等式と領域」の応用問題として頻出
- **線形計画法 × AI**：機械学習の正則化・最適化の基盤にも線形計画法的発想`,
    relatedTerms: `- [領域] — 不等式の制約が描く平面の広がり。線形計画法の舞台
- [等高線] — 目的関数 $z = a x + b y = k$ の等高線（直線群）を動かす
- [通過領域] — 動くパラメータ付き図形の通過範囲、判別式で求める応用
- [傾き] — 等高線の傾き $-a / b$ で頂点の取り方が決まる
- **シンプレックス法** — 大規模問題で頂点を効率的に巡るアルゴリズム
- **整数計画法** — 変数が整数値の場合（NP 困難な組合せ最適化）`,
    relatedSeriesId: "adv_linear_programming_01",
  },
  等高線: {
    reading: "とうこうせん",
    short: "$2$ 変数関数 $z = f(x, y)$ の値が同じになる点を結んだ線。地図の標高線と同じ仕組み。最大最小を見抜く道具。",
    meaning: `**中心の問い** ｜ 平面の各点に乗った『高さ』を、$2$ 次元の図でどう読み解くには？

────────

**地形を平面で読む——標高が同じ場所を結ぶ線。**

地図の **等高線** は「標高が同じ場所を結んだ線」。地図上に等高線が密に書かれていれば急な坂、間隔が広ければなだらかな地形。山の頂上は等高線が同心円状に集まり、谷は同心円が反転する。

数学では、$2$ 変数関数 $z = f(x, y)$ について、**$z$ が一定の値 $k$ になる点 $(x, y)$ の集合** が等高線。

$$f(x, y) = k$$

これは $x, y$ の関係式——$1$ 本の曲線（または直線）になる。$k$ を変えると等高線が動く——地形を「輪切り」した断面の境界が現れる。

**典型例**：

| 関数 $z = f(x, y)$ | 等高線 $f = k$ | 地形 |
|---|---|---|
| $z = x + y$ | 傾き $-1$ の直線群 | 平らな斜面 |
| $z = a x + b y$ | 傾き $-a / b$ の直線群 | 平らな斜面 |
| $z = x^2 + y^2$ | 原点中心の円 | すり鉢状の谷 |
| $z = -(x^2 + y^2)$ | 原点中心の円 | 小高い丘 |
| $z = x y$ | 双曲線 | 鞍点を持つ地形 |
| $z = x^2 - y^2$ | 双曲線 | 鞍点を持つ地形 |

**線形計画法での使い方**：

[領域] $D$ 上で目的関数 $z = a x + b y$ の最大・最小を求めたいとき：

1. 等高線 $a x + b y = k$ を描く（傾き $-a / b$ の直線）
2. $k$ を大きくする方向（地形の「上り」）に等高線を平行移動
3. 領域 $D$ から **離れる瞬間** が最大、領域 $D$ に **最初に触れる瞬間** が最小
4. 接触点は領域の **頂点**（[線形計画法] のキー）

**$z = x^2 + y^2$ なら円の等高線**：

等高線が直線でない場合も同じ発想。$z = x^2 + y^2 = k$ は原点中心、半径 $\\sqrt{k}$ の円。$k$ を大きくすると円が膨らみ、$k$ を小さくすると縮む。領域に **最も遠い点（最大）** または **最も近い点（最小）** で接触する。

────────

**もっと深く** — 地理学からの来歴、勾配と偏微分、天気図への応用

**地理学からの来歴**：

等高線（contour line）は $18$ 世紀後半の地図学で発明された——フランスの Charles Hutton（$1774$ 年）が **シェハリオン山**（スコットランド）の体積測定のために導入。地球の密度を測る実験で、山の正確な体積を知る必要があったから。

**勾配と偏微分（数Ⅲ・大学解析学）**：

等高線に **直交する方向** は、$z$ が最も急に増える方向。これを **勾配（gradient）** といい、$\\nabla f = (\\partial f / \\partial x, \\partial f / \\partial y)$ と書く。偏微分は「等高線に垂直方向の傾き」を測る道具——大学の解析学・物理学・機械学習の最適化（勾配降下法）の基盤。

**天気図の等値線**：

天気図の **等圧線** は気圧が同じ場所を結ぶ線——等高線の親戚。等圧線が密だと風が強く、間隔が広いと穏やか。**等温線** は温度、**等降水線** は降水量。気象学の基本道具。

**コンピュータグラフィクス・地形可視化**：

国土地理院の地形図、Google Earth の標高表示、ゲームの $3$D マップ作成——すべて等高線で表現された $2$ 変数関数を $3$ 次元的に可視化する技術。`,
    scenes: `**「平面の各点に乗った『何か』を読みたい」とき**

- **地図の標高**：山岳地帯の登山ルート、洪水ハザードマップ、土地造成計画
- **天気図**：等圧線・等温線・降水量の分布
- **化学**：化学反応の自由エネルギー地形、反応座標
- **物理**：電場・重力場のポテンシャル分布、温度場
- **経済学**：消費者の効用関数の等高線（無差別曲線）、生産関数の等量線
- **機械学習**：損失関数の地形を可視化、勾配降下法のアニメーション
- **画像処理**：エッジ検出、画像の明度等高線
- **線形計画法**：目的関数の等高線を動かして最適解を見抜く`,
    relatedTerms: `- [線形計画法] — 線形な等高線を動かして領域の頂点で最大最小を見抜く
- [領域] — 等高線が動く舞台（不等式が描く平面の広がり）
- [傾き] — 線形等高線 $a x + b y = k$ の傾き $-a / b$
- [標準形] — 円の等高線 $x^2 + y^2 = k$ の幾何
- **勾配** — 等高線に直交する方向の傾き、偏微分の入口
- **偏微分** — 等高線と勾配を計算する微分技法`,
    relatedSeriesId: "adv_linear_programming_01",
  },
  通過領域: {
    reading: "つうかりょういき",
    short: "パラメータ $t$ を含む直線や曲線が、$t$ をすべての実数で動かしたときに描き出す平面の領域。判別式の解の存在条件で求めるのが基本。",
    meaning: `**中心の問い** ｜ パラメータを含む動く図形が描き出す平面の『影絵』を、どうやって式で捉えるには？

────────

**動くワイパーが拭った跡——通過領域の発想。**

$y = 2 t x - t^2$ のような **パラメータ $t$ を含む直線** がある。$t$ を $-\\infty$ から $\\infty$ まで動かすと、直線は平面上を **ワイパー** のように動く——拭った跡が **通過領域** です。

**核となる置き換え**：

点 $(X, Y)$ がこの直線の通過領域に含まれる
$\\iff$ ある実数 $t$ が存在して $Y = 2 t X - t^2$
$\\iff$ $t$ の $2$ 次方程式 $t^2 - 2 X t + Y = 0$ が **実数解を持つ**
$\\iff$ 判別式 $D / 4 = X^2 - Y \\geq 0$
$\\iff$ $Y \\leq X^2$

通過領域は **$y \\leq x^2$**——放物線 $y = x^2$ の下側。

**この発想の核心**：

「動く直線が点 $(X, Y)$ を通る」という幾何的問いを、「**$t$ の方程式が実数解を持つ**」という代数的問いに **置き換える**——これが上級の数学テクニックの入口。$X, Y$ を **定数** とみなし、$t$ を **変数** とみなす視点の切り替えがコツです。

**池田氏の言葉**：「このような置き換えを自在にできることが、**数学の上級者と未経験者を分ける入口**」（『数学Ⅱ・B 入門問題精講』§ $7$）。

────────

**もっと深く** — 包絡線、エンベロープ、シミュレーション

**包絡線（envelope）**：

通過領域の **境界** は、パラメータ付き直線群の **包絡線**——すべての直線に接する曲線。上の例では包絡線が $y = x^2$。

数Ⅲ・大学数学では、パラメータ族 $F(x, y, t) = 0$ の包絡線を $F = 0, \\ \\partial F / \\partial t = 0$ の連立から求める一般理論があります（解析学・微分幾何）。

**実世界の応用**：

- **音響学**：音波が物体表面で反射する範囲、ソナーで捉えるエコー領域
- **天文学**：彗星や小惑星の軌道族の通過領域（衝突予測）
- **電磁気学**：荷電粒子が磁場中で描く軌道族の通過範囲
- **コンピュータグラフィクス**：パラメトリック曲面のレンダリング、レイトレーシングの影絵計算
- **入試**：難関大学の図形と方程式の典型問題（東大・京大・東工大など）

**判別式・解の存在条件のテクニックは他にも**：

- 「$2$ つの曲線が共有点を持つための条件」 $\\to$ 連立方程式の解の存在 $\\to$ 判別式
- 「$2$ 次関数のグラフが直線と接する条件」 $\\to$ 重解条件 $\\to$ 判別式 $= 0$
- 「$2$ 次方程式が実数解 $2$ つを持つための係数の範囲」 $\\to$ 判別式 $\\geq 0$

「$X, Y$ を定数視 $\\to$ $t$ を変数視 $\\to$ 解の存在条件」のフレームは、図形と方程式・$2$ 次関数・媒介変数表示の総決算となる発想です。`,
    scenes: `**「動く図形の影絵」を捉えたいとき**

- **時刻 $t$ で動く直線（ワイパー）**：ガラスを拭った範囲、レーダーの探知範囲
- **パラメータ付き放物線の通過領域**：射出角度を変えた砲弾の到達範囲
- **円の動く範囲**：自動車のヘッドライトが照らす範囲、サーチライトの動き
- **入試難問**：「パラメータを含む直線 / 曲線の通過領域を図示せよ」
- **物理シミュレーション**：振り子・回転体の動く範囲、粒子の軌道族
- **CG・アニメーション**：動く図形の軌跡から領域を構成
- **天文学**：軌道族の通過範囲、惑星の重力圏`,
    relatedTerms: `- [領域] — 通過領域はパラメータ付き図形が描く領域
- [媒介変数表示] — パラメータ $t$ で図形を記述する発想の延長
- [判別式] — $t$ の方程式の解の存在条件を判定する道具
- [軌跡] — 「動く点の軌跡」と「動く図形の通過領域」は表裏一体
- **包絡線** — 通過領域の境界、すべてのパラメータ図形に接する曲線
- **解の存在条件** — 「ある $t$ が存在する」を方程式論で扱う技法`,
    relatedSeriesId: "adv_linear_programming_01",
  },

  /* ===== 微積分 ===== */
  微分: {
    reading: "びぶん",
    short: "関数の「変化の速さ」を求める操作。グラフの接線の傾きを点ごとに求める道具。",
    meaning: `**「ほんの少し動いたとき、どれだけ変化するか」を点ごとに測る道具。**

関数 $f(x)$ を **微分** すると、新しい関数 $f'(x)$ が得られます。$f'(x)$ は「**$x$ が点 $x$ でほんの少し動いたとき、$f(x)$ がどれだけ動くか**」——**点 $x$ での変化の速さ** を表します。

直線の [傾き] は「$x$ の差 ÷ $y$ の差」で、直線全体で同じ。一方、曲線の傾きは **点ごとに違う**。その「**点ごとの傾き**」を返してくれるのが微分です。

**基本ルール**：$x^n$ を微分すると $n \\cdot x^{n-1}$。

| 元の関数 | 微分後 |
|---|---|
| $x$ | $1$ |
| $x^2$ | $2x$ |
| $x^3$ | $3x^2$ |
| $a x^n$ | $a n \\cdot x^{n-1}$ |
| 定数 $c$ | $0$（変化しない） |

「**指数を前に下ろして、指数を $1$ 減らす**」と覚える。

────────

**もっと深く** — 微分の威力

**接線の傾き**：$f'(a)$ は、$y = f(x)$ のグラフ上の点 $(a, f(a))$ における **接線の傾き**。

**最大・最小の検出**：関数の最大値や最小値は、グラフが水平に近づく場所——つまり $f'(x) = 0$ の場所で起こります。だから「$f'(x) = 0$ を解く → 最大・最小の候補」という戦略が使えます。

これは [平方完成] で見つけた頂点と **完全に一致**：$f(x) = x^2 + bx + c$ なら $f'(x) = 2x + b = 0$ → $x = -b/2$（頂点の $x$ 座標）。

**物理での威力**：

- 位置を微分すると **速度**
- 速度を微分すると **加速度**

ニュートンの運動方程式 $F = ma$ は「力 = 質量 × 位置の $2$ 階微分」——微分が物理の言語そのものになっています。

**歴史**：$17$ 世紀にニュートンとライプニッツが独立に発見。「**変化を捉える**」道具として、現代科学のすべての分野で使われています。`,
    scenes: `- **最大・最小問題**：利益・面積・距離を最適化する点を見つける
- **物理**：速度・加速度・運動方程式
- **経済**：限界費用・限界収益
- **機械学習**：勾配降下法で損失関数を最小化（微分が中心道具）
- **設計**：曲線の最も急な点・なめらかな点を見つける`,
    relatedTerms: `- [傾き]：直線の傾きを「点ごと」に拡張したのが微分
- [頂点]：$f'(x) = 0$ の解が頂点の $x$ 座標
- [タンジェント]：接線の "tangent" は「触れる線」、微分が返す傾きの正体`,
    relatedSeriesId: "algebra2_diff_01",
  },

  /* ===== 数列 ===== */
  等差数列: {
    reading: "とうさすうれつ",
    short: "次の項に行くたびに同じ数を足す数列。「+ 同じ数」を繰り返す。",
    meaning: `**毎回「同じ数を足す」リズムで進む数列。**

$1, 3, 5, 7, 9, \\dots$ のように、毎回 **同じ数を足して** 進む数列が **等差数列**。

| 数列 | 初項 $a$ | 公差 $d$ |
|---|---|---|
| $1, 3, 5, 7, \\dots$ | $1$ | $+2$ |
| $2, 5, 8, 11, \\dots$ | $2$ | $+3$ |
| $10, 7, 4, 1, \\dots$ | $10$ | $-3$ |

足す数 $d$（**公差**）は **負** でもよい（減っていく数列）。

**第 $n$ 項**：

$$a_n = a + (n - 1) d$$

「初項に $d$ を $(n-1)$ 回足したもの」——直線的な増え方。

────────

**もっと深く** — 和の公式とガウスのアイデア

**和**：$a_1 + a_2 + \\dots + a_n = \\dfrac{n (a_1 + a_n)}{2}$

「**ガウスのアイデア**」：$1$ から $100$ までの和を小学生のガウスが瞬殺したエピソード——逆順を書いて足すと、すべてのペアが $101$ になる：

$$\\begin{array}{rcccccc} 1 & + & 2 & + & \\dots & + & 100 \\\\ 100 & + & 99 & + & \\dots & + & 1 \\\\ \\hline 101 & + & 101 & + & \\dots & + & 101 \\end{array}$$

$100$ 個の $101$ の合計が $2$ 倍なので、答えは $\\dfrac{100 \\times 101}{2} = 5050$。

「**逆順を足してペアにする**」——これが和の公式の正体です。`,
    scenes: `- **階段・座席配置**：等差で並ぶ
- **積み重ねの計算**：高さが等差で増える
- **クーポン・割引**：「毎回 $100$ 円引き」
- **数列パズル**：規則性を見抜く問題
- **物理：等加速度運動**：速度が等差数列で増える`,
    relatedTerms: `- [等比数列]：「足す」が「掛ける」に変わったもの
- **公差**：等差数列の毎回足す数 $d$`,
    relatedSeriesId: "algebra2_arith_nth_01",
  },
  等比数列: {
    reading: "とうひすうれつ",
    short: "次の項に行くたびに同じ数を掛ける数列。倍々ゲーム。",
    meaning: `**毎回「同じ数を掛ける」リズムで進む数列——倍々ゲーム。**

$1, 2, 4, 8, 16, \\dots$ のように、毎回 **同じ数を掛けて** 進む数列が **等比数列**。

| 数列 | 初項 $a$ | 公比 $r$ |
|---|---|---|
| $1, 2, 4, 8, \\dots$ | $1$ | $\\times 2$ |
| $1, 3, 9, 27, \\dots$ | $1$ | $\\times 3$ |
| $100, 10, 1, 0.1, \\dots$ | $100$ | $\\times 0.1$ |

**第 $n$ 項**：

$$a_n = a \\cdot r^{n - 1}$$

「初項に公比 $r$ を $(n-1)$ 回掛ける」——[等差数列] の「足す」が「掛ける」に変わっただけ。

そして「同じ数を何回も掛ける」のは **指数関数** そのもの——だから等比数列は **離散的な指数関数** と言えます。

────────

**もっと深く** — 爆発的増加と等比数列の和

**威力**：倍々ゲームは爆発的に増えます。

- $2^{10} = 1024$
- $2^{20} \\approx 100$ 万
- $2^{30} \\approx 10$ 億

「同じ操作を $30$ 回繰り返しただけで $10$ 億」——この感覚が指数の威力。

**和の公式**：

$$S_n = a \\cdot \\dfrac{r^n - 1}{r - 1} \\quad (r \\neq 1)$$

「**無限級数**」（$n \\to \\infty$ の和）：

- $|r| < 1$ なら、無限和は $\\dfrac{a}{1 - r}$（収束）
- $|r| \\geq 1$ なら、無限和は無限大（発散）

**身の回りの例**：

- 細菌の増殖（$2$ 倍で増える）
- 複利計算（毎年 $r$ 倍）
- コンピュータの記憶容量（$2$ 倍ずつ）
- 連鎖反応（核分裂・チェーンメール）
- 紙を折る（厚みが $2$ 倍）`,
    scenes: `- **複利計算**：毎年 $5\\%$ の利息なら $1.05$ の等比数列
- **細菌・ウイルス**：$1$ 個が $2$ 個になるたびに倍増
- **コンピュータ**：メモリ・記憶容量・通信速度の進化
- **音楽の音階**：オクターブで $2$ 倍、半音で $2^{1/12}$ 倍の等比数列
- **量子論**：エネルギー準位が等比的に並ぶ系`,
    relatedTerms: `- [等差数列]：「掛ける」が「足す」に変わったもの
- **指数**：「同じ数を何回も掛ける」操作そのもの
- **対数**：等比数列を等差数列に直す道具`,
    relatedSeriesId: "algebra2_geo_nth_01",
  },

  /* ===== 統計 ===== */
  平均: {
    reading: "へいきん",
    short: "全データの合計を個数で割った値。「ならして全部同じにしたらいくつか」。",
    meaning: `**「ならして全部同じにしたら、いくつになるか」**

データを全部足して、個数で割った値が **平均**：

$$\\text{平均} = \\dfrac{\\text{合計}}{\\text{個数}}$$

**例**：$3$ 個の数 $6, 8, 10$ の平均は $(6 + 8 + 10) \\div 3 = 8$。

意味は **「ならして全部同じにしたら何になるか」**。$6, 8, 10$ という凸凹を平らにすると、全部 $8$ になる——その「水位」が平均。

────────

**もっと深く** — 平均の弱点と他の代表値

**[ハズレ値] に弱い**：平均は **全データを使う** ので、極端な値に引っ張られます。

たとえば $1, 1, 1, 1, 100$ の平均は $20.8$。でも「ふつうの感覚」では $1$ の方がこのデータを代表していそう。

このとき [中央値]（並び替えて真ん中）を見ると $1$。**真ん中の値は極端な値に強い**——だから所得分布のように極端な値があるデータでは、中央値の方が「ふつう」を表します。

**$3$ つの代表値**：

| 代表値 | 定義 | 強み |
|---|---|---|
| 平均 | 合計 $\\div$ 個数 | 計算しやすい、合計と一致する |
| [中央値] | 並び替えて真ん中 | 極端な値に強い |
| **最頻値** | 一番多く出る値 | 「典型的」を表す |

データの形（分布）によって、どの代表値を使うべきかが変わります。`,
    scenes: `- **テストの点数**：クラスの平均点
- **野球の打率・防御率**：平均値
- **天気予報**：平均気温
- **賃金統計**：平均年収（中央値も併記されることが多い）
- **物理**：平均速度、平均自由行程`,
    relatedTerms: `- [中央値]：並び替えて真ん中の値。ハズレ値に強い
- **最頻値**：一番多く出る値
- [分散]：平均からのバラつき
- [ハズレ値]：平均を引っ張る極端な値`,
    relatedSeriesId: "e5_mean_01",
  },
  中央値: {
    reading: "ちゅうおうち",
    short: "並び替えてちょうど真ん中の値。ハズレ値に強い代表値。",
    meaning: `**並び替えて、ちょうど真ん中の値。**

データを **小さい順に並べて**、ちょうど中央の値が **中央値**。

**例**：$3, 1, 4, 1, 5$ → 並び替え → $1, 1, \\textbf{3}, 4, 5$ → 真ん中（$3$ 番目）は **$3$**。

データの個数が偶数のときは、**真ん中の $2$ つの平均**：$2, 4, 6, 8$ なら真ん中は $4$ と $6$ → 中央値は $5$。

**[平均] との違い**：

- 平均は **全データを使う** → 極端な値に引っ張られやすい
- 中央値は **位置だけ** を使う → 極端な値に **強い**

**極端な値があるとどうなるか**：

$1, 1, 1, 1, 100$ の平均は $20.8$ だが、中央値は $1$。

「ふつう」を表現しているのは **中央値の方**——だから所得統計のような「裾の長い分布」では、中央値の方が現実的な代表値です。

────────

**もっと深く** — 四分位数と箱ひげ図

**四分位数**：データを並び替えて $4$ 等分する位置の値。

- 第 $1$ 四分位数：$25\\%$ の位置
- 第 $2$ 四分位数（= **中央値**）：$50\\%$ の位置
- 第 $3$ 四分位数：$75\\%$ の位置

**箱ひげ図**：四分位数を使って、データの **広がりと中心** を一目で見せる図。中央値は箱の中の太線として表現される。

**[ハズレ値] の検出**：箱から大きく外れた点（多くの場合 IQR の $1.5$ 倍以上離れた点）がハズレ値とされる。`,
    scenes: `- **所得統計**：「平均年収」と「中央値年収」の併記
- **不動産価格**：地域の中央値で「ふつうの家の価格」を表す
- **テストの点数**：上位 $50\\%$ の境目を見るのに使う
- **箱ひげ図**：データの分布を視覚化するときの基本
- **医療統計**：患者の検査値の中央値で「典型的な値」を表す`,
    relatedTerms: `- [平均]：全データを使う代表値（中央値はハズレ値に強い）
- **最頻値**：一番多く出る値
- [ハズレ値]：箱ひげ図で検出される極端な値`,
    relatedSeriesId: "stats_median_01",
  },
  分散: {
    reading: "ぶんさん",
    short: "データが平均からどれだけ散らばっているかを測る量。平均からのずれの 2 乗の平均。",
    meaning: `**「データのバラつき具合」を 1 つの数で表す。**

データが平均の周りに **集中している** か、**バラバラに散らばっている** か——これを測る量が **分散**：

$$\\text{分散} = \\dfrac{(x_1 - \\bar{x})^2 + (x_2 - \\bar{x})^2 + \\dots + (x_n - \\bar{x})^2}{n}$$

ここで $\\bar{x}$ は [平均]。「各データと平均との差を **$2$ 乗** して、平均する」。

**なぜ $2$ 乗か**：そのまま足すと、平均より大きい値と小さい値が打ち消し合って **常に $0$** になってしまう。$2$ 乗で全部プラスに揃えてから平均する——これが分散の発想。

**例で確かめる**：

| データ | 平均 | 分散 |
|---|---|---|
| $1, 2, 3, 4, 5$ | $3$ | $2$ |
| $3, 3, 3, 3, 3$ | $3$ | $0$（バラつきなし） |
| $0, 0, 3, 6, 6$ | $3$ | $7.2$（より散らばっている） |

────────

**もっと深く** — 標準偏差と偏差値

**標準偏差** $\\sigma = \\sqrt{\\text{分散}}$：分散はデータの「単位の $2$ 乗」になっている（cm のデータなら cm² の分散）。ルートを取って **データと同じ単位** に戻したものが標準偏差——「**平均からの典型的なずれ**」を表します。

**偏差値**：

$$\\text{偏差値} = 50 + \\dfrac{\\text{個別の点} - \\text{平均}}{\\text{標準偏差}} \\times 10$$

平均が $50$、標準偏差 $10$ になるように **整えた点数**。日本の入試で使われる「偏差値」はこの式そのもの。

**正規分布との関係**：自然界や社会のデータの多くは、平均 $\\bar{x}$、標準偏差 $\\sigma$ の **正規分布** に近い形をします：

- 平均 $\\pm \\sigma$ の範囲に約 $68\\%$ のデータ
- 平均 $\\pm 2\\sigma$ の範囲に約 $95\\%$ のデータ
- 平均 $\\pm 3\\sigma$ の範囲に約 $99.7\\%$ のデータ

「$3\\sigma$ を超えるデータ」が [ハズレ値] とみなされる目安。`,
    scenes: `- **テストの偏差値**：標準偏差から計算
- **品質管理**：製品の寸法のバラつきを分散で管理（$6\\sigma$ など）
- **株式・金融**：価格変動のリスク指標
- **物理**：測定誤差の評価
- **機械学習**：データの前処理（標準化 = 平均 $0$、分散 $1$）`,
    relatedTerms: `- [平均]：分散の計算に使う中心の値
- **標準偏差**：分散のルート、データと同じ単位
- [ハズレ値]：分散が大きい原因になる極端な値`,
    relatedSeriesId: "algebra_variance_01",
  },

  /* ===== 三角比 ===== */
  タンジェント: {
    reading: "たんじぇんと",
    short: "直角三角形のある角に対して「のぼった高さ ÷ 進んだ長さ」。坂のかたむきを 1 つの数で表すもの。",
    meaning: `**中心の問い** ｜ どうすれば坂のかたむきを 1 つの数で表せる？

────────

**「同じかたむきなら 進んだ長さがちがっても のぼり方は同じ」**

角がゆるくなると **のぼり方が小さくなり**、角が急になると **のぼり方が大きくなる**。

<<SLOPE_COMPARE>>

この「のぼり方を 1 つの数で表したもの」が **タンジェント** です。

**坂のきつさを、どう 1 つの数で表すか**

坂を登っている人をイメージしてみましょう。同じ角度の坂を $1$ 歩進んだら少しだけ上がる。$10$ 歩進んだら $10$ 倍上がる。
このとき **「歩いた分のうち、どれだけ上がったか」** が、坂のきつさを表す自然な数になりそうです。

<<TANGENT_DEF>>

これを式で書くと：

$$\\tan \\theta = \\dfrac{\\text{のぼった高さ}}{\\text{進んだ長さ}}$$

これがタンジェントの **定義の動機**——「歩いた分と上がった分の比」で、坂のきつさを 1 つの数にするという発想。

**なぜ「角だけで決まる量」と言えるのか**

ここで大事な観察があります。同じ角度の坂なら、$1$ cm スケールでも $1$ m スケールでも、**比は変わらない**——三角形の大きさを変えても、この比は同じ値になる。

<<TANGENT_SCALE>>

だからこそタンジェントは「**角度の性質**」と呼べる量になります。坂の大きさによらない、**形だけがもつ情報**。これが三角比の正体です。

**まずは概数でつかむ**：

| 角 | ざっくり覚え方 |
|---|---|
| $30°$ | 横の **約 $0.6$ 倍** の高さ（ゆるい坂） |
| $45°$ | **横と同じ高さ**（屋根の真ん中の角度） |
| $60°$ | 横の **約 $2$ 倍** の高さ（急な坂） |
| $70°$ | 横の **約 $3$ 倍** の高さ（かなり急な坂） |
| $89°$ | ほぼ **垂直** の壁（$\\tan$ は約 $57$ にもなる） |

「$60°$ なら約 $2$ 倍」「$70°$ なら約 $3$ 倍」とざっくり覚えると、坂やかたむきの感覚がすぐつかめます。

────────

**もっと深く** — 学校で出てくる呼び方と、ざっくりの裏にある正確な値

「のぼった高さ」は学校では **向かいの辺**、「進んだ長さ」は **となりの辺** と呼びます（角 $\\theta$ から見て向かい側／となり側にあるから）。いちばん長い斜めの辺は **斜辺**。

$$\\tan \\theta = \\dfrac{\\text{向かいの辺}}{\\text{となりの辺}} = \\dfrac{a}{p}$$

**ざっくりの裏にある正確な値**：

| 角 | $\\tan$ の正確な値 | 小数の概数 | ざっくり覚え方 |
|---|---|---|---|
| $30°$ | $\\dfrac{1}{\\sqrt{3}}$ | $\\approx 0.5774$ | 約 $0.6$ 倍 |
| $45°$ | $1$（ぴったり） | $1.0000$ | $1$ 倍 |
| $60°$ | $\\sqrt{3}$ | $\\approx 1.7321$ | 約 $2$ 倍 |
| $89°$ | — | $\\approx 57.2900$ | ほぼ垂直 |

ここで「$\\sqrt{3}$」のようなルートを含む数は **無理数** といって、小数で正確に書けない数（$1.7320508 \\dots$ と無限に続く）。だから日常では「約 $2$ 倍」のように、覚えやすい **概数（おおよその値）** で扱うのが普通です。

ちなみに：上の <<TANGENT_SCALE>> の絵は「横の $2$ 倍」で描いているので、実際の角度は厳密には $60°$ ではなく **約 $63°$**（$\\tan = 2$ になる角）。でも「$60°$ なら約 $2$ 倍」と覚える方がイメージとして強いので、入口ではこの覚え方をおすすめしています。

**ポイント**：

- 角が $90°$ に近づくと $\\tan$ は **急に大きくなる**。$90°$ ちょうどでは定義できない（横が $0$ になって割れない）
- $\\tan 45° = 1$ だけは **ぴったり**（無理数じゃない）
- $1°$ きざみの一覧は [三角比] のページにあります

────────

**問いに戻ると**

「どうすれば坂のかたむきを 1 つの数で表せる？」——その答えが **タンジェント**。

「**のぼった高さ ÷ 進んだ長さ**」という $1$ つの比で、坂の急さが完全に決まる。
同じ角度なら、三角形の大きさが変わってもこの比は変わらない——だから「**角だけで決まる量**」と呼べる。$30°$ なら横の約 $0.6$ 倍、$60°$ なら約 $2$ 倍、$90°$ で無限大、というふうに、角ひとつで坂のすべてが言える。`,
    scenes: `**川幅は、どうやって測る？**

向こう岸の木までの距離を、川を渡らずに知りたい——昔の人が三角比を使ったのは、まさにこういう場面です。

<<RIVER_WIDTH>>

岸を **$10$ m** 歩いて、そこから木を見上げる角度をはかったら **$60°$** だった。

$$\\text{川幅} = 10 \\times \\tan 60° \\approx 10 \\times 2 = \\text{約 } 20 \\text{ m}$$

大事なのは「**渡らずに測れる**」こと。三角比は、地球そのものを物差しにする道具です。

**そのほかの使う場面**：

- **木や建物の高さを測る** — 根元から $10$ m 離れて、てっぺんを見上げた角度が $60°$ なら、高さは 約 $17$ m（より正確には $10 \\times \\sqrt{3}$ m）
- **坂・道路の勾配** — 標識「勾配 $10\\%$」は $\\tan = 0.1$（$100$ m 進んで $10$ m 上がる坂）
- **影の長さで太陽の高さを知る** — 身長 $1.6$ m、影 $2$ m → $\\tan \\theta = 0.8$ → 太陽はおよそ $39°$
- **スマホの傾き検知・カメラの画角** — センサーが計算してくれている裏には $\\tan$ がいる

**出典**

- 池田洋介『数学Ⅰ・A 入門問題精講 改訂版』旺文社
  — 川幅測定の設例に着想を得て、ruisuishiki 用にオリジナル SVG で描き起こし。`,
    relatedTerms: `- [サイン]（正弦） — 同じ直角三角形の、斜辺で「向かいの辺」を割った値
- [コサイン]（余弦） — 同じ直角三角形の、斜辺で「となりの辺」を割った値
- [三角比] — サイン・コサイン・タンジェントの 3 つを まとめた呼び方

実は $\\tan \\theta = \\dfrac{\\sin \\theta}{\\cos \\theta}$（タンジェントは「サイン ÷ コサイン」で書けます）。
三兄弟は別物に見えて、根っこは **同じ三角形の比** です。`,
  },

  サイン: {
    reading: "さいん",
    short: "斜辺 1 のときに「上がった高さ」。$\\sin \\theta = a/c$（$c$ は斜辺、$a$ は向かいの辺）。",
    meaning: `**斜面の道を $1$ m 歩いたとき、何 m 上がったか——それがサイン。**

斜面を $1$ m 進む（**斜辺を $1$ と決める**）と、実際に上がる高さは角度 $\\theta$ によって変わります。この「**上がった高さ**」を表すのが **サイン**：

<<UNIT_TRIANGLE>>

$$\\sin \\theta = \\dfrac{\\text{のぼった高さ}}{\\text{斜面の長さ}} = \\dfrac{a}{c}$$

**ざっくり覚え方**：

| 角 | のぼり方（斜面 $1$ のうち） |
|---|---|
| $0°$ | $0$（**横にまっすぐ進むだけ、上がらない**） |
| $30°$ | $\\dfrac{1}{2}$（**ちょうど半分**） |
| $45°$ | 約 $0.7$ |
| $60°$ | 約 $0.87$ |
| $90°$ | $1$（**真上に行く**） |

「$30°$ なら **半分** 上がる」「$90°$ なら **真上**（全部が高さに化ける）」——この $2$ 点でサインの感覚はつかめます。

────────

**もっと深く** — 学校での呼び方と、正確な値

「のぼった高さ」は学校では **向かいの辺**、斜面の長さは **斜辺** と呼びます。

$$\\sin \\theta = \\dfrac{\\text{向かいの辺}}{\\text{斜辺}} = \\dfrac{a}{c}$$

**特別な角の正確な値**：

| 角 | $\\sin$ の正確な値 | 小数の概数 |
|---|---|---|
| $0°$ | $0$ | $0$（ぴったり） |
| $30°$ | $\\dfrac{1}{2}$ | $0.5000$（ぴったり） |
| $45°$ | $\\dfrac{\\sqrt{2}}{2}$ | $\\approx 0.7071$ |
| $60°$ | $\\dfrac{\\sqrt{3}}{2}$ | $\\approx 0.8660$ |
| $90°$ | $1$ | $1$（ぴったり） |

$\\sin 30° = 1/2$ は **きっちり半分**、$\\sin 90° = 1$ は **真上に行く**（横に進まず、全部が高さに化ける）——「サインで覚えるべきぴったりの値」です。それ以外は無理数。

**$1°$ きざみの一覧** は [三角比] のページにあります。`,
    scenes: `**斜めの動きの「縦の成分」を取り出したいとき**

[タンジェント] は「横の何倍の高さか」でかたむきを表しましたが、サインは **斜辺のうち何割が高さに化けたか** を表します。

- **斜面のきつさ**：傾斜 $30°$ の坂を $100$ m 歩いたら、$50$ m 上がる（$100 \\times \\sin 30° = 50$）
- **エスカレーター**：長さ $20$ m、角度 $30°$ なら、上がる高さは $10$ m
- **物理：放物線運動**——初速 $v$、打ち上げ角 $\\theta$ で、**縦の初速度成分** $= v \\sin \\theta$
- **波・音・電気**：時間とともに $\\sin(\\omega t)$ の形でゆらぐ——「**正弦波**」の名前の由来
- **音楽**：純音は時間に対するサインの波。重ねると倍音、ハーモニーになる

「サイン」は **斜めの動きを縦に分解する** ときの定番の道具です。

**「正弦」という名前の由来**：弓の弦（つる）の長さに似ているから。古代インド・アラビアの天文学で使われ、「弓の弦」を意味する言葉が文化を渡って "sinus" → "sine" になりました。`,
    relatedTerms: `- [コサイン]（余弦） — 同じ斜面で「横に進んだ距離」。サインの相棒
- [タンジェント]（正接） — サインとコサインの比。$\\tan \\theta = \\dfrac{\\sin \\theta}{\\cos \\theta}$
- [三角比] — サイン・コサイン・タンジェントの $3$ つをまとめた呼び方

**サインとコサインを結ぶ最大の公式**：

$$\\sin^2 \\theta + \\cos^2 \\theta = 1$$

これは [ピタゴラス数] の定理を「斜辺 $1$ の直角三角形」に当てただけ。$\\text{高さ}^2 + \\text{横}^2 = 1$ ——三角比の屋台骨です。`,
  },
  コサイン: {
    reading: "こさいん",
    short: "斜辺 1 のときに「横に進んだ距離」。$\\cos \\theta = b/c$（$c$ は斜辺、$b$ はとなりの辺）。",
    meaning: `**斜面の道を $1$ m 歩いたとき、横にどれだけ進んだか——それがコサイン。**

斜面を $1$ m 進む（**斜辺を $1$ と決める**）と、横方向にどれだけ進んだかが角度 $\\theta$ によって変わります。これを表すのが **コサイン**：

<<UNIT_TRIANGLE>>

$$\\cos \\theta = \\dfrac{\\text{横に進んだ距離}}{\\text{斜面の長さ}} = \\dfrac{b}{c}$$

**ざっくり覚え方**：

| 角 | 横の進み方（斜面 $1$ のうち） |
|---|---|
| $0°$ | $1$（**横にまっすぐ**——上がらない） |
| $30°$ | 約 $0.87$ |
| $45°$ | 約 $0.7$ |
| $60°$ | $\\dfrac{1}{2}$（**ちょうど半分**） |
| $90°$ | $0$（**真上に行く**——横の進みがゼロ） |

「$60°$ なら横の進みが **半分** になる」「$90°$ では **ゼロ**（真上に上がるだけ）」——この感覚が大事。

[サイン] と **役割が真逆**：角が大きくなるとサインは増え、コサインは減る。

────────

**もっと深く** — 学校での呼び方と、正確な値

「横に進んだ距離」は学校では **となりの辺**、斜面の長さは **斜辺** と呼びます。

$$\\cos \\theta = \\dfrac{\\text{となりの辺}}{\\text{斜辺}} = \\dfrac{b}{c}$$

**特別な角の正確な値**：

| 角 | $\\cos$ の正確な値 | 小数の概数 |
|---|---|---|
| $0°$ | $1$ | $1$（ぴったり） |
| $30°$ | $\\dfrac{\\sqrt{3}}{2}$ | $\\approx 0.8660$ |
| $45°$ | $\\dfrac{\\sqrt{2}}{2}$ | $\\approx 0.7071$ |
| $60°$ | $\\dfrac{1}{2}$ | $0.5000$（ぴったり） |
| $90°$ | $0$ | $0$（ぴったり） |

**[サイン] との対応**：表を見比べると、$\\sin 30° = \\cos 60° = 1/2$、$\\sin 60° = \\cos 30°$、$\\sin 45° = \\cos 45°$ ——**角を $90°$ から引いた角** を取ると、サインとコサインが入れ替わります：

$$\\sin (90° - \\theta) = \\cos \\theta, \\quad \\cos (90° - \\theta) = \\sin \\theta$$

これは「**直角三角形のもう一方の角** から見ると、向かい・となりが入れ替わる」という、視点の入れ替えそのものです。

**$1°$ きざみの一覧** は [三角比] のページにあります。`,
    scenes: `**斜めの動きの「横の成分」を取り出したいとき**

[サイン] は「縦の成分」、コサインは「横の成分」を取り出す道具です。

- **斜面の水平距離**：傾斜 $30°$ の坂を $100$ m 歩いたら、水平距離は $100 \\times \\cos 30° \\approx 86.6$ m
- **太陽光発電パネル**：パネルが地面から $\\theta$ 傾いていると、受ける光の強さは $\\cos \\theta$ に比例（真上向きが最大）
- **物理：放物線運動**——初速 $v$、打ち上げ角 $\\theta$ で、**横の初速度成分** $= v \\cos \\theta$
- **時計の針**：$3$ 時方向（横）からの角度を測ると、針の x 座標は $\\cos \\theta$ で出る
- **波・音・電気**：サインと並んで $\\cos(\\omega t)$ の形でゆらぐ——「余弦波」とも呼ばれる

**「余弦」という名前の由来**：「**余**」は「**補い合う**」の意。サイン（正弦）の **補い合う相方**（残りの $90°$ の方）の弦、という意味から「余弦」になりました。だから $\\cos \\theta = \\sin(90° - \\theta)$ の関係そのものが、名前に込められています。`,
    relatedTerms: `- [サイン]（正弦） — 同じ斜面で「上がった高さ」。コサインの相棒
- [タンジェント]（正接） — サインとコサインの比。$\\tan \\theta = \\dfrac{\\sin \\theta}{\\cos \\theta}$
- [三角比] — サイン・コサイン・タンジェントの $3$ つをまとめた呼び方

**サインとコサインを結ぶ最大の公式**：

$$\\sin^2 \\theta + \\cos^2 \\theta = 1$$

斜辺 $1$ の直角三角形に [ピタゴラス数] の定理を当てただけ。**「斜面 $1$ を縦と横に分けると、それぞれの $2$ 乗の和が必ず $1$」**——三角比の中で何度も顔を出す、屋台骨の関係式です。`,
  },
  三角比: {
    reading: "さんかくひ",
    short: "サイン・コサイン・タンジェントを総称する呼び方。同じ角の三角形の辺の比をひとまとめにしたもの。",
    meaning: `直角三角形のある角 $\\theta$ について、3 つの辺の比を 3 つの名前で呼びわけたもの —— これが **三角比** です。

| 名前 | 記号 | 何の比？ |
|---|---|---|
| [サイン]（正弦） | $\\sin \\theta = \\dfrac{a}{c}$ | 斜辺で「向かいの辺」を割る |
| [コサイン]（余弦） | $\\cos \\theta = \\dfrac{b}{c}$ | 斜辺で「となりの辺」を割る |
| [タンジェント]（正接） | $\\tan \\theta = \\dfrac{a}{b}$ | 「向かい」を「となり」で割る |

ここで $c$ は斜辺、$a$ は角 $\\theta$ の **向かいの辺**、$b$ は **となりの辺**。

**特別な角の正確な値**：

| 角 | $\\sin$ | $\\cos$ | $\\tan$ |
|---|---|---|---|
| $0°$ | $0$ | $1$ | $0$ |
| $30°$ | $\\dfrac{1}{2}$ | $\\dfrac{\\sqrt{3}}{2}$ | $\\dfrac{1}{\\sqrt{3}}$ |
| $45°$ | $\\dfrac{\\sqrt{2}}{2}$ | $\\dfrac{\\sqrt{2}}{2}$ | $1$ |
| $60°$ | $\\dfrac{\\sqrt{3}}{2}$ | $\\dfrac{1}{2}$ | $\\sqrt{3}$ |
| $90°$ | $1$ | $0$ | 定義できない |

これら以外の角の値は、ほとんどが **無理数（ルートを含む数で、きれいな分数にならない）** です。だから日常では **小数の概数（おおよその値）** で扱います。`,
    figures: `**三角比の表（$0° 〜 90°$）**

$1°$ きざみで $\\sin, \\cos, \\tan$ の値を **小数第 4 位まで** 示した表です。特別な角（$0°, 30°, 45°, 60°, 90°$）の行は背景に色をつけて目立たせています。

<<TRIG_TABLE>>

**この表の数字はすべて概数（近似値）です**。たとえば $\\sin 60°$ の正確な値は $\\dfrac{\\sqrt{3}}{2}$ で、表の値 $0.8660$ は $\\sqrt{3}/2$ を小数で書いて 第 $5$ 位以下を切り捨てたもの。

特別な角の中でも、**正確に小数で書けるもの** は限られます：

| 値 | 正確 | この表での見え方 |
|---|---|---|
| $\\sin 30°$ | $\\dfrac{1}{2}$（厳密に $0.5$） | $0.5000$ |
| $\\cos 60°$ | $\\dfrac{1}{2}$（厳密に $0.5$） | $0.5000$ |
| $\\tan 45°$ | $1$（厳密に $1$） | $1.0000$ |
| $\\sin 60°$ | $\\dfrac{\\sqrt{3}}{2}$（無理数） | $0.8660$（近似） |
| $\\tan 60°$ | $\\sqrt{3}$（無理数） | $1.7321$（近似） |

**なぜ概数で十分？**：実生活で角度や長さを「無限に精密に」測ることはほぼないからです。建物の高さを推定したいなら $\\tan 60° \\approx 1.73$ で十分。$\\sqrt{3}$ という形でなく、すぐ使える数値で扱えるのが、表の役割です。`,
    scenes: `- **電卓が使えない時代の必需品** — 工学・天文・航海では昔から三角比の表が辞書のように使われた
- **航海・測量** — 角度から距離・高さを求める計算で、現場で表をめくる
- **建築・大工** — 屋根の傾き、階段の段差・踏面の比、土地の境界の角度
- **歴史的役割** — プトレマイオス（古代）や江戸時代の和算家も、自前の精密な三角比表を作っていた
- **現代でも生き続ける** — 電卓やコンピュータの \`sin, cos, tan\` 関数は、内部でテイラー展開などを使って近似値を出している。表の代わりに「計算アルゴリズム」が走っているだけで、思想は同じ`,
    relatedTerms: `- [サイン]（正弦） — 三角比の一つ。「斜辺ぶんの向かい」
- [コサイン]（余弦） — 三角比の一つ。「斜辺ぶんのとなり」
- [タンジェント]（正接） — 三角比の一つ。「となりぶんの向かい」。坂のかたむき
- [ピタゴラス数] — 3 辺がすべて整数の直角三角形。三角比が「きれいな分数」になる珍しい組
- **無理数** — ルートを含む数。三角比の値の多くがこれ

3 つの三角比は独立ではなく、$\\sin^2 \\theta + \\cos^2 \\theta = 1$ や $\\tan \\theta = \\dfrac{\\sin \\theta}{\\cos \\theta}$ のようにつながっています。`,
  },

  /* ===== 補助用語 ===== */
  ハズレ値: {
    reading: "はずれち",
    short: "他のデータと大きく離れた極端な値。外れ値（outlier）とも。",
    meaning: `**「ふつう」から極端に離れた値。**

データの中で、他から **極端に離れた値** を **ハズレ値**（外れ値、英語で outlier）と言います。

**例**：あるクラス $30$ 人のテストの点数：

- $28$ 人が $60$〜$80$ 点
- $1$ 人が $5$ 点
- $1$ 人が $100$ 点

$5$ 点と $100$ 点は **ハズレ値** と考えられます。

**なぜ大事か**：

- [平均] や [分散] に **大きな影響** を与える
- [中央値] や **四分位数** の方が、ハズレ値の影響を **受けにくい**
- データを見るときは「ハズレ値があるか」を **まず確認** する

────────

**もっと深く** — ハズレ値の検出と扱い方

**検出の目安**：

- 箱ひげ図で第 $1$/$3$ 四分位数から **IQR の $1.5$ 倍** 以上離れた点
- 平均 $\\pm 3\\sigma$ を超える点（正規分布の場合、約 $0.3\\%$ しか起こらない）

**ハズレ値の正体は $3$ 通り**：

1. **測定エラー** — 機械の故障・転記ミス・人為的なエラー
2. **本物のレア事象** — 自然界に時々起こる極端な値（巨大地震・株価暴落・突然変異）
3. **集団に属さないデータ** — 紛れ込んだ別カテゴリのデータ（大人のクラスに子どもが混ざっている）

**「無視すべきか、注目すべきか」**：

- ハズレ値があるからといって「すぐに削除」は危険——**本物のレア事象** なら、それこそ最も重要な情報
- 一方、明らかな測定エラーは除外した方が分析が正確になる
- データの **背景・由来** を確認してから判断する

**応用例**：

- **異常検知**：クレジットカードの不正利用検出（ハズレ値 = 怪しい取引）
- **品質管理**：製造ラインで規格から外れた製品を検出
- **天文学**：ハズレ値が新しい発見（超新星・パルサーなど）になることも
- **医療**：検査値のハズレ値が重要な診断の手がかり`,
    scenes: `- **金融**：株価急変動の検出
- **製造**：不良品の検出
- **クレカ・金融取引**：不正利用検出
- **医療検査**：異常値の発見
- **データクレンジング**：機械学習の前処理
- **天文学・物理**：新発見の手がかり`,
    relatedTerms: `- [平均]：ハズレ値に引っ張られやすい代表値
- [中央値]：ハズレ値に強い代表値
- [分散]：ハズレ値で大きく増える
- **四分位数 / 箱ひげ図**：ハズレ値の検出に使う`,
  },
};
