# 自由詩系列① 磨きリスト（先生の実walkフィードバック台帳）

運用：先生が歩いて見つけた細かい直しをここに積む。**Fable 級の判断が不要な項目は安いモデル（Opus/Sonnet/Codex）が単独で実行してよい**——ただし handoff §2 の機械チェック4点（audit_hints_kokugo / tsc / test:mora / build）を通し、コミットメッセージに本ファイルの項目番号を書き、push 後にデプロイ成功と公開URLを確認すること。文言変更で技法名を L1/L2 に持ち込まない（言い換え語彙表を守る）。

| # | 状態 | 内容 | 修正方針 |
|---|---|---|---|
| M1 | 完了（2026-08-09・`0fecbbd`） | Step1 ヒントL3 が「上は散文…下は詩」と言うが、表示は模範文カードの**左右**並び | 位置のことばをやめ、内容で指す：「『とても かわいかった』とまとめた方が[散文]。ものだけを置いた方が[詩]」の形に（画面幅・並び順が変わっても壊れない）。実装：`seriesKokugoShi.ts` step1 L3 を書き換え（引用・行分けの説明はそのまま保持） |
| M2 | 完了（2026-08-09・先生指摘） | ジャージーの歌「6本」が縦書きで横に倒れて見える | 漢数字「六本」に（縦組みの正書法。原文の数字 "6" の生々しさは横組みラテン文字の効果なので、縦書き日本語では漢数字が等価）。詩の本文に算用数字を使う場合は今後も漢数字を基本に。算用数字の見た目が要る場面が出たら縦中横（text-combine-upright）を PoemLines に足す |
