# 別モデルによる横断監査ブリーフ（Cursor / Grok 等）

作成：メイン Opus 5（2026-08-15）。初回の対象は「微分・積分」ユニット（`frontend/src/lib/seriesCalculus.ts`）。
**2026-08-15 改訂**：ユニットを差し替えられる形に一般化し、**観点7（質を上げる提案）** を追加した。

## 対象ユニットの差し替え表

本文中の「対象ファイル」は下表で読み替える。**新しいユニットを監査するときは、この表に 1 行足すだけでよい**。

| ユニット | 系列ファイル | 背骨 | 原典リスト（リポ外） | 図の接頭辞 | 報告先 |
|---|---|---|---|---|---|
| 微分・積分（11 系列 110 問） | `frontend/src/lib/seriesCalculus.ts` | `docs/calculus_series_design.md` | `~/ruisuishiki-refs/calculus_source_notes.md` | `Calc*`（29 枚） | `~/ruisuishiki-refs/audit_calculus_grok.md` |
| **数列（12 系列 120 問）** | `frontend/src/lib/seriesSequence.ts` | `docs/sequence_series_design.md` | `~/ruisuishiki-refs/sequence_source_notes.md` | `Seq*`（20 枚＋流用 6 枚） | `~/ruisuishiki-refs/audit_sequence_grok.md` |
| **統計的な推測（14 系列 140 問）** | `frontend/src/lib/seriesStatInference.ts` | `docs/statistical_inference_series_design.md` | `~/ruisuishiki-refs/statistical_inference_source_notes.md` | `Stat*`（約 33 枚＋流用） | `~/ruisuishiki-refs/audit_statistical_inference_grok.md` |
| **図形の性質（7 系列 70 問）** | `frontend/src/lib/seriesGeometry.ts` | `docs/geometry_series_design_fable.md` | `~/ruisuishiki-refs/geometry_source_notes.md` | `GEO*`（19 枚） | `~/ruisuishiki-refs/audit_geometry_grok.md` |
| **データの分析（9 系列 90 問）** | `frontend/src/lib/seriesDataAnalysis.ts` | `docs/data_analysis_series_design.md` | `~/ruisuishiki-refs/data_analysis_source_notes.md` | `Data*`（21 枚＋`PROOF_LADDER` 流用 1 枚） | `~/ruisuishiki-refs/audit_data_analysis_grok.md` |

**渡す前に、機械 5 本を先に通す**（`python3 scripts/audit_cross_refs.py <対象ファイル>`）。
機械で拾えるものを潰してから渡すと、モデルの時間が**判断の要る読み**に寄る。
2026-08-16 のデータの分析は 5 本すべてヒット 0 で渡した。

**なぜ別のモデルを挟むのか**：メイン（Claude）は自分が作った基準で自分の仕事を検査しているので、**基準の外側は構造的に見えない**。実際、微分・積分ユニットで見つかった本番の描画バグ 2 件（`$$` の漏れ 135 箇所・`unknownLabel` の 44 箇所）は、メインの検収ではなく別の実装担当が独立に報告して発覚した。**違う目を 1 枚挟むのは、検収の穴を埋めるためであって、やり直しのためではない。**

---

## 0. 監査者への 3 つの約束（最重要）

1. **ファイルを編集しない。報告だけする。** `git add` / `commit` / `push` もしない。修正はメイン（Claude）が裁定してから行う
2. **すべての指摘に「証拠」を付ける**。`ファイル:行番号` と、**該当する文字列そのもの**を引用する。証拠の無い指摘は採用できない
3. **推測で「たぶん間違い」と書かない**。確かめられなかったものは「未確認」と正直に書く。**見落としより、確信のない断定のほうが高くつく**

---

## 1. 三段階のトリアージ（この分類で報告する）

| 段階 | 定義 | 例 |
|---|---|---|
| **Blocker** | 学習者に**誤り**が届く | 答えが違う／問題文が答えを一意に定めていない／問題文に答えが書いてある／図に答えが描いてある／数式が壊れて表示されない |
| **Major** | 誤りではないが**設計原則に反する**・歩けない | ヒント Layer 1 に手順や公式名や計算がある／derivation に定型セクションが欠けている／step 間で値が食い違う／系列間で同じ式・同じ場面を使い回している／原典と同じ値を使っている |
| **Minor** | 表現・誤字・好みの範囲 | 誤字脱字／言い回しが硬い／説明が長い |

**Blocker と Major は歩く前に直す。Minor は歩きながら判断する**（先生が実際に歩いた感触のほうが、表現については確かなので）。

---

## 2. 監査の観点（この順で。**1 と 2 が本命**）

### 観点1：問題文だけを読んで、自分で解く（★最重要）

`answer` を**見る前に**、`questionText` だけを読んで自力で解く。そのうえで `answer` と突き合わせる。

- 答えが合わないなら **Blocker**
- 答えは合うが、**問題文の条件だけでは答えが 1 つに決まらない**なら **Blocker**（例：「$a$ を求めよ」で $a$ が 2 つある・範囲指定が抜けている・「約分した形で」の断りが無くて分母の取り方が複数ある）
- 答えは合うが、**問題文にすでに答えが書いてある**なら **Blocker**（戸田の「答のみで自得」が壊れる）

> メインは SymPy で「式から答え」を検算したが、それでは **問題文の日本語が答えを一意に定めているか** は見えない。**ここが別モデルにしか埋められない層**なので、いちばん時間を使ってほしい。

### 観点2：step をまたいだ内部整合

- step N の L3 が「前題で出した◯◯」と参照している値が、**本当に step N−1 の答えと一致しているか**
- derivation の「Step 1〜10 の読み」に書かれた値・結論が、**実際の step の内容と一致しているか**
- `formulaPreview` が、その step の答えと一致しているか
- `answerDisplay` が `answer` と同じ値か（分数は既約か）
- 図の説明文（SVG のキャプション）と本文が食い違っていないか

### 観点3：ヒント 3 層の規律（`CLAUDE.md`「ヒント 3 層の必須構造」が正）

- **Layer 1** に、数式・数字と演算子・手順の動詞（代入する／を引く／を足す／をかける／展開する／因数分解する／平方完成する）・公式名・答えが入っていないか
- **Layer 2** が「変わった点を 1 つだけ名指す」になっているか（Layer 1 の言い換えになっていないか）
- **Layer 3** が、ヒストリーに出ていない式を指す「宙ぶらりん参照」をしていないか

> `python3 scripts/audit_hints.py seriesCalculus.ts` は正規表現で機械的に見ているだけなので、**「手順を日本語で言い換えているだけの Layer 1」は検出できない**。そこを人（モデル）の目で見てほしい。

### 観点4：図が答えを描いていないか

`frontend/src/components/Math.tsx` の `CALC_*` コンポーネント（29 枚）。

- 問題で問うている値（傾き・切片・面積・体積・極値・解の個数・座標）が図に描かれていないか
- キャプションが**問いの形で終わって**いるか
- 図の形と、その step の設定が食い違っていないか（例：上に凸のはずが下に凸で描かれている）

### 観点5：系列間・原典との重複

- **系列間**：同じ関数の式・同じ場面・同じ答えを、別の系列が使い回していないか
- **原典**：`~/ruisuishiki-refs/calculus_source_notes.md` §2 の「衝突回避リスト」の値・式・場面を使っていないか（このファイルはリポジトリの外にある）

### 観点6：表示が壊れる記法

- KaTeX が受け付けない記法（`$__` の二重下付き・閉じていない `{`・`\` の数）
- **数式の外に素の角括弧 `[ ]`** がないか（用語リンクに食われて角括弧が消える）
- `[用語]` の中身が `frontend/src/lib/glossary.ts` に実在するキーか

---

### 観点7：質を上げる提案（★2026-08-15 追加・Blocker/Major/Minor とは別枠）

ここまでの観点1〜6 は「**欠陥を見つける**」ための目。観点7 は「**欠陥ではないが、もっとよくできる**」を出す目で、**別の枠（S＝Suggestion）として報告する**。欠陥と提案を同じ枠に混ぜると、先生が「歩く前に直すもの」と「歩きながら考えるもの」を取り違えるので、必ず分ける。

**判断の物差しは、このプロジェクトが既に持っている基準を使う**（自分の好みではなく）：

- `CLAUDE.md`「お手本系列の品質基準（7 条件）」
- `docs/quality_bar_fable_2026-07-17.md`「経験の質の基準 Q1〜Q8」
  - **Q1** 必然性の 1 問（核でしか解けない問題が山場にあるか。素朴な道が本当に破れるか）
  - **Q2** 合流 step（前の系列の道具を、言葉でなく**問題で**呼び出しているか）
  - **Q3** 同じ事実の 2 つ目の道（交差検算が 1 回は見えるか）
  - **Q4** 誤概念の名指し（理由つきで扱われているか）
  - **Q5** 図は問いの形で終えているか
  - **Q6** 各 step の L3 の最終文が、中心の問いへの部分回答になっているか
  - **Q7** derivation が「覚え方」でなく「**忘れても導ける再建の手つき**」を渡しているか
  - **Q8** 「この道具で言えないこと」を名指す step があるか

**具体的に探してほしいもの**：

1. **山場が山場になっていない step**——「必然性の 1 問」と背骨が名指しているのに、素朴な道でも解けてしまう。あるいは逆に、山場でない step のほうが難しくて配列が波打っている
2. **10 問歩いたときに退屈する区間**——同じ操作の反復が続きすぎて、差異が「値だけ」になっている箇所（一度に差異 1 つは守っていても、**差異が小さすぎて発見が起きない**ことがある）
3. **驚きが立っていない質的変化 step**——「前のやり方がそのまま通ると思わせてから、通らない」になっているか。最初から違う顔をしていると、比較から構造の再発見が起きない
4. **Q6 が形式的な step**——L3 の最終文が中心の問いに結びついておらず、取ってつけた 1 文になっている
5. **derivation の在庫化**——公式を「こうです」と並べているだけで、生やす手つきになっていない箇所
6. **既知→未知の橋が架かっていない step1**——前学年・前単元・生活の概念から問いで入れているか。いきなり新しい記号から始まっていないか
7. **中心の問いと step の乖離**——`drivingQuestion` が謳っている原理を、実際には体験させていない系列

**S の書き方（この形式を守る。守れないものは書かない）**：

```markdown
### S-1 系列◯ step◯：〔1 行で、何が惜しいか〕
- 効く基準：Q◯（または 7 条件の◯）
- 証拠：`frontend/src/lib/seriesXxx.ts:1234`
- 現状：「〔そのまま引用〕」
- 何が惜しいか：〔学習者が歩いたとき、何が起きそこねているか〕
- 代案：〔**具体的に**。差し替えの文案・別の値・step の並べ替え。「もっと丁寧に」のような
  抽象的な要望は書かない。値を変える提案なら、その値で本当に成立するか自分で確かめて書く〕
- 優先度：**歩く前** ／ **歩いた後**（先生が実際に歩いた感触で決めるべきものは「歩いた後」）
```

**分量の上限（守ること）**：

- **1 系列につき最大 2 件**。全部の系列に無理に出さない。**出ない系列は「S なし」と書く**のが正しい報告
- ユニット全体で「**これだけは歩く前に**」と言えるものを **5 件以内** に絞り、サマリの冒頭に並べる
- **良かった点も 3〜5 件書く**。どこが効いているかが分かると、次のユニットで再現できる。ここは短くてよい

**やってはいけないこと**：

- 系列の切り方・中心の問いの選び方そのものへの異議は、S ではなく従来どおり「設計への所見」に書く（直さない・先生とメインが判断する）
- 「自分ならこう書く」という**文体の全面的な書き換え提案**は不要。文体は先生とメインが決めている
- 根拠が「なんとなく物足りない」だけのものは書かない。**どの基準に照らして惜しいのか**を言えないなら、それは Minor か、書かないほうがよい

## 3. やらなくていいこと

- **値の数学的な再検算**（メインが全問を独立検算済み・NG 0）。ただし**観点1 の「自力で解く」は別物**なので、そちらはやる
- **表現の好み**（言い回しが硬い・長い等）は Minor に留める。**ただし観点7 の S として、どの基準に効くかを言えるなら代案まで書いてよい**（2026-08-15 改訂：以前は「直し方の提案は不要」としていたが、質を上げる提案は歓迎する方針に変えた）
- **設計そのものへの異議**（系列の切り方・中心の問いの選び方）は、**気づいたことを「設計への所見」に 1 節にまとめて書くだけ**にする。直さない。先生とメインが判断する
- **全系列に S を出すこと**。出ない系列は「S なし」でよい。**数合わせの提案は、本当の提案を埋もれさせるので害になる**

---

## 4. 報告の形式

`~/ruisuishiki-refs/audit_calculus_grok.md` に、次の形で書く（リポジトリの中には置かない）。

```markdown
# 〔ユニット名〕ユニット 横断監査（Grok・2026-__-__）

## サマリ
Blocker N 件 / Major N 件 / Minor N 件 / S（質を上げる提案）N 件。
観点1（自力で解く）は 全N 問中 N 問を実施。

**歩く前に直す価値があると思うもの（5 件以内）**
1. 〔B-1〕…
2. 〔S-3〕…

**効いていると思ったところ（3〜5 件・短く）**
- …

## Blocker
### B-1 系列◯ step◯：〔1 行で何が問題か〕
- 証拠：`frontend/src/lib/seriesXxx.ts:1234`
- 該当文字列：「〔そのまま引用〕」
- 何が起きるか：〔学習者にどう見えるか／なぜ誤りか〕
- 自分の計算：〔観点1 の場合は解いた過程〕

## Major
（同じ形式）

## Minor
（1 行ずつでよい）

## S（質を上げる提案・欠陥ではない）
### S-1 系列◯ step◯：〔1 行で、何が惜しいか〕
- 効く基準：Q◯ / 7 条件の◯
- 証拠：`frontend/src/lib/seriesXxx.ts:1234`
- 現状：「〔そのまま引用〕」
- 何が惜しいか：〔学習者が歩いたとき、何が起きそこねているか〕
- 代案：〔具体的に。文案・値・並べ替え。値を変える提案なら成立を自分で確かめてから書く〕
- 優先度：歩く前 / 歩いた後

## 設計への所見（直さない・参考）
〔あれば〕

## 未確認
〔時間や手段の都合で見られなかったもの。正直に〕
```

---

## 4-b. ユニット固有の観点（そのユニットを監査するときだけ足す）

観点1〜7 は全ユニット共通。以下は**そのユニットで実際に踏んだ地雷**なので、監査でも重点的に見てほしい。

### 図形の性質ユニット（7 系列 70 問）

**★ 最重要：この単元の観点1 は「配置が一意か」に集中する**

幾何は、**問題文が書いた条件が図形を一意に決めていないと、答えが決まらない**。しかもこの単元は設計段階でその事故を実際に起こしている——背骨で「A と B の角が等しくなる配置」と指定したところ、それが $\angle A = 60°$ に**一意強制**されていて原典の値と衝突し、2 系列の step10 を実装時に差し替えた（`docs/backbone_design_handoff.md` C1 追補3）。だから：

- 問題文の与件（辺の長さ・角・比・点の位置）だけで、**その図形が本当に決まるか**を確かめる。決まらないなら Blocker（答えが一意でない）
- 逆に、与件が**多すぎて矛盾していないか**も見る（3 辺と 1 角を全部指定すると両立しないことがある）
- **検算は「座標を置いた数値幾何」で**（C1 追補3）。定理を公式として仮定せず、頂点や円周上の点に実座標を置き、交点は連立で解き、角は `atan2`、比は距離から出して、答えを再現する。**定理を使って検算すると、定理の適用ミスがそのまま素通りする**

**先に知っておいてほしい設計上の約束（これを欠陥と誤解しないでほしい）**：

| 約束 | 中身 |
|---|---|
| **等式を主張する定理の step は、答えが与値と同じになることがある** | 円周角の移動・接弦定理のような「等しい」を主張する定理では、**答え＝与えられた角** という step が構造上生じる。これは**視点の翻訳**（観測点を変える・相手の弧を選ぶ）として意図的に設計されている。「問題文に答えが書いてある」と早合点しないこと。ただし**翻訳が起きていない**（本当にただ写すだけ）なら、それは指摘してよい |
| **証明は 3 つの住所に分けてある** | ①定理の証明 → derivation ②証明型問題の骨格 → **鎖の関節の数値**（1 step 1 関節）③証明の作文 → エンジン外（F6）。「証明が採点されていない」は仕様であって欠陥ではない。ただし**関節の選び方**（そこを誤ると鎖が繋がらない位置か）は見てほしい |

| # | そのほかに見るもの | なぜ |
|---|---|---|
| 1 | **比の答えが既約か・`answerDisplay` と一致するか** | 比は分数で採点している（$BF:FC$ は $BF/FC$ の値）。約分の断りが `questionText` にあるか |
| 2 | **角は度数のプレーン数値か** | `unit: ""` ＋ `unknownLabel` に（度）。度とラジアンが混ざっていないか |
| 3 | **図 19 枚（`GEO*`）に答えが描かれていないか** | 幾何は**図が答えを持ちやすい**（角度・比・長さ・交点の位置）。問うている量が図の `<text>` や座標から読めてしまわないか。目盛りや補助線から測れてしまう場合も含む |
| 4 | **メネラウスの積の符号** | 高校流（積 $=1$）で通しているか。$-1$ 流と混ざっていないか |
| 5 | **内心の比** | $AI:ID = (AB+AC):BC$。原典と同じ辺の組を使うと比まで一致してしまうので、**原典と違う辺の組**になっているか（`~/ruisuishiki-refs/geometry_source_notes.md`） |
| 6 | **五心の言い換えの乗り換え**（S 向き） | 外心・内心・重心・垂心・傍心を「どの言い換えで捉えるか」が系列4 の核。**乗り換えが問題として起きているか**、それとも言葉の紹介で終わっていないか（Q2） |

### データの分析ユニット（9 系列 90 問）

**★ 最重要：この単元の観点1 は「限定語」に集中する**

データの分析は「要約とは情報を捨てること」を主題にした単元で、**Q8（捨てたものを名指す step）の出どころ**。だから問題文に **限定語** が多く、そこが層1（問題文の日本語が答えを一意に定めているか）の当たりどころになる：

- 「**確実に**言えることの個数」「**ありうる最小の**値」「**読み取れない**」「$Q_1$・$Q_3$ **だけ**が分ける」
- こういう限定を書いた step は、**条件を満たすものを自分で全部数え上げて**、答えと一致するか確かめる。1 つでも数え漏れ・数え過ぎがあれば Blocker

| # | 見るもの | なぜ |
|---|---|---|
| 1 | **丸めた小数を答えさせていないか** | 判定は `TOL=1e-6` の厳密一致。**この単元が C1 追補2 の出どころ**で、原典の練習問題は「小数第2位まで四捨五入」型が普通なので、素直に真似ると全滅する。危険地帯は**標準偏差・相関係数・仮平均**。標準偏差は「分散を平方数にする」か「√のまま `answerDisplay`＋`[sqrt]`」の二択以外を作っていないか、相関係数は**ちょうど有理数になるデータ**か |
| 2 | **四分位数の規約** | データの個数が偶数か奇数かで定義が割れる。**系列内で規約が一貫しているか**、箱ひげ図の図と本文が同じ規約か |
| 3 | **データ列そのものの系列間重複** | 機械の②（数式トークンの重なり）は**データ列の重複を捕まえられない**ことがある。78 本のデータ列を使う単元なので、**同じ数の並びが別の系列に出ていないか**を目で見てほしい（C15 の場面パレット事前割り当てを初めて適用した単元。効いているかの検証も兼ねる） |
| 4 | **各系列に「捨てたもの」の step があるか**（S 向き） | Q8 の本家。度数分布表だけが残ったとき／階級値で代用した平均／箱ひげ図から読めないもの／検定で「否定できない」は「正しい」ではない——**この型が薄い系列があれば S に書く** |
| 5 | **系列9（仮説検定）の合流** | 確率［独立］・場合の数［組合せ］・集合と論理［背理法］の 3 ユニットが一点で合流する、数Ⅰ・A の閉じ石。**合流が derivation の言葉だけでなく、問題として立っているか**（Q2） |
| 6 | **図 21 枚に答えが描かれていないか** | ヒストグラムの度数・箱ひげ図の中央値と四分位数・散布図の相関——**問うている値が図から読み取れてしまわないか**。この単元は図が答えを持ちやすい |
| 7 | 原典の値との衝突 | `~/ruisuishiki-refs/data_analysis_source_notes.md` が正。原典 p.292 の「小数第2位まで四捨五入」型の値は**採用不可**として避けてあるはずなので、混入していないか |

### 数列ユニット（12 系列 120 問）

**★ 最重要：観点1 の数列版＝「書き出すだけで当たってしまう step」を探す**

数列はこの罠がいちばん深い単元で、`docs/backbone_design_handoff.md` の **C1 追補7** に明文化してある。

> **小さい番号を答えにすると、一般項を求めずに書き出すだけで当たってしまう。** その step は、一般項・和の公式・漸化式を解くことを**一切採点していない**。

だから各 step について、`answer` を見る前に次を実際にやってほしい：

1. **核の道具（一般項・和の公式・漸化式の解法・Σ の公式）をいっさい使わず、手で書き出すだけで答えが出せるか** を試す
2. 出せてしまうなら、それは **S（質を上げる提案）** として「何行書けば届くか」を添えて報告する（前半の土台 step なら意図どおりなので Blocker ではない）
3. **設計の意図は二層**：前半は「書き出せば解けるが、書き出すと規則が見える」土台、**山場だけが書き出しでは届かない**。この二層が崩れている系列（山場まで書き出しで解けてしまう／逆に最初から書き出せなくて入口が高い）があれば S に書く

**そのほか、数列に固有の見どころ**：

| # | 見るもの | なぜ |
|---|---|---|
| 1 | **項数の勘定が $n$ か $n-1$ か**。誤って数えたとき、**別の整数になるか**（1 ずれて偶然一致していないか） | この単元の誤概念はほぼ全部ここに集まる。偶然一致する値だと、誤概念が採点に乗らない。隙間の個数・階差の上端・下端が 1 でない Σ・群の切れ目・「第 $a$ 項から第 $b$ 項まで」 |
| 2 | **群数列（系列9）で、群番号と群内の項番号が別 step になっているか** | 「第◯群の第◯項目」は**順序対**で、判定器は順不同の多重集合一致しかできない。1 問で両方答えさせていたら **Blocker** |
| 3 | **数学的帰納法（系列12）が 1 step 1 関節になっているか** | 証明の文章は採点できないので、鎖の関節の数値に折ってある。1 step に 2 関節あると、どちらを間違えたか分からない |
| 4 | **「初めて◯を超える」「最小の」を主張する step の境目** | 前後 2 項を実際に計算して、主張どおりか確かめる。切り上げ・切り捨てが 1 つずれていないか |
| 5 | **$n \ge 2$ の但し書き・空和・上端 $n-1$ の扱い** | 数列は「無限」を扱うぶん、論理的に微妙な言い回しが多い。**「すべての自然数 $n$ で」と「$n \ge 2$ のとき」を取り違えている箇所**は Blocker になりうる |
| 6 | **分数の答えが既約か・`answerDisplay` と `answer` が一致するか** | 系列3・4・7・11・12 に分数の答えがある。`questionText` に「既約分数で」の断りがあるか |
| 7 | **原典の有名な値を使っていないか** | `~/ruisuishiki-refs/sequence_source_notes.md` が正。とくに **$1+2+\cdots+100=5050$**・ハノイの塔の 3 枚 7 手／10 枚 1023 手／60 枚 $2^{60}-1$・**$5^n-1$ が 4 の倍数**・$3^{n+1} > 3n+2$・$a_n = 1/n$ は原典の目印になる値 |
| 8 | **図 20 枚（`Seq*`）に答えが描かれていないか** | 問うている値（項の値・和・項数・総個数・通し番号・手数・公比・折り返しの番号）は `?` のはず。流用 6 枚（`CALC_TRAPEZOID_CHECK`・`COUNT_SEQ_GAP`・`IDENT_PARTIAL_FRACTION`・`INT_MULTIPLE_RULER`・`DATA_LEVELING`・`FRAC_TELESCOPE`）は**他の単元のために作られた図**なので、数列の文脈で誤解を生まないかも見てほしい |
| 9 | **原典が架けている微積との橋が渡っているか**（Q2・S 向き） | 原典自身が 3 か所で明示している——Σ の線形性（p.290）／階差数列 ↔ 導関数（p.301）／$\sum a_k = f(n+1)-f(1)$ ↔ 定積分（p.305）。渡っていない、または言葉だけで終わっている箇所は S に書く |
| 10 | **系列8 の「1 つの式か、無数の式か」が、系列10・12 の土台として効いているか**（S 向き） | この視点が単元後半（漸化式・帰納法）の急所。系列8 で立てた見方が、系列10・12 で回収されているか |

**歩く順序で見てほしいこと**（S 向き）：系列1 → 12 は原典の節順どおりに並べてある。**前半（1〜4）で退屈しないか**、**系列5（Σ）が記号の壁になっていないか**、**系列9（群数列）が単独で浮いていないか**、**系列12（帰納法）が単元の締めくくりとして着地しているか**。

### 統計的な推測ユニット（14 系列 140 問）

**★ 最重要：観点1 の統計版＝「この step は、渡された情報だけで解けるか」を確かめる**

この単元は **学習者が巻末の標準正規分布表を持っていない** ため、表を引く step では
**使う表の値が `questionText` に書かれていないと、その step はそもそも解けない**。
背骨 `docs/statistical_inference_series_design.md` の **D2** に明記した規律で、
**書き忘れが実装フェーズの最大の事故候補**だった。だから系列8〜14 の全 step について：

1. その step を解くのに必要な $p(u)$ の値が、**`questionText` に明示されているか**
   （辞書『標準正規分布表』にあるだけでは不十分。**問題文に無ければ Blocker**）
2. 与えられた値と、`answer` が**加減算だけ**で結びつくか（掛け算で桁が伸びていないか）
3. **答えの桁が `questionText` に明記されているか**（「小数第4位まで」等）。
   明記が無くて答えが小数なら、学習者は何桁で入力すべきか分からない → Blocker

**D2 の例外を先に書いておく（監査からの申し送り・2026-08-20）**：背骨が
「この系列は表を引くこと自体が核なので、値は辞書と図から取らせる」と例外にしている系列がある
（統計では**系列8 の step3・8・9**）。**例外は Blocker にしない。**
新しいユニットの監査ブリーフを書くときは、背骨の例外を 4-b にそのまま 1 行写しておくこと
——書かないと、監査が規律どおりに Blocker を量産する。

**そのほか、統計に固有の見どころ**：

| # | 見るもの | なぜ |
|---|---|---|
| 1 | **標準化した $Z=(x-m)/\sigma$ が、標準正規分布表に載っている値か**（小数第2位までの有限小数か） | 表に無い $u$ が出ると、学習者は表を引けない。$\sigma$ で割ったときに循環小数になる組が紛れていないか、全 step で確かめてほしい。**これが紛れていたら Blocker** |
| 2 | **信頼区間を 1 問で両端とも答えさせている step が無いか** | 判定器は順不同の多重集合一致しかできないので、**順序対には原理的に使えない**。両端は別 step、あるいは $d$（幅の半分）や幅 $2d$ に折ってあるはず。1 問で両方なら **Blocker** |
| 3 | **$\sqrt{n}$ と $\sqrt{p'(1-p')}$ が有限小数になっているか** | $n$ は平方数（100・400・900・2500）、$p'$ は $\sqrt{p'(1-p')}$ が有限小数になる値だけを使う約束。外れていると答えが厳密に入力できない |
| 4 | **四捨五入の使い方**（背骨 D3） | 許してあるのは①**整数の檻**（条件を満たす最小の人数・個数・題数＝丸めではなく檻）②**一意に決まる有限小数の丸め**（丸める前の値が厳密で、丸める位置を問題文が明記）の2型だけ。**中間で丸めさせる型・丸め方の順序で結果が変わる型があれば Blocker** |
| 5 | **分数の答えが既約か・`answerDisplay` と `answer` が一致するか・「既約分数で」の断りがあるか** | 系列1〜7 は分数の答えが主役 |
| 6 | **「棄却できない」を「帰無仮説が正しい」と読ませていないか**（系列14） | 原典が明言している急所——**どちらも肯定も否定もできない**が正しい。derivation か L3 で誤って断定していれば Major |
| 7 | **「母平均がこの区間に入る確率が 95%」と書いていないか**（系列12・13） | 母平均は動かない 1 つの値で、**動くのは区間のほう**。正しくは「無作為抽出で区間を作ったとき、その区間が真の値を含む確率が 95%」。書き方を誤っていれば Major |
| 8 | **$\bar{X}$（確率変数）と $\bar{x}$（実現値）の使い分け**（系列11〜14） | 原典が「この分野を学ぶ人がとくにつまずきやすいところ」と明言している唯一の箇所。記号が入り混じっていないか |
| 9 | **原典の有名な値を使っていないか** | `~/ruisuishiki-refs/statistical_inference_source_notes.md` §2 が正。とくに **7 枚のカード（$E=2$・$V=8/7$）**・**3 枚のコイン（$E=3/2$・$V=3/4$）**・**赤玉2白玉3**・**サイコロ4回で3の倍数**・**1000人・平均72.5点・標準偏差7.0点（90点以上は6人／上位15人は88点）**・**缶詰100個・220g・2.5g（$n\ge 385$）**・**高校生400人・169.5cm・$\sigma=8$**・**有権者400人中144人（$p'=0.36$）**・**前年60点／今年62.4点・標準偏差15点** は原典の目印 |
| 10 | **図 約 33 枚（`Stat*`）に答えが描かれていないか** | 問うている値（確率・期待値・分散・つり合いの支点・$Z$ の値・面積・信頼区間の端・棄却域の境目）は `?` のはず。**図の中で数えられるもの（粒・棒・人・曲線・マス・おもり）の個数と、問題文の個数が一致しているか**も見てほしい（前回のユニットでここに 1 件あった） |
| 11 | **原典が架けている 4 本の橋が渡っているか**（Q2・S 向き） | 原典自身が明示している——**二項定理 → 二項分布**（p.342 コラム・「二項」の由来）／**定積分 → 確率密度**（p.357 が p.257 を明示参照）／**不定積分 → $p(u)$**（p.358）／**数Ⅰ・A の仮説検定 → 数Ⅱ・B の仮説検定**（p.383 冒頭）。渡っていない、または言葉だけで終わっている箇所は S に書く |
| 12 | **数Ⅰ・A「データの分析」DA9（`algebra1_data_hypothesis_01`）との重複**（系列14） | 同じ考え方を、離散の数え上げ（DA9）と正規分布の上（系列14）でやる設計。**場面・値が重複していたら Major**。逆に、違いが立っていなければ S |

**歩く順序で見てほしいこと**（S 向き）：系列1 → 14 は原典の節順どおりで、
**第1部（1〜6・分布を作る）→ 第2部（7〜10・連続へ渡る）→ 第3部（11〜14・推測する）**の三部構成。
**第1部が長すぎて息切れしないか**、**系列7（連続型）が急に抽象的にならないか**、
**系列8（表引き）が作業になっていないか**、**系列14 が単元と数Ⅱ・B 全体の締めくくりとして着地しているか**。

## 5. 起動プロンプト（Cursor にそのまま貼る）

**貼る範囲で迷わないよう、各プロンプトは平文でも置いてある**：`~/ruisuishiki-refs/prompts/audit_<unit>.txt`（リポ外）。ファイルを開いて全選択・コピーで、そのまま貼れる。新しい単元のプロンプトを書いたら、同じ場所に `.txt` も落とすこと。

### 微分・積分ユニット（2026-08-15 実施済み）

```
~/ruisuishiki をワークスペースのルートとして開いた状態で作業してください。

まず docs/audit_brief_cross_model.md を全文読み、その規律どおりに
「微分・積分」ユニット（frontend/src/lib/seriesCalculus.ts・全11系列110問）を監査してください。

あわせて読むもの：
- CLAUDE.md（お手本7条件・ヒント3層・機械チェック）
- docs/calculus_series_design.md（背骨。各系列の狙いと山場の指定）
- ~/ruisuishiki-refs/calculus_source_notes.md（原典の値＝衝突回避リスト・リポ外）
- frontend/src/components/Math.tsx の CALC_* コンポーネント（図29枚）

約束：ファイルは1つも編集しない。git 操作もしない。報告だけを
~/ruisuishiki-refs/audit_calculus_grok.md に書いてください。
すべての指摘に「ファイル:行番号」と該当文字列の引用を付けてください。

いちばん時間を使ってほしいのは観点1（answer を見る前に問題文だけで自力で解き、
答えが合うか・問題文が答えを一意に定めているか・問題文に答えが書かれていないかを見る）です。
```

### 図形の性質ユニット（数Ⅰ・A 第8章・そのまま貼れる）

```
~/ruisuishiki をワークスペースのルートとして開いた状態で作業してください。

まず docs/audit_brief_cross_model.md を全文読み、その規律どおりに
「図形の性質」ユニット（frontend/src/lib/seriesGeometry.ts・全7系列70問）を
監査してください。とくに「4-b → 図形の性質ユニット」の節は必ず通してください。

あわせて読むもの：
- CLAUDE.md（お手本7条件・ヒント3層・機械チェック）
- docs/geometry_series_design_fable.md（背骨。とくに §1「証明の 3 住所」）
- docs/backbone_design_handoff.md の「C1 追補3」（幾何単元の折り方と座標検算）
- docs/quality_bar_fable_2026-07-17.md（Q1〜Q8。観点7 の物差し）
- ~/ruisuishiki-refs/geometry_source_notes.md（原典の値＝衝突回避リスト・リポ外）
- frontend/src/components/Math.tsx の GEO* コンポーネント（図19枚）

約束：ファイルは1つも編集しない。git 操作もしない。報告だけを
~/ruisuishiki-refs/audit_geometry_grok.md に書いてください。
すべての指摘に「ファイル:行番号」と該当文字列の引用を付けてください。

**機械で拾えるものは、こちらで先に潰してあります**（答えの先出し・系列間の重複・
図の生の $ と [用語]・「前題の答え」の参照先・規模の宣言——★★ 0 件、ほぼ全ゼロ）。
だからあなたの時間は、機械に見えないところに全部使ってください。

いちばん時間を使ってほしいのは、次の2つです。

(1) 観点1（answer を見る前に問題文だけで自力で解き、答えが合うか・問題文が答えを
一意に定めているか・問題文に答えが書かれていないかを見る）。
**幾何では「配置が一意に決まるか」がいちばん危ない**——問題文の与件だけでその図形が
本当に決まるか、逆に与件が多すぎて矛盾していないかを確かめてください。
この単元は設計段階で実際にその事故を起こしています（角の指定が 60° に一意強制され、
原典と衝突して 2 系列を差し替えた）。
**検算は「座標を置いた数値幾何」でやってください**——定理を公式として仮定せず、
頂点や円周上の点に実座標を置き、交点は連立で解き、角は atan2、比は距離から出して
答えを再現する。定理を使って検算すると、定理の適用ミスがそのまま素通りします。

なお、4-b に書いた 2 つの設計上の約束（①等式を主張する定理では「答え＝与値」の step が
構造上生じる ②証明は 3 つの住所に分けてあり、作文はエンジン外）は仕様です。
これを「問題文に答えが書いてある」「証明が採点されていない」と早合点しないでください。
ただし①で視点の翻訳が起きていない（本当にただ写すだけ）なら、それは指摘してください。

(2) 観点7（質を上げる提案＝S）。欠陥ではないが「もっとよくできる」ところを、
Q1〜Q8 のどれに効くかを言える形で、具体的な代案つきで出してください。
1系列につき最大2件、出ない系列は「S なし」で構いません。
「これだけは歩く前に」と言えるものを5件以内に絞ってサマリの冒頭に置き、
効いていると思ったところも3〜5件、短く書いてください。
**代案に数値を書くときは、その配置が本当に成立するか座標で確かめてから書いてください**
（前々回の監査では、代案10件のうち4件で計算が違っていました。前回は0件でした）。
```

### データの分析ユニット（数Ⅰ・A 第7章・そのまま貼れる）

```
~/ruisuishiki をワークスペースのルートとして開いた状態で作業してください。

まず docs/audit_brief_cross_model.md を全文読み、その規律どおりに
「データの分析」ユニット（frontend/src/lib/seriesDataAnalysis.ts・全9系列90問）を
監査してください。とくに「4-b → データの分析ユニット」の節は必ず通してください。

あわせて読むもの：
- CLAUDE.md（お手本7条件・ヒント3層・機械チェック）
- docs/data_analysis_series_design.md（背骨。各系列の狙い・山場の指定・C1 追補2）
- docs/quality_bar_fable_2026-07-17.md（Q1〜Q8。観点7 の物差し。**Q8 はこの単元が
  出どころ**なので、Q8 の観点はとくに厳しく見てください）
- ~/ruisuishiki-refs/data_analysis_source_notes.md（原典の値＝衝突回避リスト・リポ外）
- frontend/src/components/Math.tsx の Data* コンポーネント（図21枚）

約束：ファイルは1つも編集しない。git 操作もしない。報告だけを
~/ruisuishiki-refs/audit_data_analysis_grok.md に書いてください。
すべての指摘に「ファイル:行番号」と該当文字列の引用を付けてください。

**機械で拾えるものは、こちらで先に潰してあります**（答えの先出し・系列間の重複・
図の生の $ と [用語]・「前題の答え」の参照先・規模の宣言——5 本ともヒット 0）。
だからあなたの時間は、機械に見えないところに全部使ってください。

いちばん時間を使ってほしいのは、次の2つです。

(1) 観点1（answer を見る前に問題文だけで自力で解き、答えが合うか・問題文が答えを
一意に定めているか・問題文に答えが書かれていないかを見る）。
**この単元は「限定語」がいちばん危ない**——「確実に言えることの個数」「ありうる
最小の値」「読み取れない」「〜だけが分ける」のような限定を書いた問題では、
その条件を満たすものを自分で全部数え上げて、答えと一致するか確かめてください。
あわせて、**丸めた小数を答えさせている step が無いか**も見てください（判定は
TOL=1e-6 の厳密一致なので、「約」「小数第2位まで」で答えを作ると正確に計算した
学習者が不正解になります。標準偏差・相関係数・仮平均のあたりが危険地帯です）。

(2) 観点7（質を上げる提案＝S）。欠陥ではないが「もっとよくできる」ところを、
Q1〜Q8 のどれに効くかを言える形で、具体的な代案つきで出してください。
1系列につき最大2件、出ない系列は「S なし」で構いません。
「これだけは歩く前に」と言えるものを5件以内に絞ってサマリの冒頭に置き、
効いていると思ったところも3〜5件、短く書いてください。
**代案に数値を書くときは、その値で本当に成立するか自分で計算してから書いてください**
（前回の監査では、代案10件のうち4件で計算が違っていました）。
```

### 数列ユニット（実施済み・2026-08-15）

```
~/ruisuishiki をワークスペースのルートとして開いた状態で作業してください。

まず docs/audit_brief_cross_model.md を全文読み、その規律どおりに
「数列」ユニット（frontend/src/lib/seriesSequence.ts・全12系列120問）を監査してください。
とくに「4-b. ユニット固有の観点 → 数列ユニット」の表は必ず通してください。

あわせて読むもの：
- CLAUDE.md（お手本7条件・ヒント3層・機械チェック）
- docs/sequence_series_design.md（背骨。各系列の狙い・山場の指定・D3 の値設計の掟）
- docs/quality_bar_fable_2026-07-17.md（Q1〜Q8。観点7 の物差し）
- docs/backbone_design_handoff.md の「C1 追補7」（この単元固有の罠）
- ~/ruisuishiki-refs/sequence_source_notes.md（原典の値＝衝突回避リスト・リポ外）
- frontend/src/components/Math.tsx の Seq* コンポーネント（図20枚）

約束：ファイルは1つも編集しない。git 操作もしない。報告だけを
~/ruisuishiki-refs/audit_sequence_grok.md に書いてください。
すべての指摘に「ファイル:行番号」と該当文字列の引用を付けてください。

いちばん時間を使ってほしいのは、次の2つです。

(1) 観点1（answer を見る前に問題文だけで自力で解き、答えが合うか・問題文が答えを
一意に定めているか・問題文に答えが書かれていないかを見る）。
数列版として、各 step で「一般項・和の公式・漸化式の解法・Σ の公式をいっさい使わず、
手で書き出すだけで答えが出せるか」も実際に試してください。出せてしまう step は、
何行書けば届くかを添えて報告してください（前半の土台 step は意図どおりなので欠陥ではありません）。

(2) 観点7（質を上げる提案＝S）。欠陥ではないが「もっとよくできる」ところを、
Q1〜Q8 のどれに効くかを言える形で、具体的な代案つきで出してください。
1系列につき最大2件、出ない系列は「S なし」で構いません。
「これだけは歩く前に」と言えるものを5件以内に絞ってサマリの冒頭に置き、
効いていると思ったところも3〜5件、短く書いてください。

この単元は120問あるので、時間が足りなければ観点1を全問やることを優先し、
見られなかったところは「未確認」に正直に書いてください。
```

### 統計的な推測ユニット（数Ⅱ・B 第8章・そのまま貼れる）

```
~/ruisuishiki をワークスペースのルートとして開いた状態で作業してください。

まず docs/audit_brief_cross_model.md を全文読み、その規律どおりに
「統計的な推測」ユニット（frontend/src/lib/seriesStatInference.ts・全14系列140問）を
監査してください。とくに「4-b. ユニット固有の観点 → 統計的な推測ユニット」の表は
必ず全部通してください。

あわせて読むもの：
- CLAUDE.md（お手本7条件・ヒント3層・機械チェック）
- docs/statistical_inference_series_design.md（背骨。§0 の確定事項 D1〜D5、
  §2 の入力系ゲート、§3 の値設計の掟、§4 の各系列の狙いと山場の指定）
- docs/quality_bar_fable_2026-07-17.md（Q1〜Q8。観点7 の物差し）
- ~/ruisuishiki-refs/statistical_inference_source_notes.md（原典の値＝衝突回避リスト・リポ外）
- ~/ruisuishiki-refs/statistical_inference_impl_handoff.md（実装ブリーフ。
  §6 に標準正規分布表があるので、表の値の照合はここで行える・リポ外）
- frontend/src/components/Math.tsx の Stat* コンポーネント（図 約33枚）
- frontend/src/lib/seriesDataAnalysis.ts の DATA_HYPOTHESIS_SERIES
  （数Ⅰ・A の仮説検定。系列14 との重複チェック用）

約束：ファイルは1つも編集しない。git 操作もしない。報告だけを
~/ruisuishiki-refs/audit_statistical_inference_grok.md に書いてください。
すべての指摘に「ファイル:行番号」と該当文字列の引用を付けてください。

いちばん時間を使ってほしいのは、次の3つです。

(1) 観点1（answer を見る前に問題文だけで自力で解き、答えが合うか・問題文が答えを
一意に定めているか・問題文に答えが書かれていないかを見る）。

(2) 統計版として、この単元は学習者が巻末の標準正規分布表を持っていません。
系列8〜14 の全 step について「その step を解くのに必要な p(u) の値が questionText に
明示的に書かれているか」を1問ずつ確かめてください。書かれていなければ、
その step はそもそも解けないので Blocker です。
あわせて、標準化した Z=(x−m)/σ が標準正規分布表に載っている値（小数第2位まで）に
なっているかも、全 step で確かめてください。循環小数になる組が紛れていたら Blocker です。

(3) 観点7（質を上げる提案＝S）。欠陥ではないが「もっとよくできる」ところを、
Q1〜Q8 のどれに効くかを言える形で、具体的な代案つきで出してください。
1系列につき最大2件、出ない系列は「S なし」で構いません。
「これだけは歩く前に」と言えるものを5件以内に絞ってサマリの冒頭に置き、
効いていると思ったところも3〜5件、短く書いてください。

この単元は140問あるので、時間が足りなければ (1) と (2) を全問やることを優先し、
見られなかったところは「未確認」に正直に書いてください。
```

---

## 6. 監査のあと（メインの手順）

1. 報告を読み、**Blocker と Major を 1 件ずつ機械で追試する**（指摘が正しいとは限らない。証拠の行を開いて自分で確かめる）
2. 採用した指摘だけ直す。**却下したものは理由を残す**（先生が読んで判断できるように）
2-a. **監査が書いた「代案の数値」も追試する**。数列ユニットでは、**S 10 件のうち 4 件で代案の計算が間違っていた**（提案された和の値・交点の番号・末項が実際と違い、1 件は提案どおりだと 2 列がそもそも整数の番号で交わらなかった）。**診断は正しいのに代案の数値だけ壊れている**、という壊れ方をする。採用するなら値は自分で作り直す
2-a-2. **「代案の数値は書かず、診断だけ書く」と指示すると、追試の負担が消える**（2026-08-20・統計）。
数列では S 10 件中 4 件で代案の計算が壊れていたが、統計の監査は
「代案に新しい数値を書くものは、成立をこの監査で確定していないので**値は採用時にメインが作り直す**」
と自分で宣言し、**壊れた数値はゼロ**だった。**監査ブリーフの S の書式に
「値を変える提案なら、値は書かずに診断と制約だけ書く」を入れると、この形になる。**
そのかわり**メインが「その代案は本当に成立するか」を探索する**（統計では
S-2 の代案が 111 通り・S-4 が 16 通り存在することを機械で確かめてから先生に出した）。

2-b. **S（質を上げる提案）は、メインが勝手に採否を決めない**。①機械で追試できるもの（「書き出しで当たってしまう」等）は追試して事実を確かめ、②「歩く前／歩いた後」の別と、直すコスト（値だけ差し替えれば済むか・系列の作り直しになるか）を添えて、**先生に一覧で出して裁定を仰ぐ**。S は欠陥ではないので、**直さない判断も同じくらい正しい**
3. 直したら `audit_hints.py` → 全問検算 → `tsc` → `build` → commit / push → デプロイ確認
4. **この監査で見つかった「メインの検収が構造的に見落とす層」を `docs/quality_bar_fable_2026-07-17.md` か `CLAUDE.md` に還流する**——次のユニットで同じ穴を開けないため。ここがこのプロセスのいちばんの収穫になる

## 7. 使いどころ（毎回やる必要はない）

- **新ユニットを完結させた直後**（今回のような 100 問規模）は費用対効果が高い
- 1〜2 系列の小さな追加では過剰。メインの検収で足りる
- **先生の歩行フィードバックの代わりにはならない**。歩いて分かることは歩かないと分からないので、監査で歩き出しを止めない（Minor は歩きながら）
