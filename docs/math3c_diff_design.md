# 背骨設計：数Ⅲ・C 第3章「関数の極限と微分」

対象：池田洋介『数学Ⅲ・C 入門問題精講』第3章（**本文 p.74〜110**。第4章「いろいろな関数の微分」は p.111 から）。
設計：メイン Opus 5（2026-09-18 着手）。形式は `docs/backbone_design_handoff.md` の C1〜C18・§4 テンプレに従う。
**状態：素材読解の途中（§進捗表を見る）**。背骨はまだ書いていない。
**具体値は書かない**（C8。値は実装時にオリジナル＋機械検算。出典は「構成を参考・値はすべてオリジナル」）。

**数Ⅲ・C はこれが 5 単元目**。第1章「いろいろな関数」（8系列80問）・第2章「数列の極限」（10系列100問）・
第9章「ベクトル」（13系列130問）・第10章「複素数平面」（11系列110問）はいずれも完結し、横断監査まで通っている。

素材メモ（リポ外・非公開）：`~/ruisuishiki-refs/第３章　関数の極限と微分_jpeg/source_notes.md`
（**写真 38 枚＝本文 p.74〜110 を全部読了・未読ゼロ**。衝突回避リスト＋章全体の見立てつき）。

**★この設計の進め方（Pro プラン）★**：並列委譲をしない。**1 セッション 1〜2 系列**で積み上げ、
毎回 commit・push・デプロイまで通す。再開の作法は **§9 進捗表**
（**新しいセッションの最初の行動は §9 進捗表を読むこと**。2 回目以降は起動プロンプトの §1 を全部読み直さない
——進捗表＋中核原則 memory ＋この背骨で足りる）。

---

## 0. 縦の鎖のどこに刺さるか（`docs/VISION.md` §3・1 行）

**〔ドラフト。裁定前〕**

> VISION の縦の鎖〈**静 → 動**〉の結び目にあたる。
> 中学「変化の割合」→ 数Ⅱ・B「平均変化率 → 微分係数（**多項式だけ**）」→
> **第3章（定義に戻り、多項式の外へ出る）** → 第4章（三角・指数対数）→ 大学「解析学」。
> この単元で起きるのは、**「つながっている」「なめらか」という直感が、
> 1 点での極限という測れる条件に置きかわる**こと。
> あわせて〈**演算の格下げ**〉の鎖にも刺さる——**関数の合成が、変化率のかけ算になる**（p.105 の装置）。
> 上は陰関数微分・多変数の連鎖律・線形近似へ伸びる。

**原典が第3章の範囲を自分で確定させている**（p.111・第4章の扉）：
「ここまでの話で、私たちは**多項式関数、分数関数、無理関数、およびその和、差、積、商や合成という操作で
作られる関数**について、微分ができるようになりました」。
**→ この章の系列で $\sin$・$\log$・$e$ を使わない。** 題材の在庫は
第1章の `math3_frac_graph_01`（分数関数）と `math3_irrational_01`（無理関数）。

---

## 1. 確定事項 D1〜D6

**〔未確定。素材を全部読んでから書く。以下は素材読解の途中で立ち上がった見立てのドラフトであって、
裁定も凍結も経ていない。〕**

### D1（ドラフト）. ハブ胚細胞の候補

原典 p.84 が自分で書いている 1 文が、そのまま胚細胞の核になりそうである：

> 「**直感に頼らない『定義』は、このような怪物と対峙するための武器なのです**」（p.84・ディリクレ関数の直後）

素材から立ち上がった線：

> 数Ⅱ・B では「肩を下ろす」という**手つき**で微分できた。「つながっている」「なめらか」も、
> グラフを見れば分かる気がしていた。**けれど、その直感が全く通用しない関数が実在する**（p.83〜84）。
> だから、直感を **1 点での極限**という測れる条件に置きかえる——連続も、微分可能も、同じ流れで定義し直される。
> そして定義に戻った瞬間、**多項式でなくても微分できる**（積・商・合成・逆）ようになり、
> 同時に「**微分できない点がある**」ということも初めて言えるようになる。

胚細胞的概念を 1 句で（候補）：**「直感を、1 点で測れる条件に翻訳する。翻訳すると、適用範囲と限界が同時に見える」**。

**原典がこの胚細胞を自分で書いている箇所**（素材メモから）：

- p.81「**その最初のステップとして、まずは関数が『各 $x$ の値に対して』連続であるかどうかを定義していきます**」
- p.83 注意1「**極限をとることと代入することが同じことになる**という性質こそ、まさに『連続関数』の性質に他なりません」
- p.84「私たちの『つながっている』という直感が全く役に立たなくなる…**直感に頼らない『定義』は…武器なのです**」
- p.87「数学Ⅱで私たちが扱ったのは『多項式関数』だけでした。…**微分係数が求められることは決して当たり前のことでは
  なくなるのです**」——**数Ⅱ・B の 6 系列との違いを、原典が 1 文で書いている**
- p.89「$f(x)$ を微分可能（な関数）と呼びます。**これは、『連続関数』を定義したときと全く同じ流れですね**」
- p.91「**『微分可能』というのは『連続』の上位互換なのです**」
- p.94「先ほどの操作が、肩の上の数が『負の数』や『分数』のときも同様に成り立っている。**これは実に驚くべきことです**」

### D2（ドラフト）. 入力系ゲート C1 — **この章はこれまでで最もきつい**

**〔§3 の C1 ゲート表は、素材を全部読んでから作る。以下は途中経過。〕**

判定は `TOL=1e-6` の厳密一致で、受け取れるのは **数値 1 つ**（`answer`）か **順不同の集合**（`solutionSet`）だけ。
この章が自然に問いたいことは、**発散の型・真偽（連続か／微分可能か）・順序対（右極限と左極限）・式（導関数）**
——**素の問いはほぼ全滅**する。折り方を設計しないと 1 問も作れない。

**素材読解の途中で見つかった、原典自身が持っている「折れる型」**（＝この章の C1 の土台）：

| 原典の箇所 | 生の問い | 原典が持っている折り方 | 折れるか |
|---|---|---|---|
| 練習問題3（p.78） | 収束するか | **収束するように定数 $a$, $b$ を決めよ** | ○（ただし順序対。下記） |
| 練習問題5（p.85） | $x=1$ で連続か | **連続となるように $a$, $b$ を決めよ** | ○（**割れる**。下記） |
| 練習問題1・2（p.75・77） | 極限値 | そのまま数値 | ○（この章で最も素直） |
| 練習問題6（p.89） | 微分可能か | 微分可能であれば**微分係数を求めよ** | △（$0$ になる型に注意） |
| 練習問題7・8（p.93・96） | 導関数（式） | — | ×（**指定係数か指定点の値**へ折る＝C1 追補6） |
| p.79・80 | 右極限と左極限 | — | ×（**順序対**。C1 追補4。1 問で 2 つ答えさせない） |
| p.74・79 | 発散（$\infty$・$-\infty$） | — | ×（**答えにしない**＝第2章 D2 の確定事項をそのまま継ぐ） |

**練習問題5 は「割れる」**（C1 追補9・11 の判定を当てた結果）：

- **$b$ を出す作業** ＝ 左極限と右極限を**一致させる**（条件①）。$a$ を一度も通らない
- **$a$ を出す作業** ＝ 一致した極限値を $f(1)$ に**合わせる**（条件②）。$b$ を使わない
- 原典自身が①②を**2 つの条件として分けて書いている**ので、「両端が同じ方程式から出る型」には当たらない
- **ただし**：$a$ の答えは左極限の値そのもの。**左極限を問う step を先に置くと $a$ の step がタダになる**（層2）

**練習問題3 は「割れない公算が大きい」**：$b=-2a-4$ で $b$ は $a$ から一意に落ちる。
$a$ を出した人は②に代入するだけ。→ **$b$ を与えて $a$ だけ問う**か、材料置換で別配置にする。

**★この章で特に危ない D2-2（提出値が結論のラベルから言える）の候補**（素材読解中に見つかったもの）：

| 踏みやすい step | 提出値 | なぜ壊れているか |
|---|---|---|
| $y=\frac1x$ が連続でない $x$ の個数 | つねに $0$ | 注意2（p.83）の結論のラベルそのもの |
| $x\lvert x\rvert$ の $x=0$ での微分係数 | つねに $0$ | 構造上の定数（p.90） |
| 「微分可能かつ不連続な関数の個数」 | つねに $0$ | p.91 の定理のラベル |
| $\lvert x\rvert$ の右微分係数と左微分係数の**和** | つねに $0$ | 対称性 |
| $(x^\alpha)'$ の指数から $\alpha$ を引いた値 | つねに $-1$ | 公式のラベル |

**★§10-1（答えが合っても途中が合わない型）が直撃する式と、その対策**：

| 式 | 取り違えが答えに届かない理由 | 値の設計で避ける |
|---|---|---|
| $\dfrac{dy}{dx}=\dfrac1{\,dx/dy\,}$ | $\frac{dx}{dy}=\pm1$ のとき逆数が自分自身 | **$\frac{dx}{dy}=\pm1$ になる点を選ばない** |
| $\dfrac{dy}{dx}=\dfrac{dy}{dt}\cdot\dfrac{dt}{dx}$（装置の変化率の積） | **かけ算は可換**なので $a$ と $b$ を取り違えても積は同じ | **$a\ne b$**。3 段連結なら全部違う値に |
| $\{f(g(x))\}'=f'(g(x))g'(x)$ | 内側が $t=x+c$ だと $\frac{dt}{dx}=1$ で、**かけ忘れても当たる** | **内側の 1 次の係数を 1 にしない** |
| $\left\{\dfrac fg\right\}'=\dfrac{f'g-fg'}{g^2}$ | 分子の引き算の順序を取り違えると**符号だけが変わる**。$f'g=fg'$ になる点では一致 | 提出値を $0$ にしない |

**一般式を書いたら「どの変数について対称か」を 1 行添える**（C1 追補16）。

#### D2（ドラフト）-2. **★この章で最良の発見＝「装置の変化率」は素の数値で問える★**

原典 p.105〜106 の「**合成関数の微分の意味**」は、変化率 $3$ の装置と変化率 $2$ の装置を**棒で連結**すると
合体した装置の変化率が $6$ になる、という絵で公式を説明している。

> $(f\circ g\ \text{の変化率})=(f\ \text{の変化率})\times(g\ \text{の変化率})$

**これは素の数値なので、入力系の拡張なしに、公式の核がそのまま採点できる。**
しかも 3 段連結（$y\leftarrow t\leftarrow s\leftarrow x$）にすれば 3 数の積になり、D2-2 にも落ちない。
**掛け算の合成は小学校の「倍の倍」そのもの**なので、ローフロア（memory `feedback_task_low_floor`）としても正しい。

**しかも第1章のお手本系列 `math3_func_device_01`（関数という装置）が、まさにこの装置の絵で作られている**
——原典が p.105 で同じ装置を再登場させ、p.108 で**入力と出力を入れ替えて**逆関数の微分に使う。
**C13（合流）が、図のレベルで 3 回成立する**（第1章の系列1 → 合成関数の微分 → 逆関数の微分）。

#### D2（ドラフト）-3. 章の構造——**原典が Q7 を章のレベルで閉じている**

**定義に戻る（p.86）→ 公式を預ける（p.94「成り立つものとして、この後は積極的に使っていく」）
→ 道具を増やす（積・商・合成）→ 預けた公式の一部を返す（p.110）。**

- p.97：商の微分の証明は「**後ほど学ぶ合成関数の微分を使う方が簡単ですから、そこで証明します（p107）**」
  → **p.107 の練習問題11 で実際に回収**
- p.94：$(x^\alpha)'=\alpha x^{\alpha-1}$ は「証明はもう少し後（p148）」と預ける
  → **p.110 のコメントで $\alpha=\frac1n$ の場合を返済**
- p.91：「微分可能 ⟹ 連続」が、**p.97 の積の公式の証明で実際に働く**（原典が「(p91)」と指さしている）

**→ 系列の設計で、この伏線構造をそのまま使える。** 最終系列の「問いに戻ると」は返済を書けばよい。

### D3（ドラフト）. 系列の粒度と順序（C9・C10）

**順序は池田本の節順に従う**（先生が紙と並行して歩けることが最優先）。
原典 **9 節＋目次に無い導入 5 ページ・本文 37 ページ**（節の実測ページは素材メモの末尾）。
第2章は 7 節 43 ページで 10 系列、第10章は 15 節 46 ページで 11 系列。

**11 本案（ドラフト）**：

| # | 仮題 | 原典 | 三段 | 備考 |
|---|---|---|---|---|
| 1 | 関数の極限——数列と何が違う | p.74〜76 | — | 第2章からの橋。「とびとび／べったり」。$x\to-\infty$ の符号（C14） |
| 2 | 不定形の原因を取り除く（$x\to a$） | p.77〜78 | — | 約分・有理化。**収束条件から定数を決める** |
| 3 | 右極限と左極限——どちらから近づくか | p.79〜80 | — | 片側の矢印・○ と ●。$\frac{\lvert x\rvert}{x}$ の伏線 |
| 4 | 関数の連続——つながっているを定義する | p.81〜85 | — | A/B/C。**連続になるように定数を決める**（C1 の本命） |
| 5 | 微分の定義——当たり前でなくなる | p.86〜90 | — | **§3 の裁定次第**。数Ⅱ・B との差は p.87 の 1 文 |
| 6 | 微分可能と連続——上位互換 | p.91 | **○** | **章の山場（C12）**。段2 は練習問題3（収束の必要条件）の再利用 |
| 7 | 導関数と $(x^\alpha)'$——肩は負でも分数でもよい | p.92〜94 | **○** | 段1 の材料が原典に揃っている（p.94「並べてみましょう」） |
| 8 | 積の微分——中継地点 | p.94〜97 | **○** | 段1＝展開して微分と一致（p.95）。段2＝中継地点（p.97） |
| 9 | 商の微分と微分の記号 | p.95〜99 | — | $\frac{dy}{dx}$。**証明は系列10 へ預ける**（原典どおり） |
| 10 | 合成関数の微分法——装置をつなぐ | p.100〜107 | **○** | 装置の絵（p.105）。**商の公式の証明を回収**（p.107） |
| 11 | 逆関数の微分法——入れ替えると逆数 | p.108〜110 | — | 軸を入れ替える 3 図。$(x^{1/n})'$ の返済 |

**懸念が 2 つある**：

1. **三段が 4 本は多い**（第10章は 11 本中 2 本）。**系列7 と 8 のどちらかを適用だけにする**か、
   **系列9 を系列8 に吸収**して 10 本にするのが現実的。**先生の裁定事項にする**
2. **系列5 を立てるかどうかが §3 の裁定に依存する**（数Ⅱ・B の 6 系列との重複）。
   立てないなら 10 本、内容を系列6 の前半に畳む

**節の長さの実測**：合成関数の微分法が **8 ページで最長**（p.100〜107）。
微分可能と連続は **1 ページ**（p.91）だが、**章の山場はここ**——
C9 の厚み基準（単独で 5 オペレータ・10 step を満たせるか）で見ると、
**ページ数と系列の厚みが一致しない唯一の節**。独立させる根拠は「山場だから」であって「長いから」ではない。

### D4（ドラフト）. 図（★実測した。起動プロンプトの流用候補は、ほとんど使えない★）

**実測値（メインが自分で数えた・層10）**：`Math.tsx` の図マーカーは **493 枚**
（起動プロンプトは「496 枚」。数え方の違いか、3 枚の差がある。**この種の数は毎回自分で数える**）。

**★流用候補を 1 枚ずつ実物で読んだ結果（C6 の精緻化・層4）★**

| 候補 | 実物の中身（`aria-label`・`<text>`） | 判定 |
|---|---|---|
| `CALC_SECANT_TO_TANGENT` | 「固定した左の点から、近づいてくる 3 つの右の点へ引いた 3 本の直線」＋「**近づけていくと、直線のかたむきはどこへ向かう？**」 | **○ 流用可**。原典 p.86 の図とほぼ同じで、問いで終えている（Q5） |
| `CALC_SECANT` | 割線 1 本 | **○ 流用可**（入口用） |
| `CALC_DERIV_MACHINE` | 「点を入れるたびに、中では計算をやり直している？ **それとも、この箱の中身は 1 本の式で書けている？**」 | **× 却下**。キャプションが**数Ⅱ・B の導関数の系列の問いそのもの**。第3章で出すと「前の単元の話が突然現れる」 |
| `CALC_AVG_VS_INSTANT` | 「区間ぜんたいは下向き。では左端ちょうどでは、どちらを向いている？」 | **× 却下**。同上（数Ⅱ・B の平均 vs 瞬間の問い） |
| `CALC_ABS_FOLD` | 「$x$ 軸より下にへこんだ曲線を折り返す」＋「**折り返してから足すのと、足してから折り返すのは、同じ答えになる？**」 | **× 却下**。$y=\lvert x\rvert$ の V 字ではなく $\lvert f(x)\rvert$ の折り返しで、**キャプションは積分の文脈**。層4 の典型 |
| `FUNC_BOX_STEP1`（第1章） | 「$x$ を入れると $y$ がただ 1 つ出てくる関数の箱」 | **× 却下**。**値の対応の絵で、変化率の絵ではない**。p.105 の装置とは別物 |
| `M3F_DEVICE_CHAIN`（第1章） | 「式の箱の出口を、**素数かどうかを判定する箱**の入口につないだ図」 | **× 却下**。**素数判定という第1章固有の文脈がハードコード**。層4 の典型 |
| `M3L_FLOOR_STAIRS`（第2章） | $y=[x]$ の階段。「左の端（●）でその高さになり、右の端（○）は次の段のもの」＋ $y=x,\ y=x-1$ の破線 | **△**。**●／○ の描き分けの先例として実在する**（起動プロンプトは「既存に無い」と書いていたが**あった**）。ただし 2 本の破線が**はさみうちの文脈**なので、そのままは使わない |

> **★起動プロンプト §4-4 の流用候補 8 枚のうち、実物を読んで残ったのは 2 枚だけ★**
> （`CalcSecant`／`CalcSecantToTangent`）。**第10章で `UNIT_CIRCLE_*` を開いて却下したのと同じ型**。
>
> **★訂正：「●／○ の描き分けは既存 496 枚に無い」は誤り★**。
> `M3L_FLOOR_STAIRS` に**ある**。ただし 1 枚だけで、文脈は第2章のはさみうち。
> **新規で起こすときは、この 1 枚の視覚言語（●＝含む／○＝含まない）を継承する**。

**新規が確実なもの**：片側からの矢印（$x\to a+0$・$x\to a-0$）／A・B・C の 3 分類（p.82）／
連続と微分可能の 3 列表（p.91）／**$y\leftarrow t\leftarrow x$ の連鎖図**（p.101）／
**装置を棒で連結する絵と変化率のかけ算**（p.105〜106）／**逆関数の 3 図・軸の入れ替え**（p.109）。

**★層8 の補足に注意★**：p.109 の図3 は「**逆数になる**」という発見そのものを描いている。
**質的変化 step の図には使えない**——仕組み（軸を入れ替える）だけを描き、逆数になることは問いに預ける。

### D5（ドラフト）. 辞書（★実測した。起動プロンプトの「8 語」は少なすぎた★）

**実測値（C16 の正しい grep＝引用符つきキーも拾う形で自分で確かめた）**：総キー数 **326**
（起動プロンプトは「327」。1 の差がある）。

**関連する既存キーは 8 語ではなく 13 語**：

| キー | 現在の `short`（要点） | 現在の `relatedSeriesId` | 第3章での扱い |
|---|---|---|---|
| `平均変化率` | 2 点のあいだでならした変わり方 | `algebra2_calc_rate_01` | そのまま（数Ⅱ・B が本家） |
| `微分係数` | 1 点での変わり方・接線の傾き | `algebra2_calc_rate_01` | **追記**（存在しないことがある） |
| `極限値` | 近づけていった先の数 | `algebra2_calc_rate_01` | **追記**（$x\to a$・右と左） |
| `導関数` | 点ごとの微分係数を 1 本の式に | `algebra2_calc_derivative_01` | そのまま |
| **`微分する`** | 「**肩の数を前に下ろして、肩を 1 つ減らす**。和・差・定数倍には分けられるが、**積には分けられない**」 | `algebra2_calc_derivative_01` | **★書き換え必須★**（下記） |
| `接線` | 傾きは微分係数に等しい | `algebra2_calc_tangent_01` | **追記**（決まらないことがある） |
| `収束` | 「**番号を大きくしていくと**」 | `math3_lim_intro_01` | **★追記必須★**（数列限定の書き方） |
| `発散` | 「**数列が**収束しないこと」 | `math3_lim_intro_01` | **★追記必須★**（同上） |
| `振動` | 数列が発散するうち…… | `math3_lim_intro_01` | 追記 |
| `不定形` | $\infty-\infty$・$\frac\infty\infty$ | `math3_lim_poly_01` | 追記（$\frac00$ を足す） |
| `ガウス記号` | $x$ を超えない最大の整数 | `math3_lim_squeeze_01` | 追記（$x\to a$ での不連続） |
| `合成関数` | 内側の $g$ が先に動く | `math3_func_compose_01` | 追記（微分法へのリンク） |
| `逆関数` | 装置を裏返して出口から入口をたどる | `math3_inverse_01` | 追記（微分法へのリンク） |

**★`微分する` の現状は、この章で 2 か所とも破れる★**（起動プロンプトの指摘は正しかった）：

1. 「**肩の数を前に下ろして、肩を 1 つ減らす**」は多項式限定の言い方。
   原典 p.94 が $(x^\alpha)'=\alpha x^{\alpha-1}$（$\alpha$ は実数）へ一般化する
2. 「**積には分けられない**」は**正しいが不十分**。原典 p.95 が
   「分けられないが、**積や商においても使える公式はちゃんと存在します**」と続ける

**新規候補（11 語）**：右極限／左極限／関数の連続／不連続／微分可能／積の微分／商の微分／
微分の記号（$\frac{dy}{dx}$）／合成関数の微分法／チェインルール／逆関数の微分法。
**`連続`・`微分可能`・`右極限`・`左極限` は 1 語も無い**ことを grep で確認済み。

**★層9 の規律**：数値例は **`relatedSeriesId` 先を先に外す**。
この章は `algebra2_calc_rate_01` と `algebra2_calc_derivative_01` にリンクが集中するので、
**その 2 系列の `answer` を先に抽出して突き合わせる**。

### D6（ドラフト）. 値の設計の急所（C1 追補2 の具体化）

- **微分係数・導関数の値は「有理数で出る点」を選ぶ**：
  $\sqrt x$ 型は **$x$ を平方数**に（$f'(9)=\frac16$）／$\sqrt[3]{x}$ 型は **$x$ を立方数**に（$f'(8)=\frac1{12}$）／
  $\frac1{x^n}$ 型はそのまま有理数
- **$\frac{x}{\sqrt{x^2+1}}$ 型は $x^2+1$ が平方数になる $x$**（$x=0,\sqrt3,2\sqrt2$）。
  ただし $\sqrt3$ を答えの中に入れない（$\pi$ と $\sqrt{\ }$ の併用を避ける方針は第10章 D2-4 を継承）
- **合成関数の「かけ忘れ」を検出するため、内側の 1 次の係数を 1 にしない**（追補14 検算1）
- **逆関数の微分で $\frac{dx}{dy}=\pm1$ になる点を選ばない**（逆数が自分自身になる）
- **装置の変化率は $a\ne b$**（積は可換なので取り違えが検出されない）
- **入口の提出値を $0$・$1$ にしない**（D2-2）
- **分数は既約で**。`answerDisplay` に既約分数、`questionText` に「既約分数で」

---

## 2. 系列の背骨

**〔未着手〕**

---

## 3. 数Ⅱ・B の微分 6 系列との関係（★最大の論点・**実物を開いて確かめた**★）

`frontend/src/lib/seriesCalculus.ts`（4083 行）に 11 系列。うち微分は 6 本で、
**第3章とぶつかるのは先頭の 2 本だけ**であることが、実物を読んで分かった。

### 3-1. 実測——`algebra2_calc_rate_01`（平均から瞬間へ）

- **中心の問い**：「$2$ 点を比べなければ『変わり方』は測れない。では、**たった $1$ 点での変わり方**は、どうやって測る？」
- **10 問の中身**（`answer` を抽出）：水そう $f(t)=t^2+2t$ の平均変化率（$11$・$18$）→ $f(x)=x^3-2x$（$26$）→
  1 次関数はどこでも同じ（$4$）→ 逆向き（平均変化率が $12$ になる $a$＝$8$）→ 幅を $0.5$ に縮める（$6.5$）→
  **$h$ で置いて $\square+h$ の形に整理**（$6$）→ **$h\to0$ の極限**（$12$）→ 平均と瞬間が食い違う（$9$）→
  $f'(a)=0$ となる $a$（$3$）
- **扱う関数は全部多項式**（$t^2+2t$・$x^3-2x$・$4x-7$・$12x-x^3$・$2x^2-12x+5$）

### 3-2. 実測——`algebra2_calc_derivative_01`（点の答えを 1 本の式にする）

- **中心の問い**：「点を変えるたびに極限を計算し直すのは、**なぜやめられる**？」
- **10 問の中身**：$3x^2+5x$ の導関数の係数（$5$）→ $2x^3$（$6$）→ $5x^4$（$20$）→ 項ごとに分ける（$7$）→
  **逆向き**（$f'$ から $f$ の最高次の係数＝$2$）→ 積の形（$17$）→ $(x+3)^3$（$27$）→ $f'(2)=7$ となる $k$（$-2$）→
  $x^{12}$ 型（$-45$）→ **二項定理から $(x^n)'=nx^{n-1}$ をその場で作る**（$16$）
- **扱う関数は全部多項式**。**$(x^n)'$ の $n$ は自然数**（二項定理で導いている）

### 3-3. 裁定材料（**実測の結果、重複ではないと言える**）

**原典 p.86 は「微分の定義自体は数学Ⅱで学んだものと何ら変わりがありません。ここで簡単に振り返ってみましょう」
と書いている**が、**p.87 が違いを 1 文で名指している**：

> 「数学Ⅱで私たちが扱ったのは『**多項式関数**』だけでした。多項式関数では、**この極限値は必ず存在していた**ので、
> それが『存在するかどうか』などを考える必要はなかったのです。しかし、これから私たちが相手にする
> 『**一般の関数**』では、この極限値は必ずしも存在するとは限りません。
> つまり、**微分係数が求められることは決して当たり前のことではなくなるのです**」

**実測がこの 1 文を裏づけた**：

| | 数Ⅱ・B の 2 系列（実測） | 第3章（原典） |
|---|---|---|
| 扱う関数 | **多項式のみ**（実測で全 20 問とも） | $\frac1x$・$\frac1{x^2}$・$\sqrt x$・$\lvert x\rvert$・$\sqrt[3]{x}$ |
| 極限の存在 | **問わない**（必ず存在する） | **主題**（$\lvert x\rvert$ で存在しない） |
| $(x^n)'$ の $n$ | **自然数**（二項定理で導出） | **実数**（$(x^\alpha)'=\alpha x^{\alpha-1}$・p.94） |
| 中心の問い | 「1 点での変わり方をどう測る？」／「なぜ計算し直さずに済む？」 | 「**その測り方は、いつ失敗する？**」 |

> **1 文で言える違い**：
> **数Ⅱ・B は「1 点での変わり方を測る方法」を教えた。第3章は「その方法が、いつ失敗するか」を問う。**
> 失敗する場所（尖った点）を見つけたことで、初めて「失敗しない」ことの中身
> （＝微分可能・連続）を定義できるようになる。

**→ メインの推奨**：**系列5「微分の定義」は立てる。ただし多項式を 1 問も使わない。**
入口から $\frac1x$・$\sqrt x$ で定義に戻り、山場を $\lvert x\rvert$ に置く。
数Ⅱ・B の 2 系列は**土台として名指しで呼び出す**（C13・step1 の既知→未知の橋）。

**残る重複の懸念は系列7（導関数と $(x^\alpha)'$）**：
`algebra2_calc_derivative_01` が**すでに二項定理で $(x^n)'=nx^{n-1}$ を作らせている**。
第3章の新しさは「**肩が負の数や分数でも同じ形が成り立つ**」こと（p.94 が「実に驚くべきことです」と書く）。
**段1 の事例（$\frac1x,\frac1{x^2},\sqrt x$）は全部非多項式**なので重複しないが、
**系列7 の前半が数Ⅱ・B の焼き直しにならないよう、指数表記への書き直しを入口にする**のが安全。

---

## 4. ★C1 ゲート表——原典の全問について「素の答えの型」を数える★

起動プロンプト §3 の指示どおり、**原典が実際に問うている 29 問（練習問題12 題の小問）＋本文の例**
について、素の答えの型と折り方を 1 行ずつ確定させる。

| 原典 | 素の問い | 素の答えの型 | 折れるか | 折り方（確定案） |
|---|---|---|---|---|
| 練1(1)(2) | $x\to\infty$ の極限 | **数値** | **○** | そのまま |
| 練1(3)(4) | $[\infty-\infty]$ の極限 | **数値** | **○** | そのまま。**(4) は符号が C14** |
| 練2(1)(2)(3) | $x\to a$ の $\left[\frac00\right]$ | **数値** | **○** | そのまま（この章で最も素直） |
| 練3 | 収束するような $a,b$ | **順序対** | **×→○** | **$b$ を与えて $a$ だけ問う**（$b$ は $a$ から一意に落ちるので割れない） |
| 練4(1)(2) | 片側極限が発散 | **$\infty$・$-\infty$** | **×** | **答えにしない**。「$+\infty$ に発散する側はどちらか」も 2 択で不可 → **分母の符号が変わる $x$ の値**へ |
| 練4(3) | $\lim$ が存在しない | **真偽** | **×→○** | **右極限と左極限の差**（$1-(-1)=2$）。$0$ にならない設計に |
| 練5 | 連続となるような $a,b$ | **順序対** | **×→○** | **割れる**（条件①と②は別の作業）。ただし**左極限の step を先に置かない**（層2） |
| 練6(1) | 微分可能か | **真偽** | **×→○** | **右微分係数と左微分係数の差**（$0-2=-2$） |
| 練6(2) | 微分可能・微分係数 | **数値だが $0$** | **×→○** | **$x=0$ 以外の点で問う**（$0$ は構造上の定数＝D2-2） |
| 練7(1)(2) | 定義から導関数 | **式** | **×→○** | **指定点の値**（平方数・立方数を選ぶ）／**係数** |
| 練8(1)〜(4) | 積・商の微分 | **式** | **×→○** | **指定係数**（1 step 1 係数）／**指定点の値** |
| 練9(1)(2) | どの文字で微分するか | **式** | **×→○** | **同じ式を $x$ で／$t$ で微分した、指定点での値**（**割れる**・片方を写すと必ず外れる） |
| 練10(1)〜(5) | 合成関数の微分 | **式** | **×→○** | 指定点の値／**$\frac{dy}{dt}$ と $\frac{dt}{dx}$ を別 step**（割れる） |
| 練11 | 商の公式を示せ | **証明** | **×→○** | **鎖の関節の数値**（C1 追補5）。関節は 3 つ |
| 練12 | 逆関数の微分 | **式** | **×→○** | 指定点の値（立方数） |
| 本文 p.82 | A/B/C のどれか | **分類** | **×→○** | **「連続でない点の個数」**（母集団 4〜5 を用意できるか要確認） |
| 本文 p.83 | $\frac1x$ は連続関数か | **真偽** | **×** | **問わない**（答えが「$0$ 個」で D2-2）。derivation で扱う |
| 本文 p.84 | ディリクレ関数 | **真偽** | **×** | **採点しない**。derivation の「もっと深く」へ |
| 本文 p.91 | 微分可能 ⟹ 連続 | **定理** | **×→○** | **段2 の関節**（分母 $\to0$ で有限に収束するには分子 $\to0$）＋「(連続,微分可能)=(○,×) に入る関数の個数」 |
| 本文 p.94 | $(x^\alpha)'=\alpha x^{\alpha-1}$ | **公式** | **×→○** | **肩の数 $\alpha$**／指定点の値 |
| **本文 p.105** | **合体した装置の変化率** | **数値** | **○** | **そのまま**（この章で最良。3 段連結なら 3 数の積） |
| 本文 p.108 | 逆関数の変化率 | **数値** | **○** | そのまま（$\frac{dx}{dy}=\pm1$ を避ける） |

### 結論：**入力系の拡張は不要**（実運用 10 例目）

折れなかったのは **3 つだけ**で、いずれも**採点しないのが正しい**もの：

1. **発散の型**（$\infty$・$-\infty$）——第2章 D2 の確定事項（「$\infty$ は値ではない」と原典 p.34 が明言）をそのまま継ぐ
2. **$y=\frac1x$ は連続関数か**——答えが「$0$ 個」になり D2-2 に落ちる
3. **ディリクレ関数**——derivation の「もっと深く」で言葉として扱う

**答えの型のばらけ（D2 の確認）**：整数（極限値・個数・差・係数・変化率）／
分数（$\frac14$・$\frac16$・$\frac1{12}$ 型の微分係数）／`["sqrt"]`（$\sqrt{\ }$ を残す step）の 3 種。
**整数ばかりには偏らない**——微分係数が主役になる系列では分数が自然に多くなる。

---

## 8. ★先生に伺いたいこと（Q1〜Q5）★

**〔まだ裁定を受けていません。ここに答えをいただいてから凍結へ進みます。〕**

### Q1. 系列5「微分の定義」を立てるか（**最大の論点**）

数Ⅱ・B の `algebra2_calc_rate_01`・`algebra2_calc_derivative_01` の実物を読みました（§3）。
**20 問とも多項式だけ**で、**極限が存在するかどうかは一度も問うていません**。

- **推奨＝立てる。ただし多項式を 1 問も使わない。**
  中心の問いは「数Ⅱ・B で手に入れた測り方は、**いつ失敗する**？」の線。
  入口から $\frac1x$・$\sqrt x$ で定義に戻り、山場を $\lvert x\rvert$ に置く
- 代案＝立てず、内容を系列6（微分可能と連続）の前半に畳んで **10 本**にする

### Q2. 三段（Q9）をどの系列でやるか

法則が 5 つあります（$(x^\alpha)'$・積・商・合成・逆）。**全部を三段にすると系列数が爆発します**
（第10章は 11 本中 2 本だけ三段にしました）。

- **推奨＝三段は 3 本**：系列7（$(x^\alpha)'$）・系列8（積の微分）・系列10（合成関数の微分法）。
  **この 3 つは段1 の材料を原典が持っている**（p.94「並べてみましょう」／p.95「展開してから微分しても同じ結果」／
  p.100「同じ結果を別の方法で導いてみたい」）。
  商（系列9）と逆（系列11）は**適用だけ**——商の証明は系列10 が回収し、逆は系列11 の中で 1 回だけ導く
- 代案＝2 本（系列8・10 だけ）に絞る

### Q3. 系列数（11 本か 10 本か）

Q1 と Q2 の答えで決まりますが、**推奨は 11 本**（§1 D3 の表）。
節が 9 つ＋目次に無い導入 5 ページで、合成関数の微分法だけが 8 ページと長いので、
**p.100〜107 を 1 系列に収める**のが原典の節に忠実です。

### Q4. 「微分可能と連続」（p.91・**わずか 1 ページ**）を独立系列にするか

**推奨＝独立させる。** 根拠は長さではなく、**章の山場（C12）だから**です。
「微分可能 ⟹ 連続、逆は成り立たない」は**反例が主役になる珍しい単元**で、
$\lvert x\rvert$（連続だが微分不可）と $x\lvert x\rvert$（両方○）が**見た目そっくりなのに結論が逆**という、
質的変化（C11）に最適な一組を原典が用意しています（p.90 のコメント）。

### Q5. 図の新規を何枚まで見込むか

**実物を読んだ結果、流用できるのは 2 枚だけでした**（§1 D4）。
起動プロンプトが挙げた 8 枚のうち 6 枚は、**キャプションが前の単元の問いでハードコード**されています。

- **推奨＝新規 15〜18 枚を見込む**。とくに ①片側からの矢印と ●／○ ②A・B・C の 3 分類
  ③連続と微分可能の 3 列表 ④$y\leftarrow t\leftarrow x$ の連鎖 ⑤**装置を棒で連結する絵**
  ⑥**逆関数の軸の入れ替え 3 図** は新規が確定です
- **●／○ の描き分けは既存に 1 枚あります**（第2章 `M3L_FLOOR_STAIRS`）。その視覚言語を継承します

---

## 9. 進捗表（セッションをまたぐときはここを最初に読む）

| 項目 | 状態 | 最終更新 |
|---|---|---|
| 素材メモ（写真 38 枚） | **済＝38 枚すべて読了・未読ゼロ**（本文 p.74〜110＝37 枚、`IMG_4015`＝p.111 は第4章の扉）。衝突回避リスト・節の実測ページ・章全体の見立てまで記載 | 2026-09-18 |
| 欠番 `IMG_4000` の判定 | **済＝ページの抜けではない**（`IMG_3999`＝p.96、`IMG_4001`＝p.97 と連続することを実物で確認）。カメラ採番の飛び。**先生への報告は不要** | 2026-09-18 |
| 実測（数Ⅱ・B の微分 6 系列・辞書・図） | **済＝全部自分で確かめた**。§3（系列の実物）・§1 D4（図を 1 枚ずつ）・§1 D5（辞書を grep）。**起動プロンプトの実測と 3 か所食い違った**（下記） | 2026-09-18 |
| C1 ゲート表（§4・全問の答えの型） | **済**。折れなかったのは 3 つだけで、いずれも採点しないのが正しい。**入力系の拡張は不要**（実運用 10 例目） | 2026-09-18 |
| 背骨 D1〜D6 | **ドラフト（6 つとも書いた）**。**裁定も凍結も経ていない** | 2026-09-18 |
| **先生の裁定（§8 の Q1〜Q5）** | **★待ち★** | — |
| Round 1 監査（背骨・Grok） | 未 | — |
| 凍結 | 未 | — |
| 系列1（お手本） | 未 | — |
| 系列2 以降 | 未 | — |
| 横断検収（層1〜12） | 未 | — |
| Round 2 監査（実装・Grok） | 未 | — |
| Round 3 監査（作り替えた step だけ） | 未 | — |
| 還流（背骨・handoff・quality_bar・memory） | 未 | — |

### 次にやること（1 行）

> **先生に §8 の Q1〜Q5 の裁定をいただく。** そのあと §2（系列の背骨・10 step 表）を書き、
> Round 1（背骨監査・Grok）へ回す。

### セッション1（2026-09-18）でやったこと

- 起動プロンプト §1 の memory 12 本とリポジトリの docs（`CLAUDE.md`・`backbone_design_handoff.md` の C1〜C18＋追補1〜16・
  `quality_bar` の層1〜12・第10章の背骨 §1）を読了
- **写真 38 枚を全部読了**（5枚ずつ 7 バッチ＋3 枚。そのつど素材メモへ追記）。
  衝突回避リスト（練習問題12 題＋本文の値）・節の実測ページ・章全体の見立てまで書き切った
- **欠番 `IMG_4000` を実測で決着**（ページの抜けではない）
- 本ファイルを新規作成し、D1〜D3 のドラフトと進捗表を置いた

### この素材読解でいちばん大きかった発見

1. **胚細胞の核を、原典が自分で 1 文で書いていた**（p.84）——
   「**直感に頼らない『定義』は、このような怪物と対峙するための武器なのです**」
2. **C1 の折り方を、原典が 4 本とも持っていた**——真偽 → 定数の決定（練習問題3・5）／
   式 → 指定点の値／**装置の変化率（素の数値）**／微分可能でない点。**入力系の拡張は不要と見込む**
3. **装置の変化率（p.105）が、この章で最良の折り方**。
   第1章のお手本 `math3_func_device_01` と同じ装置で、合成（p.105）と逆関数（p.108）の両方に効く
4. **章の構造が Q7 を自分で閉じている**——預けた公式を、増やした道具で返す（p.94 → p.110、p.97 → p.107）
5. **三段（Q9）の段1 の材料が、原典に全部ある**（p.94「並べてみましょう」・p.95「展開してから微分しても同じ結果」）

### ★起動プロンプトの実測と食い違ったところ（層10・自分で数え直した結果）★

| 項目 | 起動プロンプト | メインの実測 | 効き方 |
|---|---|---|---|
| 図マーカーの総数 | 496 枚 | **493 枚** | 小さいが、**この種の数は毎回自分で数える** |
| 辞書の総キー数 | 327 | **326** | 同上 |
| 辞書の関連既存キー | **8 語** | **13 語**（`振動`・`不定形`・`ガウス記号`・`合成関数`・`逆関数` が抜けていた） | **更新の対象が 5 語増える** |
| 図の流用候補 | 8 枚（`CalcSecant` ほか） | **実物を読んで残ったのは 2 枚**。6 枚は**キャプションが前の単元の問いでハードコード** | **新規 15〜18 枚を見込む必要がある** |
| ●／○ の描き分け | 「既存 496 枚に無い」 | **`M3L_FLOOR_STAIRS`（第2章）に**ある**** | 新規で起こすとき**その視覚言語を継承する** |
| 欠番 `IMG_4000` | 「本文が 1 ページ抜けている可能性が高い」 | **抜けていない**（カメラ採番の飛び） | 報告不要 |

**どれも起動プロンプトを疑ったから出た**。C6 の精緻化（流用候補は実物を見て却下してよい）と
quality_bar 層10（機械で確かめたと書いた検査そのものが盲点になる）が、**そのまま 6 件とも当たった**。

### この章で先に分かっている「気をつけること」

- **図**：穴あき丸（○）と塗り丸（●）の描き分け・**片側からの矢印**が新しい事故源。
  既存 496 枚に無い型。**図は必ず PNG にして目で見る**（第10章で実バグ 28 件・すべてソースでは見えなかった）
- **辞書**：`微分する` の short「肩の数を前に下ろして、肩を 1 つ減らす」は**多項式限定の書き方**。
  原典 p.94 が $(x^\alpha)'=\alpha x^{\alpha-1}$ へ一般化するので**書き換えが要る**。
  `収束`・`発散` の short は数列限定（「番号を大きくしていくと」）なので $x\to a$ 版の追記が要る
- **三段（Q9）の対象が多い章**：$(x^\alpha)'$ の一般化・積の微分・商の微分・合成関数の微分法・逆関数の微分法。
  **どれを三段にしてどれを適用だけにするかが設計判断**（第10章は 11 系列中 2 本だけを三段にした）。
  ただし**段1 の材料は原典がほぼ全部持っている**（p.94 の「並べてみましょう」・p.95 の「展開してから微分しても同じ結果」）
